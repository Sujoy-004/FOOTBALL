"""Prediction signal ingestion adapters for surviving cache-built signals.

Provides market odds (odds.py), rolling form (rolling_form.py),
squad value (squad_value.py), and rest days (rest_days.py)
cache computation in the World Cup cache-dict format.
"""
