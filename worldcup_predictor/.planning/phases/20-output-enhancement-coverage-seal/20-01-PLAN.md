---
phase: 20-output-enhancement-coverage-seal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/enrichment.py
  - src/output.py
  - tests/test_enrichment.py
  - tests/test_output.py
autonomous: true
requirements: [V2-30]

must_haves:
  truths:
    - "3 new stat fields (fouls, corner_kicks, shots_off_target) extracted from BSD live_stats for every ingested match"
    - "3 new context fields (venue_city, home_coach.name, away_coach.name) extracted from BSD event"
    - "Coverage auditor reports meaningful coverage (47-field denominator) and raw coverage"
    - "Coverage auditor shows per-category breakdown (Prediction, Display, Operational)"
    - "Coverage auditor pass/fail gated on ≥60% meaningful target"
    - "All existing tests continue to pass with zero regressions"
  artifacts:
    - path: src/enrichment.py
      provides: "3 new stat entries + 3 new context entries in field maps"
      min_lines: 38
    - path: src/output.py
      provides: "coverage_audit() + print_coverage_audit() functions"
      contains: "def coverage_audit"
    - path: tests/test_enrichment.py
      provides: "Tests for fouls/corners/shots_off_target extraction + venue_city/coach context extraction"
      contains: "def test_fouls_corners_shots_off"
    - path: tests/test_output.py
      provides: "TestCoverageAudit class with meaningful/raw/category assertions"
      contains: "class TestCoverageAudit"
  key_links:
    - from: src/enrichment.py::_STATS_FIELD_MAP
      to: "BSD live_stats.home.{field} / live_stats.away.{field}"
      via: "extract_stats() loop — same pattern as existing 8 entries"
      pattern: "\"fouls_home\""
    - from: src/output.py::coverage_audit()
      to: "47 meaningful fields (11 prediction + 27 display + 9 operational)"
      via: "hardcoded field classification lists"
      pattern: "_PREDICTION_FIELDS"
    - from: src/output.py::print_coverage_audit()
      to: "coverage_audit() dict"
      via: "format and print meaningful/raw/category coverage"
      pattern: "Coverage Audit"

---

<objective>
Add 3 high-value stat fields (fouls, corner_kicks, shots_off_target) + 3 context fields (venue_city, home_coach, away_coach) to the BSD enrichment pipeline, and implement the automated coverage auditor that measures ≥60% meaningful field coverage.

Purpose: Seal Phase 17's enriched match context with field extraction that Phase 20's focus card will display. The coverage auditor provides the V2-30 measurement mechanism — automated, per-category, dual-metric (meaningful + raw).

Output:
- src/enrichment.py: extended _STATS_FIELD_MAP (+6 entries) and _CONTEXT_SOURCE_MAP (+3 entries)
- src/output.py: coverage_audit() pure function + print_coverage_audit() display function
- tests/test_enrichment.py: new field extraction tests + context tests
- tests/test_output.py: TestCoverageAudit class
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-output-enhancement-coverage-seal/20-CONTEXT.md
  — D-18: prioritized extraction for fouls, corner_kicks, shots_off_target (6 lines in _STATS_FIELD_MAP)
  — D-15: target ≥60% meaningful field coverage, denominator = 47 meaningful fields
  — D-16: auditor reports meaningful + raw, by value category
  — D-17: meaningful coverage carries target, raw is informational
  — D-19: coach names appear inside focus card only

@.planning/phases/20-output-enhancement-coverage-seal/20-RESEARCH.md
  — Lines 570-675: coverage_audit() implementation pattern, field classifications
  — Lines 165-189: _STATS_FIELD_MAP extension pattern (6 lines)
  — Lines 191-204: _CONTEXT_SOURCE_MAP extension pattern (3 lines)

@src/enrichment.py
  — Lines 17-26: existing _STATS_FIELD_MAP (8 entries), append 6 new entries
  — Lines 28-31: existing _CONTEXT_SOURCE_MAP (2 entries), append 3 new entries
  — Lines 34-64: extract_stats() loop — automatically picks up new _STATS_FIELD_MAP entries (no loop changes needed)
  — Lines 67-84: extract_context() loop — automatically picks up new _CONTEXT_SOURCE_MAP entries (no loop changes needed)

@tests/test_enrichment.py
  — Lines 8-32: FINISHED_EVENT fixture — base event dict used by all tests
  — Line 62: TestExtractStats class — add test_fouls_corners_shots_off + test fixtures
  — Line 95: TestExtractContext class — add test_venue_city + test_coach_names

@tests/test_output.py
  — Lines 62-71: _capture() helper — reusable for coverage audit tests
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend enrichment field maps + extraction tests</name>
  <files>src/enrichment.py, tests/test_enrichment.py</files>
  <read_first>
    Read src/enrichment.py in full — every line. This is the existing pattern you must extend.
    Read tests/test_enrichment.py in full — every line. Note FINISHED_EVENT structure, existing test fixture patterns.
  </read_first>
  <action>
    Per D-18, D-19:

    In src/enrichment.py:
    1. Append 6 entries to _STATS_FIELD_MAP (after line 26, before line 28):
       - "fouls_home": "fouls"
       - "fouls_away": "fouls"
       - "corner_kicks_home": "corner_kicks"
       - "corner_kicks_away": "corner_kicks"
       - "shots_off_target_home": "shots_off_target"
       - "shots_off_target_away": "shots_off_target"
    2. Append 3 entries to _CONTEXT_SOURCE_MAP (after line 31, before the blank line before extract_stats):
       - "venue_city": ("venue", "city")
       - "home_coach": ("home_coach", "name")
       - "away_coach": ("away_coach", "name")
    3. Do NOT modify extract_stats() or extract_context() — the existing loops iterate the maps and pick up new entries automatically.

    In tests/test_enrichment.py:
    4. Add a new test event fixture FINISHED_EVENT_WITH_NEW_FIELDS after the existing fixtures (~line 59). Base it on FINISHED_EVENT, add "fouls", "corner_kicks", "shots_off_target" to both home/away in live_stats, and add "home_coach": {"name": "Gerardo Martino"}, "away_coach": {"name": "Hugo Broos"} at top level.
    5. In class TestExtractStats (after test_partial_stats), add test method test_fouls_corners_shots_off — assert stats is not None, assert fouls_home==12, fouls_away==8, corner_kicks_home==7, corner_kicks_away==3, shots_off_target_home==4, shots_off_target_away==2.
    6. In class TestExtractContext (after test_no_context), add test_venue_city — assert ctx.get("venue_city") == "Mexico City" — and test_coach_names — assert ctx.get("home_coach") == "Gerardo Martino", ctx.get("away_coach") == "Hugo Broos".
  </action>
  <verify>
    <automated>pytest tests/test_enrichment.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/enrichment.py` has 14 entries in _STATS_FIELD_MAP (up from 8)
    - `src/enrichment.py` has 5 entries in _CONTEXT_SOURCE_MAP (up from 2)
    - `pytest tests/test_enrichment.py -x -q` passes all existing + 3 new tests
    - extract_stats() returns fouls_home, fouls_away, corner_kicks_home, corner_kicks_away, shots_off_target_home, shots_off_target_away when BSD returns them
    - extract_context() returns venue_city, home_coach, away_coach when BSD returns them
  </acceptance_criteria>
  <done>test_enrichment.py passes with all 3 new tests green. No changes to extract_stats() or extract_context() loops — field map expansion is auto-detected.</done>
</task>

<task type="auto">
  <name>Task 2: Add coverage auditor to output.py + tests</name>
  <files>src/output.py, tests/test_output.py</files>
  <read_first>
    Read src/output.py lines 1-52 for imports and ANSI helpers (reuse _dim, _bold_cyan, _green, _red).
    Read src/enrichment.py lines 17-31 for field map structure (auditor references these patterns).
    Read the RESEARCH.md lines 570-675 for the exact coverage_audit() implementation pattern.
  </read_first>
  <action>
    Per D-15, D-16, D-17:

    In src/output.py, add at the end of the file:
    1. Three classification lists (module-level constants):
       - _PREDICTION_FIELDS: list of 11 field names (per RESEARCH.md lines 573-578 — odds_home, odds_draw, odds_away, expected_goals, actual_home_xg, actual_away_xg, odds_over_25, odds_under_25, odds_btts_yes, expected_home_goals, expected_away_goals)
       - _DISPLAY_FIELDS: list of 27 field names (per RESEARCH.md lines 580-589 — includes home_score, away_score, event_date, venue.name, referee.name, ai_preview.text, yellow_cards, red_cards, shots_on_target, ball_possession, venue.city, home_coach.name, away_coach.name, round_name, fouls, corner_kicks, shots_off_target, shots_inside_box, temperature_c, wind_speed, weather_code, pitch_condition, attendance, funfacts, home_score_ht, away_score_ht)
       - _OPERATIONAL_FIELDS: list of 9 field names (per RESEARCH.md lines 591-594 — id, status, home_team, away_team, league.id, group_name, winner, period, current_minute)

    2. Function coverage_audit() -> dict:
       - Define extracted_display set (currently extracted display fields including the new ones from this phase: venue.name, referee.name, ai_preview.text, yellow_cards, red_cards, shots_on_target, ball_possession, venue.city, home_coach.name, away_coach.name, fouls, corner_kicks, shots_off_target, home_score, away_score, event_date, round_name)
       - Define extracted_prediction set (currently extracted prediction fields: odds_home, odds_draw, odds_away, expected_home_goals, expected_away_goals)
       - Define extracted_operational set (currently extracted operational fields: id, status, home_team, away_team, league.id, group_name, winner)
       - Compute total_extracted as union of all three extracted sets
       - Compute meaningful_all as set(_PREDICTION_FIELDS + _DISPLAY_FIELDS + _OPERATIONAL_FIELDS) — total 47
       - n_meaningful = len(meaningful_all), n_meaningful_covered = len(total_extracted & meaningful_all), pct_meaningful = round(n_meaningful_covered / n_meaningful * 100, 1)
       - Per-category breakdown: iterate categories dict {"Prediction": (list, set), "Display": (list, set), "Operational": (list, set)} compute covered/total/pct for each
       - Return dict with keys: meaningful (covered, total, pct, target=60.0, target_met, missing as sorted list), raw (covered, total, pct), by_category (dict)

    3. Function print_coverage_audit() -> None:
       - Call audit = coverage_audit()
       - Print header: "\nCoverage Audit" in bold_white
       - Print meaningful line: f"  Meaningful:  {m['covered']}/{m['total']} ({m['pct']}%)  {'← target: ≥60%' if not m['target_met'] else '✓ target ≥60% met'}"
       - Print raw line: f"  Raw:         {r['covered']}/{r['total']} ({r['pct']}%)"
       - Print "  By value:" then each category: f"    {name}:  {cat['covered']}/{cat['total']} ({cat['pct']}%)"
       - Use _dim for secondary info, _green for met, _red for not met

    In tests/test_output.py:
    4. Add at end: class TestCoverageAudit with:
       - test_meaningful_denominator_is_47: coverage_audit()["meaningful"]["total"] == 47
       - test_meaningful_target_60: check target_met is False if n_meaningful_covered < 28.2 (60% of 47 = 28.2), True if >= 29
       - test_by_category_keys: "Prediction", "Display", "Operational" in coverage_audit()["by_category"]
       - test_print_output_format: _capture(print_coverage_audit), assert "Coverage Audit" in output, assert "Meaningful" in output, assert "Raw" in output
  </action>
  <verify>
    <automated>pytest tests/test_output.py::TestCoverageAudit -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `coverage_audit()` returns dict with meaningful.total == 47, meaningful.target == 60.0, and per-category breakdown
    - `print_coverage_audit()` prints the D-17 format (Meaningful/Raw/By value) without errors
    - Dual metric: meaningful carries target_met, raw is informational-only
    - All TestCoverageAudit tests pass
    - Full suite: `pytest -x -q` passes without regressions
  </acceptance_criteria>
  <done>coverage_audit() returns correct 47-field meaningful denominator. print_coverage_audit() displays D-17 format. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | All changes are pure Python computation + console output. No external calls, no user input, no new data persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | All | coverage_audit | accept | No security-relevant code path. Pure computation over hardcoded field name lists. |
| T-20-02 | All | enrichment.py field maps | accept | Field maps are hardcoded string constants. No user input interpolated. |
</threat_model>

<verification>
1. Run `pytest tests/test_enrichment.py -x -q` — all existing + 3 new tests pass
2. Run `pytest tests/test_output.py::TestCoverageAudit -x -q` — all 4 tests pass
3. Run `pytest -x -q` — full suite green with zero regressions
4. Verify coverage_audit()["meaningful"]["total"] == 47
</verification>

<success_criteria>
- [ ] src/enrichment.py has 14 _STATS_FIELD_MAP entries and 5 _CONTEXT_SOURCE_MAP entries
- [ ] coverage_audit() returns correct 47-field meaningful denominator
- [ ] print_coverage_audit() renders D-17 format (Meaningful/Raw/By value)
- [ ] 6 new test methods pass (3 enrichment + 3 coverage audit)
- [ ] Full test suite: 0 failures, 0 regressions
</success_criteria>

<output>
Create `.planning/phases/20-output-enhancement-coverage-seal/20-01-SUMMARY.md` when done
</output>
