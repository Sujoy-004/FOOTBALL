---
phase: 20-output-enhancement-coverage-seal
plan: 03
type: execute
wave: 3
depends_on: [20-02]
files_modified:
  - src/output.py
  - src/main.py
  - tests/test_output.py
autonomous: true
requirements: [V2-27, V2-28]

must_haves:
  truths:
    - "Per-match signal table displays all remaining/upcoming matches with 7 signal columns + blended Δ"
    - "Table is gated behind --match-detail CLI flag (no default console noise)"
    - "Focus card primary path (upcoming matches): shows 7 signals + per-signal Δ + Wilson CI + provenance — guaranteed content per D-04"
    - "Focus card stats/context: CONDITIONAL content per D-19/D-20 — populated only when match_entry exists (played matches). For upcoming matches: stats section hidden, context replaced with single dimmed placeholder line"
    - "Focus card secondary path (--match-detail PLAYED_MATCH_ID): best-effort signal reconstruction from prediction_ledger. Per-signal Δ shows '—' for played matches — not a defect"
    - "Wilson CI computed with math.sqrt only, format: [.452 — .516]"
    - "CI shown only in focus card — NOT in championship table, NOT in per-match table"
    - "Per-signal Δ values show actual deltas (not '—') after first iteration for upcoming matches — D-04"
    - "Table separator width matches header width dynamically — no hardcoded value"
    - "0 test regressions on existing suite"
  artifacts:
    - path: src/output.py
      provides: "wilson_score_ci(), format_ci(), print_match_detail_table(), print_focus_card()"
      contains: "def wilson_score_ci"
    - path: src/main.py
      provides: "_match_detail_enabled flag, _prev_signal_data per-signal snapshot, --match-detail CLI arg, _gather_match_signal_data()"
      contains: "_match_detail_enabled"
    - path: tests/test_output.py
      provides: "TestWilsonCI, TestMatchDetailTable, TestFocusCard test classes"
      contains: "class TestWilsonCI"
  key_links:
    - from: main.py::_run_iteration() -> output.py::print_match_detail_table()
      via: "conditional call: if _match_detail_enabled: output.print_match_detail_table(...)"
      pattern: "print_match_detail_table"
    - from: output.py::print_focus_card() -> match_data["prev_signals"]
      via: "reads prev_signals dict for per-signal Δ, reads blended_delta for blended Δ"
      pattern: "prev_signals"
    - from: main.py::_prev_signal_data -> main.py::_run_iteration()
      via: "captured at start of _run_iteration(), consumed for match_data Δ fields, updated at end"
      pattern: "_prev_signal_data"

---

<objective>
Build the per-match signal breakdown table, focus card, and Wilson Score confidence intervals — the two primary display features (V2-27, V2-28).

Purpose: V2-27 surfaces signal-level prediction detail that was previously hidden inside simulation internals. The per-match table shows all 7 signals (Blended, Elo, Odds, CatBoost, Form, Lineup, xG) side by side with blended Δ triage. The focus card is the single inspection point where every deferred display from Phases 17/18 lands: per-signal Δ, Wilson CI, match context, and match stats. V2-28 adds statistical rigor via Wilson score 95% CI — pure math.sqrt, no scipy dependency.

Output:
- src/output.py: wilson_score_ci(), format_ci(), print_match_detail_table(), print_focus_card()
- src/main.py: --match-detail CLI arg, _match_detail_enabled flag, _prev_signal_data module global (stores full signal dicts per match for per-signal Δ), inline signal data gathering
- tests/test_output.py: TestWilsonCI (5 tests), TestMatchDetailTable (3 tests), TestFocusCard (4 tests)
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-output-enhancement-coverage-seal/20-CONTEXT.md
  — D-01: ~85-col per-match table, 7 signal columns + blended Δ
  — D-02: --match-detail CLI flag (same pattern as --ai-preview)
  — D-03: Δ column is blended only (triage gate)
  — D-04: Focus card: 7 signals + provenance + per-signal Δ + CI + context + stats
  — D-05: Focus card accessible only from per-match table (not championship table)
  — D-06: Flow: default → --match-detail → per-match table → focus card
  — D-07: Wilson score 95% CI in focus card only, math.sqrt only
  — D-08: Format: [.452 — .516], computed via math.sqrt
  — D-19/D-20: Focus card layout — signals top, Δ/CI middle, context, stats bottom

@.planning/phases/20-output-enhancement-coverage-seal/20-RESEARCH.md
  — Lines 193-214: wilson_score_ci() + format_ci() implementation
  — Lines 270-369: _gather_match_signal_data() helper pattern
  — Lines 373-446: print_match_detail_table() pattern with _fmt_prob, _fmt_xg, _format_delta_cell
  — Lines 450-534: print_focus_card() pattern with provenance labels, stat fields, context

@.planning/phases/20-output-enhancement-coverage-seal/20-PATTERNS.md
  — Lines 233-291: --match-detail flag gate pattern (copy of --ai-preview)
  — Lines 293-306: _prev_signal_data module-level global pattern (copy of _prev_history)
  — Lines 616-636: Flag-gated display summary (all 4 steps)

@src/output.py
  — Lines 30-46: ANSI helpers reused for match table, focus card
  — Lines 54-95: print_probability_table() — analog for table rendering pattern
  — Lines 97-140: print_delta_summary() — _format_delta_cell analog color pattern

@src/main.py
  — Lines 43-44: _ai_preview_enabled flag — DIRECT ANALOG for _match_detail_enabled
  — Lines 249-254: --ai-preview arg definition — DIRECT ANALOG for --match-detail
  — Lines 46-50: _prev_history/_prev_cal_params — DIRECT ANALOG for _prev_signal_data
  — Lines 933-934: Condition display if _ai_preview_enabled — DIRECT ANALOG for match-detail
  — Lines 580-938: _run_iteration() — Add signal data snapshot capture at START, display at END

@tests/test_output.py
  — Lines 62-71: _capture() helper — reusable for all new tests
  — Lines 74-93: TestProbabilityTable — analog for table test patterns
  — Lines 368-466: TestGovernanceDashlet — analog for complex output function tests
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Wilson Score CI functions + tests (V2-28)</name>
  <files>src/output.py, tests/test_output.py</files>
  <read_first>
    Read RESEARCH.md lines 193-214 — wilson_score_ci() and format_ci() implementation.
    Read CONTEXT.md D-07, D-08 — Wilson score only in focus card, math.sqrt only, format [.452 — .516].
    Read src/output.py lines 1-12 — existing imports. Note math is NOT imported yet.
  </read_first>
  <action>
    Per D-07, D-08:

    In src/output.py:
    1. Add `import math` to the existing imports at the top (line 6 area, keep alphabetical order).
    2. Add function wilson_score_ci(k: int, n: int, z: float = 1.96) -> tuple[float, float]:
       - Docstring: "Compute Wilson score 95% CI for k successes in n trials. Closed-form using only math.sqrt (D-07). At n=50000, converges with Clopper-Pearson within 0.001."
       - if n == 0: return (0.0, 0.0)
       - p = k / n; z2 = z * z
       - denominator = 1.0 + z2 / n
       - center = (p + z2 / (2.0 * n)) / denominator
       - margin = z * math.sqrt((p * (1.0 - p) + z2 / (4.0 * n)) / n) / denominator
       - return (round(center - margin, 3), round(center + margin, 3))
    3. Add function format_ci(k: int, n: int) -> str:
       - low, high = wilson_score_ci(k, n)
       - return f"[{low:.3f} — {high:.3f}]" (em-dash U+2014 per D-08)
    4. Add helper function wilson_ci_from_prob(p: float | None, n: int = 50000) -> str | None:
       - if p is None: return None
       - k = round(p * n) — convert probability to pseudo-count for Wilson CI formula
       - return format_ci(k, n)
       - This is used by the focus card where we have a probability (not raw k/n).

    In tests/test_output.py:
    5. Add import: `from src.output import wilson_score_ci, format_ci, wilson_ci_from_prob`
    6. Add class TestWilsonCI:
       - test_ci_known_values: wilson_score_ci(25000, 50000) returns bounds symmetric around 0.5 (center ≈ 0.5, margin ≈ 0.004) — assert 0.496 <= low <= 0.504 and 0.496 <= high <= 0.504
       - test_ci_extreme: wilson_score_ci(0, 50000) returns low ≈ 0.0 (actually ~1e-6 range, but non-negative); high ≈ 0.0
       - test_ci_zero_n: wilson_score_ci(0, 0) returns (0.0, 0.0)
       - test_format_ci: format_ci(25000, 50000) matches pattern r"\[\d\.\d{3} — \d\.\d{3}\]"
       - test_wilson_ci_from_prob: wilson_ci_from_prob(0.5, 50000) returns a valid format string
  </action>
  <verify>
    <automated>pytest tests/test_output.py::TestWilsonCI -x -q</automated>
  </verify>
  <done>
    - `wilson_score_ci(25000, 50000)` returns (0.496, 0.504) within tolerance
    - `wilson_score_ci(0, 0)` returns (0.0, 0.0)
    - `format_ci(25000, 50000)` matches pattern `[0.496 — 0.504]`
    - `wilson_ci_from_prob(0.5, 50000)` returns a string matching CI format
    - Only `math.sqrt` is used in implementation (grep for "scipy" returns 0)
    - 5 Wilson CI tests all pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add per-match signal table + focus card display + tests (V2-27)</name>
  <files>src/output.py, tests/test_output.py</files>
  <read_first>
    Read RESEARCH.md lines 373-446 — print_match_detail_table() pattern with header, row, delta, _fmt_prob, _fmt_xg, _format_delta_cell helpers.
    Read RESEARCH.md lines 450-534 — print_focus_card() pattern with provenance, signal rows, CI, context section, stats section.
    Read CONTEXT.md D-01, D-03, D-04, D-19, D-20 — table structure, focus card layout.
    Read src/output.py lines 30-46 — ANSI helpers available: _dim, _bold_cyan, _green, _red, _bold_white.
    Read src/output.py lines 97-140 — print_delta_summary() for _format_delta_cell color pattern (▲ green, ▼ red, = neutral).
  </read_first>
  <action>
    Per D-01, D-03, D-04, D-05, D-19, D-20:

    In src/output.py, add the following functions:

    A. Helper functions (before any new display functions):
       - _fmt_prob(val: float | None) -> str: if val is None return "   N/A"; return f"{val:>6.3f}"
       - _fmt_xg(val: tuple | None) -> str: if val is None return "   N/A"; return f"{val[0]:.1f}/{val[1]:.1f}"
       - _format_delta_cell(delta: float) -> str: if delta > 0.0005: return f"  {_green(chr(9650))} {delta:+.3f}" (▲ unicode); elif delta < -0.0005: return f"  {_red(chr(9660))} {delta:+.3f}" (▼); else: return f"  {'=':>6}"

    B. print_match_detail_table(matches_data: list[dict], prev_matches_data: list[dict] | None = None) -> None:
       - Docstring: "Print per-match signal breakdown table (~85 cols, D-01). 7 signal columns + blended Δ triage column."
       - if not matches_data: print(_dim("No upcoming matches.")); return
       - Build prev_lookup dict: if prev_matches_data: for m in prev_matches_data: prev_lookup[m["match_id"]] = m["blended"]
       - Build header string:
         header = f"{'Match':<12} {'Team A':<18} {'Team B':<18} {'Elo':>6} {'Odds':>6} {'CB':>6} {'Form':>6} {'Line':>6} {'xG':>6} {'Δ':>6}"
       - Print header via _bold_cyan(header)
       - Print separator via _bold_cyan("-" * len(header)) — COMPUTED from header width, NOT hardcoded (fixes checker warning: hardcoded 85 would misalign with actual ~99-char header)
       - For each match sorted by match_id:
         - sig = m["signals"]; t_a = m["team_a"][:18]; t_b = m["team_b"][:18]; mid_short = m["match_id"][:12]
         - Row: f"{mid_short:<12} {t_a:<18} {t_b:<18} {sig['elo']:>6.3f} {_fmt_prob(sig['odds']):>6} {_fmt_prob(sig['catboost']):>6} {_fmt_prob(sig['form']):>6} {_fmt_prob(sig['lineup']):>6} {_fmt_xg(sig['xg']):>6} "
         - Delta: if match_id in prev_lookup: delta = m["blended"] - prev_lookup[match_id]; row += _format_delta_cell(delta); else: row += f"  {'—':>6}"
         - print(row)
       - Print trailing newline

    C. print_focus_card(match_data: dict, match_entry: dict | None = None) -> None:
       - Docstring: "Print the match focus card (~84 chars, Mockup 3 layout per D-20). Signals top → Δ/CI → context → stats."
       - sig = match_data["signals"]; stats = (match_entry or {}).get("stats") or {}; ctx = (match_entry or {}).get("context") or {}
       - Match header: print(); print(_bold_white(f"─── {match_data['team_a']} vs {match_data['team_b']} ({match_data['match_id']}) ───"))
       - Signal section header: _bold_cyan(f"{'Signal':<14} {'Prob':>8} {'Δ':>8} {'CI':>16}") with separator _bold_cyan("-" * 48)
       - provenance dict: "blended": "Brier-weighted blend", "elo": "eloratings.net expected score", "odds": "Market vig-removed odds", "catboost": "BSD CatBoost prediction", "form": "Last-5 form residual", "lineup": "Squad market value ratio", "xg": "BSD xG Poisson lambda"
       - Row data list (7 rows):
         - ("Blended", blended, match_data.get("blended_delta"), None) — blended_delta provided by main.py wiring per D-04
         - ("Elo", elo_p, prev_elo_p, wilson_ci_from_prob(elo_p)) — prev_elo_p from match_data.get("prev_signals", {}).get("elo")
         - ("Odds", odds_p, prev_odds_p, wilson_ci_from_prob(odds_p))
         - ("CatBoost", cb_p, prev_cb_p, wilson_ci_from_prob(cb_p))
         - ("Form", form_p, prev_form_p, wilson_ci_from_prob(form_p))
         - ("Lineup", lineup_p, prev_lineup_p, wilson_ci_from_prob(lineup_p))
         - ("xG", xg_tuple, None, None) — xG is display-only, no prob or CI
       - Iterate rows: label + prob_str + delta_str + ci_str
         - prob_str: f"{prob:.3f}" if isinstance(prob, (int, float)) else str(prob) if prob else "N/A"
         - delta_str: _format_delta_cell(prob - prev_prob) if both are numeric, else f"  {'—':>6}"
         - ci_str: ci if ci else " " * 16
        - Context section — CONDITIONAL on match_entry existence (Architecture Decision: upcoming-match focus card primary path):
          - If match_entry is None (upcoming match — no enrichment data yet): print a single dimmed line `_dim("Context/stat data available after match completion.")` and SKIP rendering per-field context blocks. Do NOT show "N/A" for each field individually.
          - If match_entry is not None (played match — enrichment data exists): print(); print(_bold_white("Match Context")); show venue, referee, venue_city, home_coach, away_coach from ctx; use "—" for truly missing fields (not "N/A").
        - Stats section — CONDITIONAL on match_entry existence AND stats having entries:
          - If not match_entry or not stats: SKIP the entire stats section (no header, no rows, no placeholder).
          - If match_entry and stats has entries: print(); print(_bold_white("Match Stats")); stat_fields list: [("Fouls","fouls_home","fouls_away"), ("Corners","corner_kicks_home","corner_kicks_away"), ("Shots off","shots_off_target_home","shots_off_target_away"), ("Shots on","shots_on_target_home","shots_on_target_away"), ("Possession","possession_home","possession_away"), ("YC","yellow_cards_home","yellow_cards_away"), ("RC","red_cards_home","red_cards_away")]; for each: h_val = stats.get(home_key, "—"), a_val = stats.get(away_key, "—"), print formatted
        - ARCHITECTURE NOTE: Conditional content design (per phase-review decision):
          - D-19/D-20 describe display slots owned by the focus card. Population is conditional on data availability.
          - Upcoming match (match_entry=None): signals + Δ + CI + provenance guaranteed. Context shows placeholder.
            Stats section hidden entirely. This is the PRIMARY V2-27 path.
          - Played match (match_entry exists): signals + Δ + CI + provenance from prediction_ledger reconstruction.
            Context and stats fully populated from enrichment data. Per-signal Δ shows "—" (single historical snapshot).
          - This matches project graceful-degradation philosophy (Phase 13 fallback chains, enrichment sparse-field convention).
        - Note: per-signal Δ values depend on match_data having "prev_signals" and "blended_delta" keys. These are populated by main.py Task 3b (not this task). The function gracefully handles missing keys by showing "—".

    In tests/test_output.py:
    D. Add class TestMatchDetailTable:
       - test_empty_shows_no_upcoming: _capture(print_match_detail_table, []) contains "No upcoming matches."
       - test_table_header_columns: build header with known format, _capture(print_match_detail_table, [_sample_match_data()]) contains "Match", "Team A", "Team B", "Elo", "Odds", "CB", "Form", "Line", "xG", "Δ"
       - test_delta_shows_for_prev_data: _capture(print_match_detail_table, [_sample_match_data()], [_sample_prev_data()]) contains "▲" or "▼" or "="
       - Add helper _sample_match_data() -> dict that returns a match dict with structure: {"match_id": "M73", "team_a": "Argentina", "team_b": "Brazil", "signals": {"elo": 0.55, "odds": 0.52, "catboost": 0.53, "form": 0.51, "lineup": 0.54, "xg": (2.1, 1.2)}, "blended": 0.53}
       - Add helper _sample_prev_data() -> dict: same structure but blended=0.50

    E. Add class TestFocusCard:
        - test_signal_section_header: _capture(print_focus_card, _sample_match_data_focus()) contains "Signal", "Prob", "Δ", "CI"
        - test_upcoming_match_context: match_entry=None (upcoming match) — output contains dimmed placeholder "Context/stat data available after match completion." and does NOT contain "Match Context" header, "Match Stats" header, or any per-field context lines
        - test_played_match_context: match_data with prev_signals and context in match_entry — output contains venue name, referee name, "Match Context" header
        - test_played_match_stats: match_entry with stats — output contains "Match Stats" header and stat field labels ("Fouls", "Corners", "Shots off", "Shots on", "Possession", "YC", "RC")
        - test_missing_stats_played: match_entry without stats — output does NOT throw, gracefully omits stats section (no "Match Stats" header)
        - Add helper _sample_match_data_focus() -> dict: extends _sample_match_data() with "prev_signals": {"elo": 0.53, "odds": 0.50, "catboost": 0.51, "form": 0.50, "lineup": 0.52} and "blended_delta": 0.03
  </action>
  <verify>
    <automated>pytest tests/test_output.py::TestMatchDetailTable tests/test_output.py::TestFocusCard -x -q</automated>
  </verify>
  <done>
    - print_match_detail_table renders table with separator matching header width (len(header), not hardcoded 85)
    - print_match_detail_table with empty list shows "No upcoming matches."
    - Δ column shows ▲/▼/— for delta greater/same
    - print_focus_card renders signals section (Signal, Prob, Δ, CI headers)
    - print_focus_card with match_entry=None renders dimmed placeholder "Context/stat data available after match completion." — no per-field N/A blocks
    - print_focus_card with match_entry=None does NOT render "Match Context" header, "Match Stats" header, or stat lines
    - print_focus_card with match_entry and context renders venue, referee — conditional content per D-19/D-20
    - print_focus_card with match_entry and stats renders Fouls, Corners, Shots, etc. — conditional content per D-19/D-20
    - print_focus_card handles missing match_entry gracefully (no crash)
    - All 7 test methods pass (3 MatchDetailTable + 4 FocusCard with 2 new conditional tests)
  </done>
</task>

<task type="auto">
  <name>Task 3a: Add --match-detail CLI flag + _prev_signal_data module state + signal helpers</name>
  <files>src/main.py</files>
  <read_first>
    Read src/main.py lines 43-50 — module-level flags _ai_preview_enabled, _prev_history, _prev_cal_params.
    Read src/main.py lines 249-254 — --ai-preview argparse definition.
    Read src/main.py lines 1088-1089 — _ai_preview_enabled wiring in main().
    Read src/main.py lines 933-934 — conditional display in _run_iteration() for ai_preview.
  </read_first>
  <action>
    Per D-02, D-04, D-05, D-06:

    In src/main.py:

    A. Add module-level state (after _ai_preview_enabled, around line 44):
       1. `_match_detail_enabled: str | None = None`
          Docstring: "Stores --match-detail arg value. None=disabled, 'table'=match table, MATCH_ID=focus card (Phase 20)."
       2. `_prev_signal_data: dict | None = None`
          Docstring: "Snapshot of per-match signal DATA (full dicts per match_id, NOT just blended probs) from previous iteration. Used for per-signal Δ computation in focus card per D-04. Captured at start of _run_iteration(), updated at end. Structure: {match_id: {'signals': {...}, 'blended': float}}"
          REASONING (D-04 compliance): _prev_signal_data stores full signal dicts per match so that per-signal Δ can be computed in the focus card. If it only stored blended probabilities, all per-signal Δ values would show "—" on every run (the checker BLOCKER that motivated this fix).

    B. Add --match-detail CLI arg (after --ai-preview argparse, around line 254):
       - `parser.add_argument("--match-detail", type=str, nargs="?", const="table", default=None, help="Display per-match signal breakdown. Use --match-detail to show table, --match-detail MATCH_ID to show focus card (Phase 20)")`
       - nargs="?": `--match-detail` alone → const="table" (shows per-match table). `--match-detail M73` → "M73" (shows focus card). Not specified → default None (hidden).

    C. In main() (after line 1089, where _ai_preview_enabled is set):
       - `global _match_detail_enabled`
       - `_match_detail_enabled = args.match_detail`  — stores the string value directly ("table" | match_id | None)

    D. Add two inline helper functions at module level (around line 50-60 area, after _prev_cal_params):
       - `def _get_signal_prob(match_id: str, entry: dict | None) -> float | None:` — return entry.get("probability") if entry and entry.get("available") else None
       - `def _get_signal_xg(match_id: str, entry: dict | None) -> tuple | None:` — if entry and entry.get("expected_home_goals") is not None: return (entry["expected_home_goals"], entry["expected_away_goals"]); return None
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('src/main.py').read()); print('Syntax OK')"</automated>
  </verify>
  <done>
    - _match_detail_enabled module variable defined (str | None, stores "table" | match_id | None)
    - _prev_signal_data module variable defined (dict | None, stores full signal dicts per match — not just blended)
    - --match-detail CLI arg with nargs="?" registered
    - inline helper functions _get_signal_prob() and _get_signal_xg() created
    - All wiring complete: argparse → main() → module state
  </done>
</task>

<task type="auto">
  <name>Task 3b: Wire signal data gathering + display dispatch in _run_iteration()</name>
  <files>src/main.py</files>
  <read_first>
    Read src/main.py lines 580-600 — _run_iteration signature + params.
    Read src/main.py lines 725-783 — signal cache loading (odds_cache, cb_cache, form_cache, lineup_cache).
    Read src/main.py lines 920-938 — display output block (prob table, ai preview, delta summary).
    Read src/knockout.py — resolve_knockout_slot_teams (exists — import pattern).
  </read_first>
  <action>
    Per D-04, D-05, D-06:

    NOTE: Task 3a created the module-level variables and helpers. This task wires the signal data gathering loops and display dispatch, including the per-signal Δ fix (BLOCKER fix): _prev_signal_data stores full signal dicts, match_data includes prev_signals and blended_delta for focus card consumption.

    In src/main.py _run_iteration():

    E. At the START of _run_iteration() (after global declarations, around line 604):
       1. Add `global _match_detail_enabled, _prev_signal_data`
       2. Initialize `current_signal_data: dict[str, dict] = {}` — will store full signal dicts per match_id

    F. AFTER signal caches are loaded (after lineup_cache around line 784) but BEFORE simulation, capture current signal data snapshot:
       - Build a dict `current_signal_data: dict[str, dict]` mapping match_id → full signal dict
       - match_probs = (blend_params or {}).get("match_probs", {})
       - for mid in match_probs: current_signal_data[mid] = {"blended": match_probs[mid]}
       - REASONING: We capture signals AFTER cache load but BEFORE simulation runs for the current iteration. This ensures current_signal_data reflects the state used in this iteration's simulation. _prev_signal_data (from last iteration) is used for Δ computation.

    G. In the display block AFTER print_delta_summary (around line 936), BEFORE the return:
       - If `_match_detail_enabled` is truthy:
         - Import signal data gathering deps inline:
           from src.knockout import resolve_knockout_slot_teams
           from src.elo import expected_score as elo_expected
         - Build elo_ratings dict: {name: data["elo"] for name, data in teams.items()}
         - Resolve slot teams for upcoming bracket matches
         - Gather cache lookups:
           odds_matches = odds_cache.get("matches", {}) if odds_cache else {}
           cb_matches = cb_cache.get("matches", {}) if cb_cache else {}
           form_matches = form_cache.get("matches", {}) if form_cache else {}
           lineup_matches = lineup_cache.get("matches", {}) if lineup_cache else {}
         - matches_data = [] (empty list)
         
         - BRACKET LOOP — iterate slot_teams items:
           for mid, slot in slot_teams.items():
             if mid in played: continue
             t_a, t_b = slot["team_a"], slot["team_b"]
             if t_a not in elo_ratings or t_b not in elo_ratings: continue
             signals = {
               "elo": elo_expected(elo_ratings[t_a], elo_ratings[t_b]),
               "odds": _get_signal_prob(mid, odds_matches.get(mid)),
               "catboost": _get_signal_prob(mid, cb_matches.get(mid)),
               "form": _get_signal_prob(mid, form_matches.get(mid)),
               "lineup": _get_signal_prob(mid, lineup_matches.get(mid)),
               "xg": _get_signal_xg(mid, cb_matches.get(mid)),
             }
             blended = match_probs.get(mid) or signals["elo"]
             
             # --- PER-SIGNAL Δ FIX (D-04 compliance) ---
             # _prev_signal_data stores full signal dicts per match from previous iteration.
             # Extract prev_signals for per-signal Δ computation in focus card.
             prev = _prev_signal_data.get(mid, {}) if _prev_signal_data else {}
             prev_signals = prev.get("signals", {})
             prev_blended = prev.get("blended")
             blended_delta = blended - prev_blended if prev_blended is not None else None
             
             matches_data.append({
               "match_id": mid, "team_a": t_a, "team_b": t_b,
               "signals": signals, "blended": blended,
               "prev_signals": prev_signals,
               "blended_delta": blended_delta,
             })
             # Also update current_signal_data with full signal dict for next iteration
             current_signal_data[mid] = {"signals": signals, "blended": blended}

         - GROUP LOOP — iterate upcoming group matches:
           if groups and "groups" in groups:
             for letter in "ABCDEFGHIJKL":
               group = groups["groups"].get(letter)
               if not group: continue
               for match in group.get("matches", []):
                 mid = match["match_id"]
                 if mid in played_groups: continue
                 t_a, t_b = match["team_a"], match["team_b"]
                 if t_a not in elo_ratings or t_b not in elo_ratings: continue
                 signals = {
                   "elo": elo_expected(elo_ratings[t_a], elo_ratings[t_b]),
                   "odds": _get_signal_prob(mid, odds_matches.get(mid)),
                   "catboost": _get_signal_prob(mid, cb_matches.get(mid)),
                   "form": _get_signal_prob(mid, form_matches.get(mid)),
                   "lineup": _get_signal_prob(mid, lineup_matches.get(mid)),
                   "xg": _get_signal_xg(mid, cb_matches.get(mid)),
                 }
                 blended = match_probs.get(mid) or signals["elo"]
                 
                 # Same per-signal Δ fix as bracket loop
                 prev = _prev_signal_data.get(mid, {}) if _prev_signal_data else {}
                 prev_signals = prev.get("signals", {})
                 prev_blended = prev.get("blended")
                 blended_delta = blended - prev_blended if prev_blended is not None else None
                 
                 matches_data.append({
                   "match_id": mid, "team_a": t_a, "team_b": t_b,
                   "signals": signals, "blended": blended,
                   "prev_signals": prev_signals,
                   "blended_delta": blended_delta,
                 })
                 current_signal_data[mid] = {"signals": signals, "blended": blended}

          - DISPLAY DISPATCH:
            if _match_detail_enabled == "table":
              # Build prev_matches_data from _prev_signal_data for blended Δ column
              prev_matches_data = None
              if _prev_signal_data:
                prev_matches_data = [
                  {"match_id": mid, "blended": d.get("blended", 0.0)}
                  for mid, d in _prev_signal_data.items()
                ]
              output.print_match_detail_table(matches_data, prev_matches_data)
            elif _match_detail_enabled and _match_detail_enabled != "table":
              # Focus card for a specific match (match_id = _match_detail_enabled)
              match_id = _match_detail_enabled
              match_data = next((m for m in matches_data if m["match_id"] == match_id), None)
              if match_data:
                # PRIMARY PATH: upcoming match — signals from caches, match_entry may be None
                match_entry = played.get(match_id) or played_groups.get(match_id)
                output.print_focus_card(match_data, match_entry)
              else:
                # SECONDARY PATH: played match — reconstruct from prediction_ledger + enrichment
                # Available signals: Odds (market_odds), CatBoost (catboost), Form (form),
                #   Lineup (lineup_strength), xG (catboost entry expected_home/away_goals)
                # Elo: recomputed from final teams.json ratings at match time
                # Blended: from prediction_history.json per-match entry
                # Per-signal Δ: NOT AVAILABLE for played matches (single snapshot) —
                #   displays "—" and is NOT considered a defect
                from src.state import load_prediction_ledger, load_prediction_history
                from src.elo import expected_score as elo_expected

                ledger = load_prediction_ledger(data_dir) or {}
                ph = load_prediction_history(data_dir) or []

                match_entry = played.get(match_id) or played_groups.get(match_id)
                if not match_entry:
                  print(f"Match {match_id} not found.")
                  return

                t_a = match_entry.get("team_a", "")
                t_b = match_entry.get("team_b", "")

                # Build signal dict from prediction_ledger
                led = ledger.get(match_id, {})
                signals = {
                  "elo": elo_expected(teams[t_a]["elo"], teams[t_b]["elo"]) if t_a in teams and t_b in teams else None,
                  "odds": (led.get("market_odds") or {}).get("probability"),
                  "catboost": (led.get("catboost") or {}).get("probability"),
                  "form": (led.get("form") or {}).get("probability"),
                  "lineup": (led.get("lineup_strength") or {}).get("probability"),
                  "xg": _get_signal_xg(match_id, (led.get("catboost") or {})),
                }

                # Get blended from prediction_history
                blended = None
                for entry in ph:
                  if entry.get("match_id") == match_id:
                    blended = entry.get("blended", entry.get("signals", {}).get("blended", {}).get("probability"))
                    break

                match_data = {
                  "match_id": match_id, "team_a": t_a, "team_b": t_b,
                  "signals": signals, "blended": blended or next(iter(signals.values())),
                  "prev_signals": {},  # No historical Δ for played matches
                  "blended_delta": None,
                }
                output.print_focus_card(match_data, match_entry)

    H. Before the return statement (line 938), ADD:
       - `_prev_signal_data = current_signal_data` — captures current signal data for next iteration's Δ computation
       - This MUST happen BEFORE the return so the snapshot persists to the next cycle
       - If current_signal_data is empty (no matches), store None to distinguish "no data yet" from "first run"

    NOTE FOR EXECUTOR: Both bracket and group loops share the same per-signal Δ computation pattern. The key difference from the original plan is:
    1. _prev_signal_data stores full dicts: {mid: {"signals": {...}, "blended": float}} — NOT just blended floats
    2. match_data entries include "prev_signals" (dict of previous signal values) and "blended_delta" (float | None)
    3. current_signal_data is populated alongside match_data construction (avoids second pass)
    4. Display dispatch for "table" mode builds prev_matches_data from _prev_signal_data (for blended Δ column in the table)
  </action>
  <verify>
    <automated>pytest -x -q</automated>
  </verify>
  <done>
    - _run_iteration() captures current_signal_data with full signal dicts (not just blended) after cache load
    - Signal data gathering loops for bracket + group matches populate match_data with prev_signals and blended_delta
    - _match_detail_enabled == "table" displays per-match signal table via output.print_match_detail_table()
    - _match_detail_enabled == "table" displays per-match signal table via output.print_match_detail_table()
    - _match_detail_enabled == match_id for upcoming match — PRIMARY PATH: match_data from matches_data, match_entry from played/played_groups (may be None)
    - _match_detail_enabled == match_id for played match — SECONDARY PATH: signal reconstruction from prediction_ledger + prediction_history + Elo recompute
    - Secondary path documents available (4 ledger signals + Elo + blended) vs unavailable (per-signal Δ — shows "—", not a defect)
    - _prev_signal_data stores full signal dicts per match_id — per-signal Δ works on second+ iteration (D-04 compliance)
    - All existing tests pass with zero regressions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | All changes are console output formatting + stdlib math computation. No new external API calls, no user input (CLI arg is --match-detail which is a boolean flag or match-id string), no new data persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | All | Wilson CI computation | accept | Pure math.sqrt on already-public probability data. No external input. |
| T-20-05 | All | match-detail display | accept | Match IDs are pre-defined simulation identifiers. Read from group/bracket data, not user input. --match-detail arg is a simple string comparison against known match_ids. |
| T-20-06 | All | Focus card stats display (primary path) | accept | Reads already-extracted enrichment data from played.json/played_groups.json. match_entry may be None for upcoming matches — conditional section hides gracefully. |
| T-20-07 | All | Focus card secondary path (played match) | accept | Reads prediction_ledger + prediction_history for signal reconstruction. Data is already-public predictions. Per-signal Δ shows "—" (documented limitation). |
</threat_model>

<verification>
1. Run `pytest tests/test_output.py::TestWilsonCI -x -q` — 5 tests pass
2. Run `pytest tests/test_output.py::TestMatchDetailTable -x -q` — 3 tests pass (separator computed from len(header))
3. Run `pytest tests/test_output.py::TestFocusCard -x -q` — 4 tests pass including conditional context/stats behavior
4. Run `python -c "import ast; ast.parse(open('src/main.py').read())"` — syntax OK
5. Run `pytest -x -q` — full suite green with zero regressions
6. Verify `python main.py --help` shows --match-detail flag
7. Verify `python main.py --once` (without --match-detail) shows no match detail output
8. Verify `python main.py --once --match-detail` shows per-match signal table with upcoming matches only
9. Verify `python main.py --once --match-detail M73` (upcoming match) shows focus card with signals + Δ + CI + dimmed context placeholder
10. Verify `python main.py --once --match-detail GS_A_01` (played match) shows focus card with signals from prediction_ledger + enrichment stats/context — Δ column shows "—" (expected for played matches)
</verification>

<success_criteria>
- [ ] wilson_score_ci(), format_ci(), wilson_ci_from_prob() added to output.py — 5 tests green
- [ ] print_match_detail_table() renders table with separator width computed from len(header) — 3 tests green
- [ ] print_focus_card() renders signals + Δ/CI + provenance for upcoming matches (primary path, match_entry=None)
- [ ] print_focus_card() with match_entry=None: stats section hidden, context replaced with dimmed placeholder "Context/stat data available after match completion." — no per-field N/A blocks
- [ ] print_focus_card() with match_entry: context and stats rendered as conditional content (D-19/D-20)
- [ ] Focus card secondary path (--match-detail PLAYED_MATCH_ID): reconstructs signals from prediction_ledger + prediction_history + Elo. Per-signal Δ shows "—" (not a defect)
- [ ] _prev_signal_data stores full signal dicts per match (dict[str, dict]) — NOT just blended floats
- [ ] match_data entries include prev_signals (for per-signal Δ) and blended_delta (for blended Δ)
- [ ] --match-detail CLI flag with table/focus card modes via nargs="?"
- [ ] 12 new tests + full suite: 0 regressions
- [ ] All task types use standard "auto" attribute (no hybrid checkpoint attributes)
</success_criteria>

<output>
Create `.planning/phases/20-output-enhancement-coverage-seal/20-03-SUMMARY.md` when done
</output>
