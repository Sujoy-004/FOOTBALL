---
phase: 20-output-enhancement-coverage-seal
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - src/constants.py
  - src/state.py
  - src/main.py
  - src/output.py
  - tests/test_state.py
  - tests/test_output.py
autonomous: true
requirements: [V2-29]

must_haves:
  truths:
    - "probability_log.json is created in data/ directory after every _run_iteration()"
    - "Snapshot contains full probability dict + ISO 8601 timestamp"
    - "Trend column (↑/↓/→) appears in championship table after first run"
    - "Trend compares current champion prob vs rolling 5-snapshot window mean"
    - "Trend threshold at 0.005 prevents noise flipping"
    - "Trend column hidden on first run (no prior snapshots to compare)"
    - "All existing tests continue to pass"
  artifacts:
    - path: src/constants.py
      provides: "PROBABILITY_LOG_FILE filename constant"
      contains: "PROBABILITY_LOG_FILE"
    - path: src/state.py
      provides: "load_probability_log() and append_probability_log() persistence functions"
      contains: "def append_probability_log"
    - path: src/main.py
      provides: "Probability log snapshot at end of _run_iteration(), after display"
      contains: "append_probability_log"
    - path: src/output.py
      provides: "_compute_trend_arrow() function and Trend column in print_probability_table()"
      contains: "def _compute_trend_arrow"
    - path: tests/test_state.py
      provides: "TestProbabilityLog class"
      contains: "class TestProbabilityLog"
    - path: tests/test_output.py
      provides: "TestTrendColumn class"
      contains: "class TestTrendColumn"
  key_links:
    - from: src/main.py::_run_iteration()
      to: src/state.py::append_probability_log()
      via: "called at end of _run_iteration() after print_probability_table()"
      pattern: "append_probability_log"
    - from: src/output.py::_compute_trend_arrow()
      to: "probability_log.json snapshots"
      via: "reads probability_log list, computes 5-window rolling mean"
      pattern: "prob_log\\[-6:-1\\]"

---

<objective>
Add persistent probability log with per-iteration snapshots and a trend arrow column in the championship table. Every simulation cycle writes a timestamped probability snapshot; the championship table shows ↑/↓/→ based on a 5-snapshot rolling window.

Purpose: V2-29 requires trend tracking across tournament duration. The log persists all probability states for retrospective analysis. The trend arrow gives the user instant visual feedback on momentum shifts without scanning delta columns.

Output:
- src/constants.py: PROBABILITY_LOG_FILE string constant
- src/state.py: load_probability_log() + append_probability_log()
- src/main.py: snapshot capture at end of _run_iteration()
- src/output.py: _compute_trend_arrow() + Trend column in print_probability_table()
- tests/test_state.py: TestProbabilityLog class
- tests/test_output.py: TestTrendColumn class
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-output-enhancement-coverage-seal/20-CONTEXT.md
  — D-09: Single rolling JSON file (probability_log.json), array of snapshot dicts appended after every _run_iteration()
  — D-10: Snapshot: full probability dict + timestamped
  — D-11: Cadence: every _run_iteration(), no separate timer
  — D-12: Trend arrow (↑/↓/→) in championship table, compares current prob vs rolling 5-window mean
  — D-13: Trend column hidden on first run (no window to compare)
  — D-14: Trend is championship-table only — NOT in per-match table, NOT in focus card

@.planning/phases/20-output-enhancement-coverage-seal/20-PATTERNS.md
  — Lines 326-404: load_probability_log() + append_probability_log() pattern (copy of append_prediction_history)
  — Lines 408-433: PROBABILITY_LOG_FILE constant pattern (copy of PREDICTION_LEDGER_FILE)
  — Lines 293-320: main.py probability snapshot wiring pattern
  — Lines 538-566: _compute_trend_arrow() + Trend column pattern (from RESEARCH.md)
  — Lines 76-86: Trend column header addition pattern in print_probability_table()

@.planning/phases/20-output-enhancement-coverage-seal/20-RESEARCH.md
  — Lines 538-566: _compute_trend_arrow() implementation with threshold=0.005

@src/constants.py
  — Lines 165-171: EXISTING file-name constants — PREDICTION_LEDGER_FILE, PREDICTION_HISTORY_SCHEMA_VERSION

@src/state.py
  — Lines 682-714: EXISTING load_prediction_history() + append_prediction_history() — DIRECT ANALOG, copy with PROBABILITY_LOG_FILE

@src/main.py
  — Lines 580-938: _run_iteration() — add snapshot capture AFTER line 938 (after print_delta_summary)
  — Lines 43-50: _prev_history/_prev_cal_params pattern — analog for module-level state variable

@src/output.py
  — Lines 54-95: print_probability_table() — add Trend column after Delta column
  — Research.md 538-566: _compute_trend_arrow() pattern

@tests/test_state.py
  — Lines 615-638: EXISTING test_save_prediction_history — analog for probability log tests
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PROBABILITY_LOG_FILE constant + state persistence functions + tests</name>
  <files>src/constants.py, src/state.py, tests/test_state.py</files>
  <read_first>
    Read src/constants.py lines 159-171 — existing file-name constant pattern. Insert PROBABILITY_LOG_FILE after line 171.
    Read src/state.py lines 682-714 — load_prediction_history() and append_prediction_history(). These are DIRECT analogs.
    Read src/state.py lines 99-132 — _atomic_write_json (reused, no changes needed).
    Read tests/test_state.py lines 615-638 — test_save_prediction_history pattern for analog.
  </read_first>
  <action>
    Per D-09, D-10, D-11:

    In src/constants.py:
    1. After line 171 (PREDICTION_HISTORY_SCHEMA_VERSION), add:
       - PROBABILITY_LOG_FILE: str = "probability_log.json" with docstring: "Filename for rolling probability log in data/ directory (Phase 20). Array of snapshot dicts appended after every _run_iteration(). Never pruned."

    In src/state.py:
    2. Add function load_probability_log(data_dir=None) -> list[dict] — DIRECT COPY of load_prediction_history pattern (lines 682-696), but use PROBABILITY_LOG_FILE from constants instead of hardcoded "prediction_history.json". Import PROBABILITY_LOG_FILE inside function body to avoid circular import. Returns empty list if file doesn't exist.
    3. Add function append_probability_log(snapshot: dict, data_dir=None) -> None — DIRECT COPY of append_prediction_history pattern (lines 699-714), but call load_probability_log() and use PROBABILITY_LOG_FILE for the path. Atomic write via the existing _atomic_write_json.

    In tests/test_state.py:
    4. Add class TestProbabilityLog with:
       - test_load_empty_when_no_file: load_probability_log(tmp_path) returns []
       - test_append_and_load: append_probability_log({"timestamp": "2026-06-21T12:00:00", "probabilities": {}}, data_dir=tmp_path) then load_probability_log(tmp_path) returns list with that entry
       - test_multiple_appends: append 3 snapshots, load verifies all 3 in order
  </action>
  <verify>
    <automated>pytest tests/test_state.py::TestProbabilityLog -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/constants.py has PROBABILITY_LOG_FILE = "probability_log.json"
    - src/state.py has load_probability_log() imported via constants.PROBABILITY_LOG_FILE
    - src/state.py has append_probability_log() using load + append + _atomic_write_json pattern
    - 3 test methods pass in TestProbabilityLog
    - No circular import errors
  </acceptance_criteria>
  <done>Probability log state persistence complete: constant defined, load/append functions work, 3 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire probability log snapshot into main.py _run_iteration()</name>
  <files>src/main.py</files>
  <read_first>
    Read src/main.py lines 580-600 — _run_iteration() signature, docstring, return value tuple.
    Read src/main.py lines 920-938 — simulation + display output block. This is where the snapshot must be added AFTER display.
    Read src/main.py lines 43-50 — _prev_history / _prev_cal_params module-level globals pattern (analog for future _prev_signal_data in Plan 03, not needed here).
  </read_first>
  <action>
    Per D-09, D-10, D-11:

    In src/main.py, at the very end of _run_iteration(), after line 938 (after print_delta_summary) but BEFORE the return statement at line 938:
    1. Add try/except block that captures a probability snapshot dict and appends to the log:
       - snapshot = {"timestamp": datetime.now(timezone.utc).isoformat(), "probabilities": probs}
       - state.append_probability_log(snapshot, data_dir=data_dir)
       - On exception, print "Warning: Failed to save probability log snapshot" to stderr
       - This MUST NOT block iteration — the try/except ensures zero disruption
    2. Use the existing `probs` variable (already computed by run_full_simulation and passed as return value at line 938) — no need to recompute

    NOTE: The return statement at line 938 is `return time.time(), last_request_time, probs`. The snapshot capture goes between line 936 (print_delta_summary) and the return. The existing `from datetime import datetime, timezone` is already at the top of main.py (check — if not, add it inline).

    No new module-level variables needed for this task. The snapshot capture is self-contained.
  </action>
  <verify>
    <automated>pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - _run_iteration() captures {timestamp, probabilities} snapshot after output display
    - Crash in snapshot capture does not propagate — caught by try/except, stderr warning
    - Full test suite passes with no regressions
  </acceptance_criteria>
  <done>main.py captures probability snapshot at end of every _run_iteration(). Zero regression on full test suite.</done>
</task>

<task type="auto">
  <name>Task 3: Add trend arrow computation + Trend column to championship table + tests</name>
  <files>src/output.py, tests/test_output.py</files>
  <read_first>
    Read src/output.py lines 54-95 — print_probability_table() signature, header construction, delta column pattern.
    Read PATTERNS.md lines 76-86 — Trend column header addition pattern.
    Read PATTERNS.md lines 676-691 — _compute_trend_arrow() pattern with threshold=0.005.
    Read tests/test_output.py lines 74-93 — TestProbabilityTable class (existing pattern for table tests).
  </read_first>
  <action>
    Per D-12, D-13, D-14:

    In src/output.py:
    1. Add function _compute_trend_arrow(current_prob: float, team_name: str, prob_log: list[dict]) -> str:
       - threshold = 0.005 (D-12 recommendation, use agents discretion)
       - if len(prob_log) < 6: return " " (need at least 6 entries for a 5-window: D-13)
       - window = prob_log[-6:-1] (5 snapshots before current)
       - window_probs = [s.get("probabilities", {}).get(team_name, {}).get("champion", 0.0) for s in window]
       - window_mean = sum(window_probs) / len(window_probs)
       - if current_prob > window_mean + threshold: return "↑"
       - elif current_prob < window_mean - threshold: return "↓"
       - else: return "→"
       - Import math if not already imported (check existing imports at line 1-12)

    2. In print_probability_table(), ADD a prob_log: list[dict] | None = None parameter after prev_probs.
       - has_trend = prob_log is not None and len(prob_log) >= 6
       - In header: after the Delta column addition (line 67), add: if has_trend: header += f"  {'Trend':>6}"
       - Update sep_len to account for trend column (+8 chars when has_trend)
       - In row loop (line 73-87), after the delta column logic, add: if has_trend and name in probs: arrow = _compute_trend_arrow(probs[name]["champion"], name, prob_log); row += f"  {arrow:>6}"
       - DEFAULT the prob_log parameter to None — existing callers (tests, main.py) continue to work unchanged

    NOTE: The function signature change (adding prob_log parameter with default None) is backward compatible. All existing calls to print_probability_table(probs) and print_probability_table(probs, prev_probs) continue to work without modification.

    In tests/test_output.py:
    3. Add class TestTrendColumn after TestProbabilityTable:
       - def test_trend_arrow_up(self): arrow = _compute_trend_arrow(0.25, "Argentina", _make_prob_log(0.24, 0.24, 0.24, 0.24, 0.24)); assert arrow == "↑"  # current 0.25 > 0.24+0.005
       - def test_trend_arrow_down(self): arrow = _compute_trend_arrow(0.23, "Argentina", _make_prob_log(0.24, 0.24, 0.24, 0.24, 0.24)); assert arrow == "↓"
       - def test_trend_arrow_flat(self): arrow = _compute_trend_arrow(0.242, "Argentina", _make_prob_log(0.24, 0.24, 0.24, 0.24, 0.24)); assert arrow == "→"
       - def test_trend_hidden_first_run(self): output = _capture(print_probability_table, full_probs) — assert "Trend" not in output (because prob_log=None by default)
       - def test_trend_column_printed(self): output = _capture(print_probability_table, full_probs, None, [{"timestamp": "1", "probabilities": {"Argentina": {"champion": 0.24}, "Brazil": {"champion": 0.25}, "France": {"champion": 0.20}, "Germany": {"champion": 0.15}, "England": {"champion": 0.10}}}] * 7) — assert "Trend" in output
       - def test_trend_hidden_when_insufficient_data(self): prob_log = [{"timestamp": "1", "probabilities": {"Argentina": {"champion": 0.24}}}] * 3; output = _capture(print_probability_table, full_probs, None, prob_log); assert "Trend" not in output

    Add a helper at module level in test_output.py (before TestTrendColumn):
    - def _make_prob_log(*probs: float) -> list[dict]: return [{"timestamp": "t", "probabilities": {"Argentina": {"champion": p}, "Brazil": {"champion": p+0.01}, "France": {"champion": p-0.01}, "Germany": {"champion": p-0.02}, "England": {"champion": p-0.03}}} for p in probs]

    Import _compute_trend_arrow at the top of test_output.py.
  </action>
  <verify>
    <automated>pytest tests/test_output.py::TestTrendColumn -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `_compute_trend_arrow(0.25, "Argentina", log_6_entries)` returns "↑" when window mean=0.24
    - `_compute_trend_arrow(0.23, "Argentina", log_6_entries)` returns "↓" when window mean=0.24
    - `_compute_trend_arrow(0.242, "Argentina", log_6_entries)` returns "→" when window mean=0.24
    - `_compute_trend_arrow(0.25, "Argentina", log_3_entries)` returns " " (insufficient data)
    - Trend column header "Trend" appears in print_probability_table output when prob_log has >= 6 entries
    - Trend column hidden when prob_log is None or has < 6 entries
    - All existing callers of print_probability_table(probs, prev_probs) continue to work unchanged
    - All TestTrendColumn tests pass
  </acceptance_criteria>
  <done>Trend arrow computation + column render complete. 6 tests pass. Existing callers unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | All changes are JSON persistence, stdlib datetime, and console formatting. No network, no user input, no sensitive data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | All | state.py probability_log | accept | Append-only JSON, same pattern as prediction_history. No PII in probability data. |
| T-20-03 | All | output.py trend arrow | accept | Pure computation over already-public probability data. No security relevance. |
</threat_model>

<verification>
1. Run `pytest tests/test_state.py::TestProbabilityLog -x -q` — 3 tests pass
2. Run `pytest tests/test_output.py::TestTrendColumn -x -q` — 6 tests pass
3. Run `pytest -x -q` — full suite green with zero regressions
4. Verify print_probability_table(probs, prev_probs, prob_log=log) shows Trend column header
5. Verify print_probability_table(probs, prev_probs) — existing call — shows no Trend column
</verification>

<success_criteria>
- [ ] PROBABILITY_LOG_FILE constant in constants.py
- [ ] load_probability_log() + append_probability_log() in state.py with 3 passing tests
- [ ] main.py captures probability snapshot at end of _run_iteration() — never blocks on failure
- [ ] _compute_trend_arrow() function with threshold=0.005, 5-window, correct ↑/↓/→ logic
- [ ] Trend column in championship table (header + arrows), hidden on first run
- [ ] 3 state tests + 6 output tests green
- [ ] Full suite: 0 regressions
</success_criteria>

<output>
Create `.planning/phases/20-output-enhancement-coverage-seal/20-02-SUMMARY.md` when done
</output>
