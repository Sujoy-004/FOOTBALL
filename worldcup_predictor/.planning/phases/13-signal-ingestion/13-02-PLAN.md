---
phase: 13-signal-ingestion
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - worldcup_predictor/src/predictors/catboost.py
  - worldcup_predictor/tests/test_catboost.py
autonomous: true
requirements:
  - V2-06
user_setup: []
must_haves:
  truths:
    - "CatBoost predictions are fetched from BSD /api/predictions/ endpoint for upcoming matches"
    - "CatBoost response is parsed into canonical home-win probability with confidence score"
    - "CatBoost cache file persists with correct schema and TTL expiry (24h)"
    - "Missing CatBoost predictions per match are flagged as available: false with reason"
    - "BSD event IDs are matched to internal match_ids via team-pair resolution"
  artifacts:
    - path: "worldcup_predictor/src/predictors/catboost.py"
      provides: "CatBoost prediction fetching, parsing, and caching"
      min_lines: 80
      contains: "def fetch_and_cache_catboost"
    - path: "worldcup_predictor/tests/test_catboost.py"
      provides: "Tests for prediction parsing, missing predictions, cache TTL"
      min_lines: 80
  key_links:
    - from: "worldcup_predictor/src/predictors/catboost.py"
      to: "worldcup_predictor/src/state.py"
      via: "imports load_signal_cache, save_signal_cache, is_cache_valid"
      pattern: "from src.state import"
    - from: "worldcup_predictor/src/predictors/catboost.py"
      to: "worldcup_predictor/src/fetcher.py"
      via: "imports team-resolution helpers (alias lookup, normalize, find match)"
      pattern: "from src.fetcher import"
---

<objective>
Implement CatBoost ML prediction ingestion from BSD's `/api/predictions/` endpoint. Downloads home/draw/away probabilities with confidence scores, maps BSD event IDs to internal match_ids via team-pair resolution, and caches results with 24h TTL and graceful degradation.

**Purpose:** The BSD CatBoost model (58k+ matches, 163 features, catboost-v5.0) provides a mature ML signal that diversifies the predictor beyond pure Elo. Consumed via REST API — no local CatBoost library needed.

**Outputs:**
- `src/predictors/catboost.py` — `fetch_and_cache_catboost()`, `parse_catboost_response()`
- `tests/test_catboost.py` — prediction parsing, missing predictions, cache TTL tests
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/.planning/phases/13-signal-ingestion/13-CONTEXT.md
@C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/.planning/phases/13-signal-ingestion/13-RESEARCH.md
@C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/constants.py
@C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/state.py
@C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/fetcher.py
@C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/predictors/odds.py

<interfaces>
From src/constants.py (extended by Plan 01):
```python
CATBOOST_CACHE_TTL_HOURS: int = 24
CATBOOST_CACHE_FILE: str = "catboost_cache.json"
API_TIMEOUT: int = 10
DATA_DIR: Path
```

From src/state.py (extended by Plan 01):
```python
load_signal_cache(cache_filename, data_dir=None) -> dict
save_signal_cache(cache, cache_filename, data_dir=None) -> None
is_cache_valid(cache, ttl_hours=12) -> bool
save_prediction_history(history, data_dir=None) -> None
```

From src/fetcher.py (team resolution helpers):
```python
_build_alias_lookup(aliases, bracket) -> dict[str, str]
_normalize_team(api_name, alias_lookup) -> str | None
_find_group_match(home_norm, away_norm, group_letter, round_number, groups) -> str | None
_find_bracket_match(home_norm, away_norm, bracket) -> str | None
_extract_group_letter(group_name) -> str | None
```

BSD predictions endpoint (from RESEARCH.md):
```
GET https://sports.bzzoiro.com/api/predictions/?league=27
Authorization: Token {BSD_API_KEY}
```

Expected response shape (BSD predictions page + docs):
```json
{
  "count": 10,
  "results": [
    {
      "event_id": 12345,
      "home_team": "Argentina",
      "away_team": "Algeria",
      "event_date": "2026-06-17T05:00:00+00:00",
      "predictions": {
        "home_probability": 0.64,
        "draw_probability": 0.20,
        "away_probability": 0.17,
        "confidence": 0.88,
        "markets": ["1x2", "btts", "over_under"],
        "model_version": "catboost-v5.0"
      },
      "updated_at": "2026-06-16T12:00:00+00:00"
    }
  ]
}
```

From src/predictors/odds.py (follow same pattern):
```python
def parse_odds_response(bsd_events, alias_lookup, groups) -> dict[str, dict]
def fetch_and_cache_odds(api_key, bsd_events, alias_lookup, groups, cache_ttl_hours=12) -> dict
  # Returns {fetched_at, expires_at, matches: {match_id: {probability, timestamp, available, reason?}}}
```

The BSD predictions endpoint may return different field names (Assumption A1 from RESEARCH.md). The executor MUST handle multiple field name patterns with a priority-ordered fallback chain. The response format documented above is the expected shape based on BSD docs — actual fields may differ (e.g., `home_win` vs `home_probability`). Use a flexible parser that handles both documented and alternate field names.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement catboost.py — parse_catboost_response and fetch_and_cache_catboost</name>
  <files>
    worldcup_predictor/src/predictors/catboost.py
  </files>

  <read_first>
    @C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/fetcher.py — fetch_raw_matches retry/backoff pattern lines 22-50, API key header, team resolution helpers
    @C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/.planning/phases/13-signal-ingestion/13-RESEARCH.md — Code Examples section for BSD predictions API response format, don't-hand-roll table, common pitfalls
    @C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/predictors/odds.py — follow same pattern (parse_*_response, fetch_and_cache_*)
    @C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/state.py — load_signal_cache, save_signal_cache, is_cache_valid signatures (added by Plan 01 Task 1)
  </read_first>

  <acceptance_criteria>
    1. Behavior assertion (test): `pytest tests/test_catboost.py::TestParsePredictions -x -q` passes all parse tests
    2. Behavior assertion (test): `pytest tests/test_catboost.py::TestMissingPredictions -x -q` passes all missing predictions tests
    3. Behavior assertion (test): `pytest tests/test_catboost.py::TestCatboostCache -x -q` passes all cache tests
    4. Source assertion: parse_catboost_response handles 3+ field name patterns (home_probability, home_win, probability_home) with priority-ordered fallback chain
    5. Source assertion: fetch_and_cache_catboost uses requests with Authorization: Token header, 3-attempt exponential backoff (1s, 2s, 4s)
    6. CLI assertion: `python -c "from src.predictors.catboost import parse_catboost_response; r=parse_catboost_response([], {}, {}, []); assert isinstance(r, dict)"` — module loads cleanly
  </acceptance_criteria>

  <behavior>
    RED: Write failing tests in test_catboost.py BEFORE creating catboost.py.

    Tests for TestParsePredictions:
    - test_parse_valid_prediction: BSD prediction dict with home=0.64, draw=0.20, away=0.17 → home_prob=0.64, available=True
    - test_parse_all_fields: verify confidence, model_version, updated_at stored in cache entry
    - test_parse_null_predictions: predictions field is None → available=False, reason="predictions_not_available"
    - test_parse_missing_event_id: no event_id → skip gracefully
    - test_parse_negative_probability: negative probability → available=False with reason="invalid_probability"
    - test_parse_probabilities_not_sum_one: non-normalized probs → stored as-is (normalization happens in Phase 14)
    - test_parse_different_field_names: accept alternative field names via fallback chain (home_win, home_prob, probability_home, etc.)

    Tests for TestCatboostCache (per VALIDATION.md):
    - test_cache_schema_valid: cache dict has fetched_at, expires_at, matches
    - test_cache_produces_24h_ttl: expires_at is ~24h from fetched_at

    Tests for TestMissingPredictions (per VALIDATION.md):
    - test_all_missing: all predictions null → all entries available=False
    - test_partial_missing: some events have predictions, some don't → mixed available flags
  </behavior>

  <action>
    Step 1 — Create tests/test_catboost.py with all RED tests (see behavior above).
    Import from src.predictors.catboost (will fail until created).
    Use tmp_path fixture for file I/O tests.
    Use from src.state import load_signal_cache, save_signal_cache, is_cache_valid.

    Step 2 — Create src/predictors/catboost.py:

    `parse_catboost_response(bsd_predictions: list[dict], alias_lookup: dict[str, str], groups: dict, bracket: list[dict]) -> dict[str, dict]`
    - Takes raw BSD predictions list (from `/api/predictions/` endpoint)
    - For each prediction entry:
      - Extract BSD event_id and team names
      - Normalize team names via alias_lookup
      - Resolve match_id: check group matches first via _find_group_match, then bracket matches via _find_bracket_match
      - Extract predictions dict (handles None → available=False)
      - Try common field name patterns for home/draw/away probabilities with priority-ordered fallback chain:
        Primary: predictions.get("home_probability"), predictions.get("draw_probability"), predictions.get("away_probability")
        Fallback 1: predictions.get("home_win"), predictions.get("draw"), predictions.get("away_win")
        Fallback 2: predictions.get("probability_home"), predictions.get("probability_draw"), predictions.get("probability_away")
      - Validate probability is a number in [0, 1] range; if not → available=False with reason="invalid_probability"
      - Store as canonical home-win probability (per D-13): {"probability": p, "confidence": ..., "model_version": ..., "timestamp": ..., "available": true/false, "reason": "..."}
    - Returns dict mapping match_id → entry dict
    - Uses from src.fetcher import _build_alias_lookup, _find_group_match, _find_bracket_match, _normalize_team
    - Uses from datetime import datetime, timezone

    `fetch_and_cache_catboost(api_key: str, alias_lookup: dict[str, str], groups: dict, bracket: list[dict], cache_ttl_hours: int = 24) -> dict`
    - Fetches from BSD predictions API: `GET https://sports.bzzoiro.com/api/predictions/?league=27`
    - Uses requests library with same `Authorization: Token {api_key}` header pattern as fetcher.py
    - 3-attempt exponential backoff (1s, 2s, 4s) — same as fetch_raw_matches
    - Returns {"matches": {}} on any fetch failure (timeout, connection, HTTP error, JSON decode) — graceful degradation
    - If fetch succeeds, calls parse_catboost_response(data["results"], ...)
    - Builds cache dict with fetched_at (now isoformat), expires_at ((now + ttl).isoformat), matches
    - Uses timezone-aware UTC datetimes (datetime.now(timezone.utc))
    - Returns the complete cache dict

    Step 3 — Verify RED: pytest tests/test_catboost.py -x -q → ImportError

    Step 4 — Implement all functions. Verify GREEN: pytest tests/test_catboost.py -x -q → all pass.
  </action>

  <verify>
    <automated>pytest tests/test_catboost.py -x -q</automated>
  </verify>

  <done>
    - parse_catboost_response() handles valid, null, missing, and malformed predictions
    - Multiple field name patterns supported with priority-ordered fallback chain (home_probability → home_win → probability_home)
    - fetch_and_cache_catboost() fetches from BSD, parses, and returns cache dict
    - Cache schema: {fetched_at, expires_at, matches: {match_id: {probability, timestamp, available, reason?}}}
    - All catboost tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive test_catboost.py with all test classes</name>
  <files>
    worldcup_predictor/tests/test_catboost.py
  </files>

  <read_first>
    @C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/predictors/catboost.py — verify function signatures before writing tests
    @C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/.planning/phases/13-signal-ingestion/13-VALIDATION.md — test map reference (TestCatboostCache, TestCatboostFetch)
    @C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/fetcher.py — lines 22-50 for retry/backoff mock pattern
  </read_first>

  <acceptance_criteria>
    1. Behavior assertion (test): `pytest tests/test_catboost.py -x -q` — all tests pass (≥12 tests across 5 classes)
    2. Behavior assertion (test): `pytest tests/test_odds.py -x -q` — odds tests from Plan 01 still pass
    3. Behavior assertion (test): `pytest -x` — full suite green with zero regressions
    4. Source assertion: TestCatboostFetch uses monkeypatch to mock requests.get, never contacts real BSD API
    5. Source assertion: TestParsePredictionsEdgeCases tests unmatchable teams, bracket resolution, group resolution
  </acceptance_criteria>

  <action>
    Add these additional test classes to tests/test_catboost.py:

    `class TestCatboostFetch`
    - test_fetch_returns_cache_dict: fetch_and_cache_catboost returns dict with fetched_at, expires_at, matches
    - test_fetch_handles_api_error: when requests.get raises exception, returns empty matches dict gracefully
    - Uses monkeypatch to mock requests.get (follow same mock pattern as test_fetcher.py tests)
    - Ensure the mock does not actually contact the BSD API

    `class TestParsePredictionsEdgeCases`
    - test_no_results: empty predictions list → empty matches dict
    - test_unmatchable_team: team not in alias lookup → entry skipped (not in matches)
    - test_bracket_match_resolution: prediction for knockout match uses _find_bracket_match path
    - test_group_match_resolution: prediction for group match uses _find_group_match path

    Verify: pytest tests/test_catboost.py -x -q → all pass (≥12 tests total).
    Verify: pytest -x → full suite green (no regressions).
  </action>

  <verify>
    <automated>pytest tests/test_catboost.py -x -q && pytest tests/test_odds.py -x -q</automated>
  </verify>

  <done>
    - test_catboost.py has TestParsePredictions, TestMissingPredictions, TestCatboostCache, TestCatboostFetch, TestParsePredictionsEdgeCases
    - All catboost tests pass (≥12 tests)
    - Full test suite passes with pytest -x
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BSD predictions API → catboost.py | Untrusted JSON — field names may differ from docs, values may be out of range |
| state.py → catboost_cache.json | Write path — partial writes could corrupt cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | parse_catboost_response | mitigate | Handle multiple field name patterns via priority-ordered fallback chain (RESEARCH.md Assumption A1). Validate probability ∈ [0,1] and set type checks. Set available=False for anything outside range or None. |
| T-13-05 | Tampering | fetch_and_cache_catboost | mitigate | 3-attempt retry with backoff (1s, 2s, 4s). On all failures, return empty matches dict (never crash). Same pattern as fetch_raw_matches. |
| T-13-06 | Information Disclosure | fetch_and_cache_catboost | mitigate | Never log BSD_API_KEY. Use same Authorization: Token header pattern as fetcher.py. Exception messages exclude sensitive data. |
| T-13-SC | Tampering | pip installs | mitigate | No new packages. catboost Python library NOT installed (we consume REST API). |
</threat_model>

<verification>
1. `pytest tests/test_catboost.py -x -q` — ≥12 catboost tests pass
2. `pytest tests/test_odds.py -x -q` — odds tests from Plan 01 still pass
3. `python -c "from src.predictors.catboost import parse_catboost_response; r=parse_catboost_response([], {}, {}, []); assert isinstance(r, dict)"` — module loads
4. `pytest -x` — full test suite green
</verification>

<success_criteria>
1. `src/predictors/catboost.py` has `parse_catboost_response()` and `fetch_and_cache_catboost()`
2. BSD predictions parsed with multiple field name fallbacks (home_probability → home_win → probability_home)
3. Missing/null/invalid predictions handled gracefully (available=False + reason)
4. Match_id resolution works for both group and knockout matches
5. Cache schema: `{fetched_at, expires_at, matches}` with 24h default TTL
6. Test file `tests/test_catboost.py` with 12+ tests covering all functions
7. Full test suite passes with zero regressions
</success_criteria>

<output>
Create `.planning/phases/13-signal-ingestion/13-02-SUMMARY.md` when done
</output>
