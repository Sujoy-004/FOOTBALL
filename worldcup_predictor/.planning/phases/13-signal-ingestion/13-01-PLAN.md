---
phase: 13-signal-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - worldcup_predictor/src/constants.py
  - worldcup_predictor/src/state.py
  - worldcup_predictor/src/predictors/__init__.py
  - worldcup_predictor/src/predictors/odds.py
  - worldcup_predictor/tests/test_odds.py
autonomous: true
requirements:
  - V2-05
  - V2-06
user_setup: []
must_haves:
  truths:
    - "Market odds are extracted from BSD events endpoint and converted to vig-removed home-win probabilities"
    - "Vig removal produces probabilities summing to 1.0 ± 0.01"
    - "Odds cache file persists with correct schema and TTL expiry"
    - "Missing/null odds for a match are flagged as available: false with reason"
    - "Signal constants, cache functions, save_prediction_history, and predictors package exist for both signals"
  artifacts:
    - path: "worldcup_predictor/src/constants.py"
      provides: "Signal constants (URL, TTLs, cache filenames for both signals)"
      min_lines: 13
      contains: "ODDS_CACHE_TTL_HOURS"
    - path: "worldcup_predictor/src/state.py"
      provides: "Cache persistence helpers (load_signal_cache, save_signal_cache, is_cache_valid) + save_prediction_history()"
      min_lines: 80
      contains: "def save_prediction_history"
    - path: "worldcup_predictor/src/predictors/__init__.py"
      provides: "Predictors package init"
      min_lines: 1
    - path: "worldcup_predictor/src/predictors/odds.py"
      provides: "Market odds fetching, vig removal, response parsing"
      contains: "def remove_vig"
    - path: "worldcup_predictor/tests/test_odds.py"
      provides: "Tests for vig removal, cache TTL, missing odds, persistence"
      min_lines: 80
  key_links:
    - from: "worldcup_predictor/src/predictors/odds.py"
      to: "worldcup_predictor/src/state.py"
      via: "imports load_signal_cache / save_signal_cache / is_cache_valid"
      pattern: "from src.state import"
    - from: "worldcup_predictor/src/predictors/odds.py"
      to: "worldcup_predictor/src/constants.py"
      via: "imports ODDS_CACHE_TTL_HOURS, ODDS_CACHE_FILE, API_TIMEOUT"
      pattern: "from src.constants import"
---

<objective>
Create the foundation for both signals (constants, cache helpers, save_prediction_history, predictors package) and implement the complete market odds ingestion pipeline — BSD events endpoint extraction, vig removal via basic 1/odds normalization, and cache persistence with TTL expiry.

**Purpose:** Market odds (V2-05) require zero extra API calls — BSD already embeds `odds_home`, `odds_draw`, `odds_away` in every events response. This plan provides the shared infrastructure both signals need and the odds-specific ingestion.

**Outputs:**
- `src/constants.py` extensions — signal URLs, TTLs (12h odds, 24h catboost), cache filenames
- `src/state.py` extensions — `load_signal_cache()`, `save_signal_cache()`, `is_cache_valid()`, `save_prediction_history()`
- `src/predictors/__init__.py` — package init
- `src/predictors/odds.py` — `remove_vig()`, `fetch_and_cache_odds()`, `parse_odds_response()`
- `tests/test_odds.py` — vig removal, cache TTL, missing odds, persistence tests
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/.planning/phases/13-signal-ingestion/13-CONTEXT.md
@C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/.planning/phases/13-signal-ingestion/13-RESEARCH.md
@C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/constants.py
@C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/state.py
@C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/fetcher.py

<interfaces>
From src/constants.py (existing — line 46 before Elo Sync constants):
```python
K_FACTOR: int = 60
DEFAULT_ELO: int = 1500
DATA_DIR: Path = Path(__file__).resolve().parent.parent / "data"
API_URL: str = "https://sports.bzzoiro.com/api/events/?league_id=27&limit=200"
API_TIMEOUT: int = 10
POLL_INTERVAL: int = 60
ELO_SYNC_INTERVAL_HOURS: int = 24
```

From src/state.py (existing — prediction history section starts at line 657):
```python
def load_prediction_history(data_dir=None) -> list[dict]   # line 660
def append_prediction_history(entry, data_dir=None) -> None # line 677
# NO save_prediction_history() exists — must be added
```

From src/state.py (existing — cache patterns):
```python
def _atomic_write_json(data: dict | list, path: Path) -> None  # line 98 — tmpfile + os.replace
def load_eloratings_cache(data_dir=None) -> dict  # line 257 — returns {} if missing
def save_eloratings_cache(cache, data_dir=None) -> None  # line 276
# Cache schema: {"fetched_at": "...", "values": {...}}
```

From src/fetcher.py (team resolution helpers):
```python
def _build_alias_lookup(aliases, bracket) -> dict[str, str]                # line 162
def _normalize_team(api_name, alias_lookup) -> str | None                  # line 179
def _find_bracket_match(home_norm, away_norm, bracket) -> str | None      # line 183
def _find_group_match(home_norm, away_norm, group_letter, round_number, groups) -> str | None  # line 204
```

Transformed BSD events response shape for odds extraction:
```json
{
  "id": 209476,
  "home_team": "Argentina",
  "away_team": "Algeria",
  "odds_home": 1.45,
  "odds_draw": 4.20,
  "odds_away": 7.50,
  "status": "upcoming",
  "league_id": 27,
  "event_date": "2026-06-17T05:00:00+00:00"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add signal constants, state.py cache helpers, save_prediction_history, and predictors package</name>
  <files>
    worldcup_predictor/src/constants.py
    worldcup_predictor/src/state.py
    worldcup_predictor/src/predictors/__init__.py
    worldcup_predictor/tests/test_state.py
  </files>

  <read_first>
    @C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/constants.py — read all, append after line 74 (ELORATINGS_TEAM_CODES)
    @C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/state.py — focus on _atomic_write_json (line 98), load_eloratings_cache (line 257), save_eloratings_cache (line 276); also read prediction history section lines 657-693 for load_prediction_history and append_prediction_history
  </read_first>

  <acceptance_criteria>
    1. Source assertion: constants.py contains ODDS_CACHE_TTL_HOURS=12, CATBOAST_CACHE_TTL_HOURS=24, ODDS_CACHE_FILE="odds_cache.json", CATBOAST_CACHE_FILE="catboost_cache.json", PREDICTION_HISTORY_SCHEMA_VERSION=2
    2. Behavior assertion (test): pytest tests/test_state.py passes all tests including new cache + save_prediction_history tests
    3. CLI assertion: `python -c "from src.state import is_cache_valid; assert is_cache_valid({'expires_at':'2099-01-01T00:00:00+00:00'}, 12)"` returns True
    4. Source assertion: state.py has save_prediction_history(history, data_dir) alongside load_prediction_history() and append_prediction_history()
    5. CLI assertion: save_prediction_history called with a test list persists the complete history (not just appends)
    6. Source assertion: predictors/__init__.py exists and is importable: `python -c "import src.predictors"`
  </acceptance_criteria>

  <behavior>
    RED: Write failing tests for is_cache_valid, load_signal_cache, save_signal_cache, save_prediction_history BEFORE implementing.

    Tests to add to test_state.py:
    - test_is_cache_valid_valid: cache with future expires_at → True
    - test_is_cache_valid_expired: cache with past expires_at → False
    - test_is_cache_valid_empty: empty dict → False
    - test_is_cache_valid_no_expires: cache without expires_at → False
    - test_is_cache_valid_malformed: invalid date string → False
    - test_save_load_signal_cache: save then load → equal content
    - test_signal_cache_not_exists: file doesn't exist → empty dict
    - test_save_prediction_history: save a 3-entry list → load_prediction_history returns identical list
    - test_save_prediction_history_overwrite: save twice, second replaces first (not append)

    GREEN: Implement all four.
  </behavior>

  <action>
    Step 1 — Add to constants.py (after ELORATINGS_TEAM_CODES dict, at end of file):
    ```python
    # ─── Signal Ingestion Constants (Phase 13) ────────────────────────────

    ODDS_CACHE_TTL_HOURS: int = 12
    """How long odds cache is valid in hours (D-06: resolved to 12h per research)."""

    CATBOOST_CACHE_TTL_HOURS: int = 24
    """How long CatBoost cache is valid in hours (D-06: resolved to 24h per research)."""

    ODDS_CACHE_FILE: str = "odds_cache.json"
    """Filename for market odds cache in data/ directory (D-04)."""

    CATBOOST_CACHE_FILE: str = "catboost_cache.json"
    """Filename for CatBoost prediction cache in data/ directory (D-04)."""

    PREDICTION_HISTORY_SCHEMA_VERSION: int = 2
    """Schema version for prediction_history.json. v1=flat (Phase 12b), v2=compound (Phase 13+)."""
    ```

    Step 2 — Add to state.py (after append_prediction_history at line 692, before load_eval_baseline_report at line 695), three cache functions and save_prediction_history:

    `load_signal_cache(cache_filename: str, data_dir=None) -> dict`
    - Path: _resolve_data_dir(data_dir) / cache_filename
    - Returns {} if file doesn't exist (graceful bootstrap, same pattern as load_eloratings_cache)
    - Opens with encoding='utf-8'

    `save_signal_cache(cache: dict, cache_filename: str, data_dir=None) -> None`
    - Path: _resolve_data_dir(data_dir) / cache_filename
    - Delegates to _atomic_write_json(cache, path)
    - Accepts any dict (both odds and catboost caches use this)

    `is_cache_valid(cache: dict, ttl_hours: int = 12) -> bool`
    - Returns False if cache is empty or no 'expires_at' key
    - Parses expires_at via datetime.fromisoformat()
    - Compares to datetime.now(timezone.utc)
    - Returns True only if cache has not expired
    - Catches ValueError/TypeError from malformed dates → returns False
    - Import at top of state.py: `from datetime import datetime, timezone`
    - Same pattern as existing eloratings cache validation in elo_sync.py

    `save_prediction_history(history: list[dict], data_dir=None) -> None`
    - **REVISION: This function replaces the full prediction_history.json (not append)**
    - Used by _merge_signals_into_history() in Plan 03 to inject signal data into existing entries
    - Path: _resolve_data_dir(data_dir) / "prediction_history.json"
    - Delegates to _atomic_write_json(history, path)
    - Signature: (history: list[dict], data_dir=None)

    Step 3 — Create worldcup_predictor/src/predictors/__init__.py:
    Create the directory first: `mkdir -p worldcup_predictor/src/predictors`
    Only a docstring — no exports needed:
    ```python
    """Prediction signal ingestion package.
    
    Provides market odds (odds.py) and CatBoost ML (catboost.py)
    prediction signal ingestion from the BSD API.
    """
    ```

    Step 4 — Verify: `pytest tests/test_state.py -x -q` → all existing + new tests pass.
  </action>

  <verify>
    <automated>pytest tests/test_state.py -x -q</automated>
  </verify>

  <done>
    - constants.py has ODDS_CACHE_TTL_HOURS, CATBOOST_CACHE_TTL_HOURS, ODDS_CACHE_FILE, CATBOOST_CACHE_FILE, PREDICTION_HISTORY_SCHEMA_VERSION
    - state.py has load_signal_cache(), save_signal_cache(), is_cache_valid(), save_prediction_history()
    - predictors/__init__.py exists with docstring
    - is_cache_valid returns correct boolean for valid/expired/empty/malformed caches
    - load_signal_cache returns {} for non-existent file
    - save_signal_cache persists atomically
    - save_prediction_history replaces entire history atomically (not append)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement odds.py — remove_vig, parse_odds_response, fetch_and_cache_odds</name>
  <files>
    worldcup_predictor/src/predictors/odds.py
  </files>

  <read_first>
    @C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/fetcher.py — fetch_raw_matches retry/backoff pattern lines 22-50, _build_alias_lookup line 162, _find_group_match line 204, _find_bracket_match line 183, _normalize_team line 179
    @C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/.planning/phases/13-signal-ingestion/13-RESEARCH.md — Code Examples section for remove_vig_decimal, BSD events endpoint odds extraction
  </read_first>

  <acceptance_criteria>
    1. Behavior assertion (test): `pytest tests/test_odds.py::TestVigRemoval -x -q` passes all vig removal tests
    2. Behavior assertion (test): `pytest tests/test_odds.py::TestMissingOdds -x -q` passes all missing odds tests
    3. Source assertion: odds.py has remove_vig, parse_odds_response, fetch_and_cache_odds functions
    4. CLI assertion: `python -c "from src.predictors.odds import remove_vig; r=remove_vig(2.0, 3.0, 2.0); assert abs(sum(r.values())-1.0)<1e-10"`
    5. Source assertion: parse_odds_response handles None odds → available=False with reason="odds_not_available"
    6. Source assertion: fetch_and_cache_odds returns dict with keys fetched_at, expires_at, matches
  </acceptance_criteria>

  <behavior>
    RED: Write failing tests in test_odds.py (before creating odds.py):
    - test_remove_vig_basic: odds [1.85, 3.40, 4.50] → normalized home prob ~0.498 (sum to 1.0)
    - test_remove_vig_all_three: sum of all three normalized probs == 1.0 ± 1e-10
    - test_remove_vig_even_odds: [2.0, 3.0, 2.0] → home=0.375, draw=0.25, away=0.375
    - test_remove_vig_high_vig: [1.10, 8.0, 15.0] → sum of all three == 1.0
    - test_parse_odds_response_valid: event with odds → match_id mapped, probability, available=True
    - test_parse_odds_response_null_odds: event with None odds → available=False, reason="odds_not_available"
    - test_parse_odds_response_zero_odds: event with 0 odds → available=False
    - test_parse_odds_response_missing_field: event missing odds keys → available=False

    GREEN: Implement odds.py.
  </behavior>

  <action>
    Step 1 — Create tests/test_odds.py with all RED tests (see behavior above).
    Import targets from src.predictors.odds (will fail until odds.py exists).
    Use from src.state import load_signal_cache, save_signal_cache, is_cache_valid for cache tests.
    Use from src.fetcher import _build_alias_lookup, _find_group_match, _find_bracket_match, _normalize_team for match resolution tests.

    Step 2 — Create src/predictors/odds.py with three functions:

    `remove_vig(odds_home: float, odds_draw: float, odds_away: float) -> dict[str, float]`
    - Implements basic 1/odds normalization (per RESEARCH.md "Don't Hand-Roll": "basic 1/odds normalization for MVP")
    - Convert each decimal odd → implied probability via 1/odds
    - Normalize: divide each by sum of all three
    - Returns dict: {"home": float, "draw": float, "away": float}
    - Sum of returned values must equal 1.0 ± floating point tolerance
    - Returns probabilities as floats, not rounded

    `parse_odds_response(bsd_events: list[dict], alias_lookup: dict[str, str], groups: dict) -> dict[str, dict]`
    - Takes raw BSD events list (already fetched by main loop)
    - For each event with status != "finished":
      - Normalize team names using alias_lookup
      - Resolve match_id: for group events use _find_group_match; for knockout use _find_bracket_match
      - Extract odds_home, odds_draw, odds_away from event dict (may be None or missing)
      - If all three odds present and > 0: call remove_vig, store normalized home probability with available=True
      - If any missing/null/zero: store available=False, reason="odds_not_available"
    - Returns dict mapping match_id → {probability, timestamp, available, reason?}
    - Uses from src.fetcher import _build_alias_lookup, _find_group_match, _find_bracket_match, _normalize_team
    - Uses from src.constants import ODDS_CACHE_TTL_HOURS
    - Uses from datetime import datetime, timezone

    `fetch_and_cache_odds(api_key: str, bsd_events: list[dict], alias_lookup: dict[str, str], groups: dict, cache_ttl_hours: int = 12) -> dict`
    - Full fetch → parse → cache pipeline
    - Calls parse_odds_response(bsd_events, alias_lookup, groups)
    - Builds cache dict: {fetched_at (now isoformat), expires_at ((now+ttl).isoformat), matches: {match_id: {...}}}
    - Uses timezone-aware UTC datetimes via datetime.now(timezone.utc)
    - Returns the complete cache dict ready for persistence via save_signal_cache()

    Step 3 — Verify RED: pytest tests/test_odds.py -x -q → ImportError or test failures.

    Step 4 — Implement all three functions. Verify GREEN: pytest tests/test_odds.py -x -q → all pass.
  </action>

  <verify>
    <automated>pytest tests/test_odds.py -x -q</automated>
  </verify>

  <done>
    - remove_vig() converts decimal odds to normalized probabilities summing to 1.0
    - parse_odds_response() handles valid, null, zero, and missing odds
    - fetch_and_cache_odds() produces cache dict with fetched_at, expires_at, matches
    - All odds tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Create comprehensive test_odds.py with integration tests</name>
  <files>
    worldcup_predictor/tests/test_odds.py
  </files>

  <read_first>
    @C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/worldcup_predictor/src/predictors/odds.py — verify function signatures before writing integration tests
    @C:/Users/KIIT0001/Documents/antigravity%20skills/FIFA-WC/.planning/phases/13-signal-ingestion/13-VALIDATION.md — test map reference (TestOddsCache, TestOddsPersistence)
  </read_first>

  <acceptance_criteria>
    1. Behavior assertion (test): `pytest tests/test_odds.py -x -q` — all tests pass (≥12 tests across 5 classes)
    2. Source assertion: TestOddsPersistence uses tmp_path for isolated file I/O, never writes to production data/
    3. Behavior assertion (test): `pytest tests/test_state.py -x -q` — state tests still pass
    4. Behavior assertion (test): `pytest -x` — full suite green with zero regressions
  </acceptance_criteria>

  <action>
    Add these additional test classes to tests/test_odds.py:

    `class TestOddsCache (per VALIDATION.md TestOddsCache)`
    - test_cache_produces_valid_schema: fetch_and_cache_odds returns dict with fetched_at, expires_at, matches keys
    - test_cache_expires_at_is_future: expires_at is a valid ISO datetime in the future (12h from fetched_at)
    - test_cache_matches_contains_entries: matches is a dict (may be empty for no events)

    `class TestOddsPersistence (per VALIDATION.md TestOddsPersistence)`
    - test_save_and_load_cache: save via save_signal_cache, load via load_signal_cache → identical content
    - test_cache_file_created: after save, cache file exists on disk
    - test_cache_round_trip: full save→load→verify values preserved
    - Uses tmp_path fixture for isolated file I/O
    - Uses from src.state import load_signal_cache, save_signal_cache

    `class TestFetchAndCacheOdds (per VALIDATION.md integration)`
    - test_fetch_and_cache_with_valid_events: minimal BSD event dicts → matches contain probability values
    - test_fetch_and_cache_all_missing_odds: events without odds → all matches have available=False
    - test_fetch_and_cache_mixed: some with odds, some without → correct available flags

    Also ensure TestVigRemoval class from Task 2 is comprehensive (at least 4 tests).

    Verify: `pytest tests/test_odds.py -x -q` → all pass (≥12 tests total).
    Verify: `pytest -x` → full suite green (all existing tests still pass).
  </action>

  <verify>
    <automated>pytest tests/test_odds.py -x -q && pytest tests/test_state.py -x -q</automated>
  </verify>

  <done>
    - test_odds.py has TestVigRemoval, TestMissingOdds, TestOddsCache, TestOddsPersistence, TestFetchAndCacheOdds
    - All odds tests pass (≥12 tests)
    - Full test suite passes with pytest -x
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BSD API → odds.py | Untrusted JSON response crosses here — odds fields may be null/missing/malformed |
| state.py → cache file | Write path — partial writes could corrupt cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | odds.py, fetch_raw_matches | mitigate | Never log BSD_API_KEY directly. Use same `Authorization: Token` header pattern as fetcher.py — token is in header, not logged. |
| T-13-02 | Tampering | parse_odds_response | mitigate | Type-check odds_home/odds_draw/odds_away: guard for `is not None and o > 0`. Set available=False + reason for any missing/null/non-positive value. |
| T-13-03 | Tampering | save_signal_cache, save_prediction_history | mitigate | Reuse `_atomic_write_json()` from state.py (tmpfile.mkstemp + os.replace) — prevents partial write corruption. |
| T-13-SC | Tampering | pip installs | mitigate | No new packages. requests already installed, slopcheck [OK]. No checkpoint needed. |
</threat_model>

<verification>
1. `pytest tests/test_odds.py -x -q` — ≥12 odds tests pass
2. `pytest tests/test_state.py -x -q` — state tests including new cache + save_prediction_history tests pass
3. `python -c "from src.predictors.odds import remove_vig; r=remove_vig(1.85,3.40,4.50); assert abs(sum(r.values())-1.0)<1e-10"` — vig removal correctness
4. `pytest -x` — full test suite green (no existing test regressions)
</verification>

<success_criteria>
1. `ODDS_CACHE_TTL_HOURS=12`, `CATBOOST_CACHE_TTL_HOURS=24`, cache filenames, and schema version defined in constants.py
2. `state.py` has `load_signal_cache()`, `save_signal_cache()`, `is_cache_valid()`, `save_prediction_history()` — all tested
3. `src/predictors/` package exists with `__init__.py`
4. `src/predictors/odds.py` has `remove_vig()`, `parse_odds_response()`, `fetch_and_cache_odds()`
5. Vig removal: normalized probabilities sum to 1.0 ± 1e-10
6. Missing/null odds handled gracefully (available=False + reason)
7. Cache schema: `{fetched_at, expires_at, matches: {match_id: {probability, timestamp, available, reason?}}}`
8. `save_prediction_history()` persists a complete history list atomically (replaces, not appends)
9. Test file `tests/test_odds.py` with 12+ tests covering all functions and edge cases
10. Full test suite passes with zero regressions
</success_criteria>

<output>
Create `.planning/phases/13-signal-ingestion/13-01-SUMMARY.md` when done
</output>
