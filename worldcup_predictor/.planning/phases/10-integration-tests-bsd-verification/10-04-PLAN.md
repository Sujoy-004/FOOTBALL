---
phase: 10-integration-tests-bsd-verification
plan: 04
type: execute
wave: 4
depends_on: [10-01-PLAN.md, 10-02-PLAN.md, 10-03-PLAN.md]
files_modified:
  - SOTs/PRD.md
  - SOTs/TRD.md
  - SOTs/MVP.md
  - SOTs/Appflow.md
  - SOTs/Backend_Schema.md
  - SOTs/UI_UX_Design.md
  - SOTs/Implementation_plan.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/FEATURES.md
autonomous: true
requirements: [INTG-08, INTG-10]
user_setup:
  - service: BSD
    why: "Live smoke test (INTG-08) requires valid BSD_API_KEY"
    env_vars:
      - name: BSD_API_KEY
        source: "https://sports.bzzoiro.com/account/"
---

<objective>
Batch-update all 7 Sources of Truth (SOTs) + 4 planning documents + 2 codebase docs to reflect Phase 10 completion and v1.1 readiness. Run the live BSD smoke test to verify end-to-end functionality.

Purpose: All project documentation accurately reflects the 48-team format. The live smoke test proves the full pipeline works with real API data.
Output: Documentation updated, manual smoke test results documented.
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-integration-tests-bsd-verification/10-CONTEXT.md (D-16, D-17, D-25)
@SOTs/PRD.md
@SOTs/TRD.md
@SOTs/MVP.md
@SOTs/Appflow.md
@SOTs/Backend_Schema.md
@SOTs/UI_UX_Design.md
@SOTs/Implementation_plan.md

<interfaces>
Current document status (pre-update):
- All 10 INTG requirements marked Pending in REQUIREMENTS.md
- Phase 10 success criteria marked TBD in ROADMAP.md
- STATE.md: Phase 10 Status: "Ready to execute"
- PROJECT.md: Phase 10 not marked complete
- ARCHITECTURE.md: Missing group match ingestion flow + played_groups.json persistence
- INTEGRATIONS.md: References Football-Data.org (needs BSD update)
- FEATURES.md: Missing group standings display, third-place bubble, live integration features

Document locations:
- SOTs: C:\Users\KIIT0001\Documents\antigravity skills\FIFA-WC\SOTs\
- Planning: C:\Users\KIIT0001\Documents\antigravity skills\FIFA-WC\.planning\
- Codebase: C:\Users\KIIT0001\Documents\antigravity skills\FIFA-WC\.planning\codebase\
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch-update all 7 SOTs + 4 planning docs + 2 codebase docs (INTG-10)</name>
  <files>
    SOTs/PRD.md
    SOTs/TRD.md
    SOTs/MVP.md
    SOTs/Appflow.md
    SOTs/Backend_Schema.md
    SOTs/UI_UX_Design.md
    SOTs/Implementation_plan.md
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/codebase/ARCHITECTURE.md
    .planning/codebase/FEATURES.md
    .planning/codebase/INTEGRATIONS.md
  </files>
  <read_first>
    Each file listed in <files> — read each one to understand its current state before editing
    .planning/phases/10-integration-tests-bsd-verification/10-CONTEXT.md (D-25, specific update instructions for each SOT)
    .planning/phases/10-integration-tests-bsd-verification/10-01-PLAN.md (for architecture details)
    .planning/phases/10-integration-tests-bsd-verification/10-02-PLAN.md (for feature descriptions)
    .planning/phases/10-integration-tests-bsd-verification/10-03-PLAN.md (test coverage info)
  </read_first>
  <action>
    Per D-25, batch-update all documents. Read each file first, then apply targeted edits. Do NOT rewrite entire files — make surgical updates to the relevant sections.

    **1. `.planning/PROJECT.md`**
    - Find the requirement traceability table (look for INTG-01 through INTG-10 rows)
    - Change all 10 INTG requirements from `Pending` or `[ ]` to `Complete` or `[x]`
    - Find the phase status section; mark Phase 10 as `Complete`

    **2. `.planning/REQUIREMENTS.md`**
    - Find the traceability table at lines 76-109
    - Change INTG-01 through INTG-10 status from `Pending` to `Complete`
    - Find the Integration & Live Data section (lines 41-51); change `[ ]` to `[x]` for all 10 items
    - Add a note: `Phase 10 complete: 2026-06-14`
    - Update the coverage stats at lines 112-114 (should show 31/31 mapped)

    **3. `.planning/STATE.md`**
    - Find the Current Position section (lines 29-35)
    - Update Status from "Ready to execute" to "Complete"
    - Update Plans from "TBD" to "4 plans in 4 waves — all complete"
    - Find the Completed Phases section (lines 38-62)
    - Add Phase 10 entry after Phase 9's entry (after line 62):
      ```markdown
      ### Phase 10: Integration, Tests & BSD Verification — Complete 2026-06-14
      - 4 plans, 4 waves, all executed
      - BSD API group match ingestion + played_groups.json persistence
      - 12-group standings display with box-drawing and third-place bubble
      - 2 deferred test failures fixed, 7 new integration tests
      - All 7 SOTs batch-updated for 48-team format
      - Live BSD smoke test: [PASS/FAIL]
      ```
    - Update the Accumulated Context section: move deferred items from "Carried to Phase 10" to "Resolved in Phase 10"
    - Update Research Flags for Phase 10: mark BSD API format research as ✅ resolved
    - Mark v1.1 milestone as complete in the Milestones section

    **4. `.planning/ROADMAP.md`**
    - Find Phase 10 entry (lines 162-181)
    - Change `[ ]` to `[x]` for Phase 10
    - Update Plans from "TBD" to "4 plans"
    - Update success criteria: add verification status (e.g., "✅ Verified" for each criterion)
    - Plan list:
      ```markdown
      Plans:
      - [x] 10-01-PLAN.md — Group Match Ingestion & Persistence (Wave 1)
      - [x] 10-02-PLAN.md — Group Standings Console Display (Wave 2)
      - [x] 10-03-PLAN.md — Test Fixes & E2E Tests (Wave 3)
      - [x] 10-04-PLAN.md — SOT Batch Update & Smoke Test (Wave 4)
      ```
    - Update Progress table at line 187: mark Phase 10 as `4/4`, Status `Complete`, Completed date

    **5. `SOTs/PRD.md`**
    - Scan for any references to integration requirements, group match ingestion, or Phase 10 scope
    - Update the status of Phase 10 deliverables from "planned"/"pending" to "complete"
    - Add Phase 10 implementation summary

    **6. `SOTs/TRD.md`**
    - Update technical decisions for Phase 10: document played_groups.json persistence, process_group_matches(), box-drawing display
    - Ensure architecture diagram or text includes group match flow

    **7. `SOTs/MVP.md`**
    - Update the milestone checklist: mark v1.1 Phase 10 items as complete
    - Add integration verification results

    **8. `SOTs/Appflow.md`**
    - Update application flow diagram or description to include group match ingestion, played_groups.json persistence, and group standings display
    - Show the split routing: group_name != null → process_group_matches() vs null → process_matches()

    **9. `SOTs/Backend_Schema.md`**
    - Add played_groups.json schema (per D-04 entry structure)
    - Document the new process_group_matches() function signature
    - Add simulate_group_matches() played_groups parameter

    **10. `SOTs/UI_UX_Design.md`**
    - Add group standings display layout (box-drawing table with columns)
    - Add third-place bubble display format
    - Add header update for 48-team counts

    **11. `SOTs/Implementation_plan.md`**
    - Mark Phase 10 plans as implemented
    - Add any implementation notes or deviations from original plan

    **12. `.planning/codebase/ARCHITECTURE.md`**
    - Add group match ingestion flow diagram (BSD API → process_group_matches → played_groups.json)
    - Add documentation for `process_group_matches()` and helper functions
    - Add `load_played_groups()` / `save_played_groups()` to state layer
    - Document `simulate_group_matches()` played_groups parameter
    - Document `run_full_simulation()` played_groups parameter
    - Add `print_group_standings()` and `print_third_place_bubble()` to display layer

    **13. `.planning/codebase/FEATURES.md`**
    - Add section: "Live Group Match Ingestion — BSD API polls detect group matches and persist them"
    - Add section: "Group Standings Display — 12-group box-drawing table with third-place bubble"
    - Add section: "48-Team Console Header — Updated startup banner with group match counts"
    - Document known limitation per D-17: "Tiebreaker Step 7 uses Elo-as-FIFA-ranking-proxy (~0.92 correlation)"

    **14. `.planning/codebase/INTEGRATIONS.md`**
    - Replace all Football-Data.org references with BSD (Bzzoiro Sports Data)
    - Update API endpoint: `https://sports.bzzoiro.com/api/events/?status=finished&league_id=27`
    - Update authentication header: `Authorization: Token {api_key}`
    - Document the `group_name` field discrimination for group vs knockout match routing
    - Document the `played_groups.json` persistence path
    - Remove or update any deprecated Football-Data.org integration docs

    IMPORTANT NOTES:
    - Each edit must be surgical — read the file, identify the exact sections to change, make targeted edits
    - Do NOT rewrite entire files
    - For the codebase docs, read them first to understand the current structure, then add Phase 10 sections following the same format
    - Use the Write tool for each file edit (NOT heredoc/Bash)
  </action>
  <verify>
    <automated>
      # Verify all SOTs exist and are valid markdown
      python -c "
import os
files = [
    'SOTs/PRD.md', 'SOTs/TRD.md', 'SOTs/MVP.md', 'SOTs/Appflow.md',
    'SOTs/Backend_Schema.md', 'SOTs/UI_UX_Design.md', 'SOTs/Implementation_plan.md',
    '.planning/PROJECT.md', '.planning/REQUIREMENTS.md', '.planning/STATE.md',
    '.planning/ROADMAP.md', '.planning/codebase/ARCHITECTURE.md',
    '.planning/codebase/FEATURES.md', '.planning/codebase/INTEGRATIONS.md',
]
for f in files:
    assert os.path.exists(f), f'Missing: {f}'
    with open(f, encoding='utf-8') as fh:
        content = fh.read()
    assert len(content) > 100, f'{f} appears truncated'
    print(f'✅ {f} ({len(content)} bytes)')
print('All 14 files verified')
"
    </automated>
  </verify>
  <done>
    All 14 documents updated with Phase 10 completion status. INTG-01 through INTG-10 marked Complete in REQUIREMENTS.md. v1.1 milestone marked complete in STATE.md and ROADMAP.md.
  </done>
  <acceptance_criteria>
    - REQUIREMENTS.md: all INTG requirements show Status=Complete
    - ROADMAP.md: Phase 10 marked [x] with 4 plans and verified success criteria
    - STATE.md: Phase 10 completion entry added, v1.1 milestone marked complete
    - PROJECT.md: requirement traceability shows all INTG rows complete
    - ARCHITECTURE.md: includes group match ingestion flow and new functions
    - FEATURES.md: includes group standings, third-place bubble, header update sections
    - INTEGRATIONS.md: uses BSD API documentation (not Football-Data.org)
    - All 7 SOTs reflect Phase 10 completion with accurate implementation details
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live BSD smoke test with --once (INTG-08)</name>
  <what-built>
    Full 48-team pipeline with BSD API group match ingestion, played_groups.json persistence, 
    group standings display, third-place bubble, and 192+ passing tests.
  </what-built>
  <how-to-verify>
    Run the live BSD smoke test to verify end-to-end functionality:

    1. Open PowerShell/CMD in `C:\Users\KIIT0001\Documents\antigravity skills\FIFA-WC\worldcup_predictor`
    2. Make sure `BSD_API_KEY` environment variable is set:
       ```
       $env:BSD_API_KEY = "your_key_here"
       ```
       (Replace with your actual BSD API key from https://sports.bzzoiro.com/account/)

    3. Run the smoke test:
       ```
       python -m pytest tests/test_live_smoke.py -x -v
       ```
       Expected: Test PASSES (fetches live data, simulates, prints valid 48-team predictions)

    4. Alternative manual verification:
       ```
       python main.py --once --seed 42
       ```
       Expected output (verify these elements):
       ```
       [timestamp] WORLD CUP DYNAMIC PREDICTOR — v1.1
       [timestamp] Loaded 48 teams, 12 groups (72 group matches, 40 bracket matches), ...
       [timestamp] GROUP STANDINGS — 12 groups, best 8 third-placed advance
       ┌─────────┬────────────────────┬───┬─────┬──────┐
       │ Group A │ ...                │   │     │      │
       ...
       └─────────┴────────────────────┴───┴─────┴──────┘
       [timestamp] Third-place bubble: ...
       [timestamp] Re-simulating (50000 runs)... done in ...s
       [timestamp] UPDATED PROBABILITIES:
       [timestamp] Initial probabilities:
         1. Spain    ...
       ```

    5. If group matches exist (WC is ongoing), verify:
       - Group standings show real results (not simulated) for played matches
       - Third-place bubble shows correct cutoffs
       - Probability table shows 48 teams

    If API key is not available:
    - Document the test as "Deferred — needs BSD_API_KEY"
    - Run with `--once --seed 42` using the mock path (won't work without key)
    - Defer to manual verification when key becomes available
  </how-to-verify>
  <resume-signal>
    Type "smoke-passed" if the live smoke test succeeded, or describe any issues found
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Document file I/O | Writing .md files in project tree |
| BSD API (live smoke test) | External HTTP call with API key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Information Disclosure | API key in smoke test output | mitigate | Smoke test does NOT echo the API key; subprocess captures stdout/stderr separately, only verified for expected patterns |
| T-10-11 | Denial of Service | Live API call in test | accept | 60s timeout on subprocess; skipif decorator prevents automatic runs; only triggers when user explicitly provides key |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this phase |
</threat_model>

<verification>
## Per-Task Verification

1. Task 1: All 14 files pass the existence + content length check
2. Task 2: Live smoke test passes (or deferred with documentation)

## Phase-Level
- Full test suite: `python -m pytest tests/ -x -q` — 192+ tests pass
- All 14 documentation files updated and verified
- Live smoke test (INTG-08) passed or documented as deferred
</verification>

<success_criteria>
1. All 7 SOTs + 4 planning docs + 3 codebase docs updated for Phase 10 completion
2. All 10 INTG requirements marked Complete in REQUIREMENTS.md
3. v1.1 milestone marked complete
4. Live BSD smoke test passes (or clearly documented as deferred with reason)
5. Full test suite: 192+ tests pass with 0 failures
</success_criteria>

<output>
Create `10-04-SUMMARY.md` when done
</output>
