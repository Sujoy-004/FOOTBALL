---
phase: "17"
plan: "03"
type: "execute"
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified:
  - tests/test_enrichment.py (new)
  - tests/test_fetcher.py (extend)
autonomous: true
requirements:
  - V2-21
  - V2-22
---

# Phase 17 — Plan 03: Tests

<objective>
Create `tests/test_enrichment.py` with unit tests for `extract_stats()` and `extract_context()`, covering all P0 fields, graceful degradation modes, and edge cases. Extend `test_fetcher.py` with integration tests verifying enrichment keys appear on entries produced by both process functions.
</objective>

<tasks>
## Tasks

### Task 1: Create BSD event fixtures for tests

<type>execute</type>
<files>
  - tests/test_enrichment.py (new)
  - tests/conftest.py (extend with BSD event fixtures)
</files>
<read_first>
  - _probe/completed_match_raw.json (real BSD event payload for fixture reference)
  - tests/conftest.py (existing fixture patterns — sample_teams, sample_bracket)
  - tests/test_fetcher.py:31-44 (SAMPLE_MATCHES pattern)
</read_first>

<action>
In `tests/conftest.py`, add:

1. `bsd_finished_event` fixture — returns a dict shaped like a real finished BSD event with:
   - All standard fields (id, status="finished", home_team, away_team, home_score, away_score, event_date, group_name, round_number)
   - `live_stats` populated with home/away dicts containing all P0 stat fields: `yellow_cards` (int), `red_cards` (int), `shots_on_target` (int), `ball_possession` (int 0-100)
   - `venue` dict with `{"name": "Estadio Azteca", "city": "Mexico City"}`
   - `referee` dict with `{"name": "Wilton Pereira Sampaio"}`
   - Minimal team/non-stat fields (home_team_obj, away_team_obj, league, season) — just enough to be realistic without bloat

2. `bsd_finished_event_no_stats` fixture — same as above but `live_stats: None`

3. `bsd_finished_event_partial_stats` fixture — same as finished but `live_stats.home` and `live_stats.away` contain only `ball_possession` (no cards/shots fields)

4. `bsd_finished_event_no_context` fixture — same as finished but `venue: None` and `referee: None`

Alternatively, define these as module-level dicts in `test_enrichment.py` (simpler, avoids conftest bloat). Use module-level dicts.
</action>

<acceptance_criteria>
- Fixtures are valid dicts that pass `isinstance(x, dict)` checks
- `bsd_finished_event["live_stats"]["home"]["yellow_cards"]` returns an int
- `bsd_finished_event_no_stats["live_stats"]` is None
- `bsd_finished_event_no_context["venue"]` is None
- Fixtures exist as module-level constants in tests/test_enrichment.py
</acceptance_criteria>

### Task 2: Unit tests for `extract_stats()`

<type>tdd</type>
<files>
  - tests/test_enrichment.py (new)
</files>
<read_first>
  - src/enrichment.py (extract_stats implementation)
  - .planning/phases/17-enriched-match-context/17-CONTEXT.md:54-55 (D-07 — sparse fields convention)
  - .planning/phases/17-enriched-match-context/17-CONTEXT.md:73-78 (D-13 — P0 scope: 6 fields)
</read_first>

<action>
In `tests/test_enrichment.py`, add these test functions:

1. `test_extract_stats_all_fields` — feed `bsd_finished_event` to `extract_stats()`, assert returns dict with all 8 internal stat names (_home + _away for 4 stat types):
   - `yellow_cards_home`: int
   - `yellow_cards_away`: int
   - `red_cards_home`: int
   - `red_cards_away`: int
   - `shots_on_target_home`: int
   - `shots_on_target_away`: int
   - `possession_home`: int (0-100)
   - `possession_away`: int (0-100)

2. `test_extract_stats_none` — feed `bsd_finished_event_no_stats` (live_stats=None), assert returns None

3. `test_extract_stats_partial` — feed `bsd_finished_event_partial_stats` (only ball_possession), assert returns dict with only `possession_home` and `possession_away` keys (no cards or shots keys — sparse-fields convention per D-07)

4. `test_extract_stats_possession_type` — assert `possession_home` is int (not float or str)

5. `test_extract_stats_empty_live_stats` — feed event with `live_stats={}` (empty dict, not None), assert returns None (or empty dict — the "no stats available" contract)

6. `test_extract_stats_upcoming_match` — feed event with `status="notstarted"` and `live_stats=None`, assert returns None
</action>

<acceptance_criteria>
- `test_extract_stats_all_fields` passes with all 8 field values matching fixture data
- `test_extract_stats_none` passes — None live_stats returns None
- `test_extract_stats_partial` passes — returns dict with only `possession_home` and `possession_away`
- `test_extract_stats_possession_type` passes — values are int, not float
- `pytest tests/test_enrichment.py::test_extract_stats_all_fields -x` exits 0
- All 5-6 extract_stat tests pass
</acceptance_criteria>

### Task 3: Unit tests for `extract_context()`

<type>tdd</type>
<files>
  - tests/test_enrichment.py
</files>
<read_first>
  - src/enrichment.py (extract_context implementation)
</read_first>

<action>
Add these test functions:

1. `test_extract_context_both` — feed `bsd_finished_event`, assert returns `{"venue": "Estadio Azteca", "referee": "Wilton Pereira Sampaio"}`

2. `test_extract_context_no_context` — feed `bsd_finished_event_no_context` (venue=None, referee=None), assert returns None

3. `test_extract_context_only_venue` — feed event with venue dict present but referee=None, assert returns `{"venue": "..."}` (no referee key — sparse-fields per D-07)

4. `test_extract_context_only_referee` — feed event with referee dict present but venue=None, assert returns `{"referee": "..."}` (no venue key)

5. `test_extract_context_upcoming_match` — feed notstarted event with venue/referee populated, assert returns both values (context is available even for future matches, unlike stats)
</action>

<acceptance_criteria>
- `test_extract_context_both` passes — both venue and referee extracted correctly
- `test_extract_context_no_context` passes — None when both sources are None
- `test_extract_context_only_venue` passes — partial dict with only venue key
- `test_extract_context_only_referee` passes — partial dict with only referee key
- `test_extract_context_upcoming_match` passes — venue/referee still extracted for notstarted events
- All 5 extract_context tests pass
</acceptance_criteria>

### Task 4: Integration tests for fetcher enrichment

<type>tdd</type>
<files>
  - tests/test_fetcher.py (extend)
</files>
<read_first>
  - tests/test_fetcher.py:31-44 (SAMPLE_MATCHES pattern)
  - tests/test_fetcher.py:59-end (existing test structure for process_matches)
  - src/fetcher.py:86-159 (process_matches enriched)
  - src/fetcher.py:236-353 (process_group_matches enriched)
</read_first>

<action>
In `tests/test_fetcher.py`, add:

1. `test_process_matches_with_stats` — create a SAMPLE_MATCHES-like list where the BSD event includes `live_stats` with home/away dicts. Call `process_matches()`. Assert the returned entry has `"stats"` key with expected field values.

2. `test_process_matches_with_context` — same event but assert `"context"` key present with venue + referee.

3. `test_process_matches_no_stats_for_upcoming` — create a SAMPLE_MATCHES-like list with `status="finished"` but `live_stats=None` (simulating a finished match with no stats data). Assert returned entry does NOT have `"stats"` key.

4. (optional) `test_process_group_matches_with_enrichment` — same integration concept for `process_group_matches()` path, if the existing test infrastructure supports it easily.
</action>

<acceptance_criteria>
- `test_process_matches_with_stats` passes — stats dict present with matching values
- `test_process_matches_with_context` passes — context dict present with venue/referee
- `test_process_matches_no_stats_for_upcoming` passes — stats key absent from entry
- All 3-4 integration tests pass alongside existing fetcher tests
- `pytest tests/test_fetcher.py -x` exits 0 with all tests passing
</acceptance_criteria>

</tasks>

<verification>
## Verification

1. `pytest tests/test_enrichment.py -v` — all enrichment unit tests pass
2. `pytest tests/test_fetcher.py -v` — all fetcher tests pass (existing + new integration)
3. `pytest tests/ -x --tb=short` — full test suite passes with 0 regressions (may exclude live smoke)
4. Verify `pytest --coverage` or equivalent shows new code has ≥90% line coverage
</verification>

<success_criteria>
- 10-12 new tests created across test_enrichment.py and test_fetcher.py
- All enrichment tests pass with verified P0 field extraction
- Full test suite passes with 0 regressions
- Coverage confirms enrichment module is well-tested (edge cases: None live_stats, partial stats, no context, upcoming match)
</success_criteria>

<deep_work_rules>
All tasks include `<read_first>` and `<acceptance_criteria>`. Tasks 2 and 3 use `type: tdd` (business logic with defined I/O).
</deep_work_rules>
</objective>
