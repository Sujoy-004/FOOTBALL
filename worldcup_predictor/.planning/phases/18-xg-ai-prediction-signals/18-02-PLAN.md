---
phase: 18-xg-ai-prediction-signals
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fetcher.py
  - src/output.py
  - tests/test_fetcher.py
  - tests/test_output.py
autonomous: true
requirements:
  - V2-24
must_haves:
  truths:
    - "AI preview text extracted from BSD events endpoint and stored inline on played.json/played_groups.json entries"
    - "AI preview follows Phase 17 enrichment pattern — inline storage, optional key, graceful degradation"
    - "print_ai_previews() function exists in output.py — displays AI previews in a single block"
    - "Missing ai_preview produces no warnings, no errors, no output"
    - "No separate ai_previews.json file created — stored inline per D-08"
  artifacts:
    - path: src/fetcher.py
      provides: "_extract_ai_preview() helper + wiring in process_matches() and process_group_matches()"
      contains: "_extract_ai_preview|ai_preview"
    - path: src/output.py
      provides: "print_ai_previews() display function"
      contains: "def print_ai_previews"
    - path: tests/test_fetcher.py
      provides: "AI preview extraction tests"
      contains: "ai_preview"
    - path: tests/test_output.py
      provides: "AI preview display tests"
      contains: "print_ai_previews"
  key_links:
    - from: "_extract_ai_preview(match)"
      to: "entry[ai_preview]"
      via: "entry construction"
      pattern: "ai_preview"
    - from: "entry[ai_preview]"
      to: "print_ai_previews()"
      via: "--ai-preview flag"
      pattern: "ai_preview"
---

<objective>
**AI Preview Extraction & Display**

**Purpose:** Extract BSD AI pre-match analysis (`ai_preview.text` field) from the events endpoint during match processing and store it inline on played.json/played_groups.json entries per Phase 17 enrichment pattern (D-08). Add a display function `print_ai_previews()` in output.py that prints AI previews for all matches that have them (D-09, D-11).

**Output:** Modified `fetcher.py` (extraction helper + wiring), `output.py` (display function), `test_fetcher.py` + `test_output.py` (tests).
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context
@.planning/phases/18-xg-ai-prediction-signals/18-CONTEXT.md
@.planning/phases/18-xg-ai-prediction-signals/18-RESEARCH.md
@.planning/phases/18-xg-ai-prediction-signals/18-PATTERNS.md

# Phase 17 enrichment pattern (AI preview extraction follows same pattern)
@.planning/phases/17-enriched-match-context/17-CONTEXT.md

# Source files to modify (read before editing)
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/src/fetcher.py
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/src/output.py
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/tests/test_fetcher.py
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/tests/test_output.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: AI preview extraction helper + wiring in fetcher.py</name>
  <files>src/fetcher.py</files>
  <read_first>
    @C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/src/fetcher.py

    Read the full file. Key focus areas:
    - Lines 86-170: process_matches() — entry construction at lines 149-167. Note where stats/context extraction happens (lines 160-166)
    - Lines 247-374: process_group_matches() — entry construction at lines 353-370. Note where stats/context extraction happens (lines 364-370)
    - Module-level helper pattern: _extract_group_letter() at lines 221-232 is a good pattern analog
    - Existing imports at top (lines 1-84) — no new imports needed
  </read_first>
  <action>
    Add AI preview extraction to src/fetcher.py:

    1. Add a new module-level helper function `_extract_ai_preview(raw_event: dict) -> str | None`:
       - BSD API returns `ai_preview` as a nested dict: `{"text": "...", "generated_at": "..."}`
       - Check `raw_event.get("ai_preview")` — if it is a dict, get `.get("text")`
       - If text is a non-empty string, return `text.strip()`
       - Otherwise return None
       - Docstring: "Extract AI preview text from a raw BSD event dict. Returns the text string or None if absent. Graceful degradation per D-11: missing preview = None. No warnings. No errors."

    2. In `process_matches()` AFTER the context extraction block (lines 164-166), BEFORE `results.append(entry)` (line 168), ADD:
       ```
       ai_preview = _extract_ai_preview(match)
       if ai_preview is not None:
           entry["ai_preview"] = ai_preview
       ```

    3. In `process_group_matches()` AFTER the context extraction block (lines 368-370), BEFORE `results.append(entry)` (line 372), ADD:
       ```
       ai_preview = _extract_ai_preview(match)
       if ai_preview is not None:
           entry["ai_preview"] = ai_preview
       ```

    Do NOT modify existing enrichment logic. AI preview extraction is additive.
  </action>
  <verify>
    <automated>python -c "from src.fetcher import _extract_ai_preview; assert _extract_ai_preview({'ai_preview': {'text': 'Mexico expected to dominate.'}}) == 'Mexico expected to dominate.'; assert _extract_ai_preview({'ai_preview': {'text': ''}}) is None; assert _extract_ai_preview({'ai_preview': {}}) is None; assert _extract_ai_preview({}) is None; print('_extract_ai_preview OK')"</automated>
  </verify>
  <done>
    _extract_ai_preview() returns text for valid preview, None for missing/empty. Wired into both process_matches() and process_group_matches().
  </done>
  <acceptance_criteria>
    - _extract_ai_preview() exists as module-level function in fetcher.py
    - Returns stripped text when ai_preview.text is present
    - Returns None when ai_preview is missing, empty, or text is empty
    - process_matches() output includes "ai_preview" key when BSD event has it
    - process_group_matches() output includes "ai_preview" key when BSD event has it
    - Neither function crashes when ai_preview is absent
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: print_ai_previews() display function in output.py</name>
  <files>src/output.py</files>
  <read_first>
    @C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/src/output.py

    Read the full file. Key focus areas:
    - Lines 272-282: print_match_alert() — display function pattern (docstring, print formatting)
    - Lines 176-216: print_group_standings() — section header pattern
    - Lines 39-46: ANSI wrappers (_dim, _bold_white) available for use
    - Existing imports on lines 6-11 — no new imports needed
  </read_first>
  <action>
    Add `print_ai_previews()` function to src/output.py:

    Add a new function between `print_match_alert()` (ending line 282) and `print_elo_changes()` (starting line 284):

    ```
    def print_ai_previews(played: dict, played_groups: dict) -> None:
        """Print AI preview text for all played matches.

        Default console output unchanged. AI preview shown only when --ai-preview
        CLI flag is passed (D-09). Missing ai_preview produces no warnings or errors (D-11).

        Args:
            played: Dict of played knockout matches.
            played_groups: Dict of played group matches.
        """
        has_any = False

        # Group matches (chronological by group letter, then match_id)
        for group_letter in "ABCDEFGHIJKL"[:GROUP_COUNT]:
            group_matches = [
                m for m in played_groups.values()
                if m.get("match_id", "").startswith(f"GS_{group_letter}_")
            ]
            if not group_matches:
                continue
            for match in sorted(group_matches, key=lambda m: m.get("match_id", "")):
                preview = match.get("ai_preview")
                if preview:
                    if not has_any:
                        print(_bold_white("\n─── AI Previews ───"))
                        has_any = True
                    print(f"\n{_bold_white(match['team_a'])} vs {_bold_white(match['team_b'])}:")
                    print(preview)

        # Knockout matches (after group section)
        if played:
            for mid in sorted(played):
                match = played[mid]
                preview = match.get("ai_preview")
                if preview:
                    if not has_any:
                        print(_bold_white("\n─── AI Previews ───"))
                        has_any = True
                    print(f"\n{_bold_white(match['team_a'])} vs {_bold_white(match['team_b'])}:")
                    print(preview)

        if not has_any:
            print(_dim("No AI previews available."))
    ```

    Use `_bold_white` for header and team names. Use `_dim` for no-previews message. Both already defined in output.py.
  </action>
  <verify>
    <automated>python -c "from src.output import print_ai_previews; import inspect; assert callable(print_ai_previews); sig = inspect.signature(print_ai_previews); assert 'played' in sig.parameters; assert 'played_groups' in sig.parameters; print('print_ai_previews signature OK')"</automated>
  </verify>
  <done>
    print_ai_previews() is callable, takes (played, played_groups), uses _bold_white and _dim ANSI wrappers.
  </done>
  <acceptance_criteria>
    - print_ai_previews() is a function in output.py
    - Signature: def print_ai_previews(played: dict, played_groups: dict) -> None
    - Function uses _bold_white for section header and team names
    - Function prints "No AI previews available." when no matches have ai_preview
    - Function prints matches with ai_preview, skips those without
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: AI preview extraction + display tests</name>
  <files>tests/test_fetcher.py, tests/test_output.py</files>
  <read_first>
    @C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/tests/test_fetcher.py (lines 1-70)
    @C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/tests/test_output.py (lines 1-100)

    Read both test files. Key patterns:
    - test_fetcher.py: SAMPLE_MATCHES fixture (lines 31-42), MockResponse class (lines 11-28)
    - test_output.py: _capture() helper (lines 61-70) for stdout capture
    - output test pattern: fixtures + _capture + assertions on captured text
  </read_first>
  <action>
    Add tests:

    A — In tests/test_fetcher.py:

    1. Import `_extract_ai_preview` from `src.fetcher` at the top of the file (line 8).

    2. Add a new test function `test_extract_ai_preview_present()`:
       - Create a raw BSD match dict with `ai_preview: {"text": "Mexico expected to dominate possession."}`
       - Call `_extract_ai_preview(match)`
       - Assert result == "Mexico expected to dominate possession."

    3. Add `test_extract_ai_preview_missing()`:
       - Create a raw BSD match dict WITHOUT ai_preview key
       - Call `_extract_ai_preview(match)`
       - Assert result is None

    4. Add `test_extract_ai_preview_empty_text()`:
       - Create match dict with `ai_preview: {"text": ""}`
       - Assert result is None

    5. Add `test_extract_ai_preview_not_dict()`:
       - Create match dict with `ai_preview: "just a string, not a dict"`
       - Assert result is None

    B — In tests/test_output.py:

    1. Import `print_ai_previews` from `src.output` at the top (line 10, add to existing imports).

    2. Add a new test function `test_print_ai_previews_shows_text()`:
       - Create `played_groups` dict with one entry having ai_preview
       - Create empty `played` dict
       - Use `_capture(print_ai_previews, {}, played_groups)`
       - Assert team names and preview text appear in captured output
       - Assert "AI Previews" header appears

    3. Add `test_print_ai_previews_no_data()`:
       - Create played_groups dict with entry that has NO ai_preview
       - Use _capture
       - Assert "No AI previews available." in output

    4. Add `test_print_ai_previews_knockout_and_group()`:
       - Create both played and played_groups with ai_preview entries
       - Use _capture
       - Assert both knockout and group match previews appear
  </action>
  <verify>
    <automated>pytest tests/test_fetcher.py -x -q -k "ai_preview" -v</automated>
    <automated>pytest tests/test_output.py -x -q -k "ai_preview" -v</automated>
    <automated>pytest tests/test_fetcher.py -x -q 2>&1 | Select-String -NotMatch "PASSED|test_fetcher"</automated>
    <automated>pytest tests/test_output.py -x -q 2>&1 | Select-String -NotMatch "PASSED|test_output"</automated>
  </verify>
  <done>
    AI preview extraction tests (4) + display tests (3) all passing. Zero regressions on fetcher and output test suites.
  </done>
  <acceptance_criteria>
    - test_extract_ai_preview_present passes: valid ai_preview returns text
    - test_extract_ai_preview_missing passes: absent ai_preview returns None
    - test_extract_ai_preview_empty_text passes: empty text returns None
    - test_extract_ai_preview_not_dict passes: non-dict ai_preview returns None
    - test_print_ai_previews_shows_text passes: preview text appears in captured output
    - test_print_ai_previews_no_data passes: "No AI previews available." shown
    - test_print_ai_previews_knockout_and_group passes: both match types shown
    - All existing fetcher and output tests pass (zero regressions)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BSD API -> ai_preview field | AI preview text from events endpoint crosses into played.json storage |
| ai_preview -> terminal display | User sees AI preview text via --ai-preview flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | ai_preview extraction | mitigate | Values from trusted BSD HTTPS endpoint only. Type-check (isinstance dict) before accessing nested text. |
| T-18-05 | Spoofing | Terminal display of ai_preview | accept | CLI display is raw text. No HTML rendering. Terminal escape injection risk is low and matches existing pattern (all match data displayed as raw text). |
| T-18-SC | Tampering | cargo-cult dependency risk | mitigate | No new packages. All changes use existing imports (typing, io for tests). |
</threat_model>

<verification>
- `python -c "from src.fetcher import _extract_ai_preview; assert _extract_ai_preview({'ai_preview': {'text': 'test'}})"` — helper works
- `python -c "from src.output import print_ai_previews; import inspect; assert 'played' in inspect.signature(print_ai_previews).parameters"` — display callable
- `pytest tests/test_fetcher.py -x -q -k "ai_preview" -v` — 4 extraction tests pass
- `pytest tests/test_output.py -x -q -k "ai_preview" -v` — 3 display tests pass
- `pytest tests/test_fetcher.py tests/test_output.py -x -q` — zero regressions
</verification>

<success_criteria>
- [x] _extract_ai_preview() helper added to fetcher.py
- [x] AI preview extraction wired into process_matches() and process_group_matches()
- [x] print_ai_previews() function added to output.py
- [x] 4 AI preview extraction tests in test_fetcher.py
- [x] 3 AI preview display tests in test_output.py
- [x] Zero regressions on fetcher + output test suites
</success_criteria>

<output>
Create `.planning/phases/18-xg-ai-prediction-signals/18-02-SUMMARY.md` when done
</output>
