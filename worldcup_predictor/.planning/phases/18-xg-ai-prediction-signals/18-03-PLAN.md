---
phase: 18-xg-ai-prediction-signals
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
  - 18-02
files_modified:
  - main.py
  - tests/test_cli.py
  - tests/test_main_loop.py
  - tests/test_group_integration.py
autonomous: true
requirements:
  - V2-23
  - V2-24
must_haves:
  truths:
    - "--ai-preview CLI flag accepted and parsed (ai_preview=True)"
    - "xg_overrides dict built from CatBoost cache entries that have expected_home_goals and expected_away_goals"
    - "xg_overrides passed to run_full_simulation() using the new parameter added in Plan 18-01"
    - "AI preview display triggered when --ai-preview flag is True"
    - "_compute_group_display() call to precompute_matchup_lambdas() does NOT receive xg_overrides (correct — display uses Elo lambdas)"
    - "Zero regression on full test suite"
  artifacts:
    - path: main.py
      provides: "--ai-preview CLI flag + xg_overrides wiring + AI preview display call"
      contains: "ai_preview|xg_overrides|print_ai_previews"
    - path: tests/test_cli.py
      provides: "--ai-preview flag parse test"
      contains: "ai_preview"
    - path: tests/test_main_loop.py
      provides: "Integration test for --ai-preview flag"
      contains: "ai_preview"
    - path: tests/test_group_integration.py
      provides: "xG override simulation integration test"
      contains: "xg_overrides"
  key_links:
    - from: "_parse_args()"
      to: "--ai-preview flag"
      via: "argparse"
      pattern: "ai_preview"
    - from: "_run_iteration()"
      to: "cb_cache matches"
      via: "xg_overrides dict build"
      pattern: "xg_overrides"
    - from: "xg_overrides dict"
      to: "run_full_simulation()"
      via: "keyword argument"
      pattern: "xg_overrides"
    - from: "args.ai_preview"
      to: "output.print_ai_previews()"
      via: "conditional call"
      pattern: "ai_preview"
---

<objective>
**CLI Wiring & Integration Tests**

**Purpose:** Wire together the xG extraction (Plan 18-01) and AI preview enrichment (Plan 18-02) into `main.py` — add `--ai-preview` CLI flag, build `xg_overrides` dict from CatBoost cache, pass it through the simulation chain, and trigger AI preview display. Add CLI tests and integration tests.

**Depends on:** Plan 18-01 (xG extraction + lambda override) and Plan 18-02 (AI preview extraction + display).

**Output:** Modified `main.py` (~25 lines changed), `test_cli.py`, `test_main_loop.py`, `test_group_integration.py`.
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase context (locked decisions)
@.planning/phases/18-xg-ai-prediction-signals/18-CONTEXT.md
@.planning/phases/18-xg-ai-prediction-signals/18-RESEARCH.md
@.planning/phases/18-xg-ai-prediction-signals/18-PATTERNS.md

# Source files to modify (read before editing)
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/main.py
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/tests/test_cli.py
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/tests/test_main_loop.py
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/tests/test_group_integration.py

# Plan 18-01 output (xG functions created)
@.planning/phases/18-xg-ai-prediction-signals/18-01-SUMMARY.md

# Plan 18-02 output (AI preview functions created)
@.planning/phases/18-xg-ai-prediction-signals/18-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI flag + xG wiring + AI preview call in main.py</name>
  <files>main.py</files>
  <read_first>
    @C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/main.py

    Read the full file. Focus on:
    - Lines 7-31: Import block — output is already imported
    - Lines 191-228: _parse_args() — add --ai-preview flag alongside existing --once/--no-color/--seed
    - Lines 689-700: CatBoost cache load + refresh section (where xg_overrides dict will be built)
    - Lines 702-705: Form signal section — xg_overrides block goes AFTER catboost block, BEFORE form section
    - Line 858: run_full_simulation() call (where xg_overrides is passed)
    - Lines 868-870: Output section after probability table (where AI preview call goes — after delta summary, before return)
    - Line 574: hourly re-sim path calls run_full_simulation() without xg_overrides — correct per D-04 (default=None uses Elo)
  </read_first>
  <action>
    Wire Phase 18 features into main.py at four modification points:

    **Point 1 — `_parse_args()` (~line 221-227):**
    Add BEFORE `return parser.parse_args(argv)` (line 228):
    ```
    parser.add_argument(
        "--ai-preview",
        action="store_true",
        dest="ai_preview",
        help="Display AI-powered pre-match analysis for played matches",
    )
    ```

    **Point 2 — xG overrides dict build (after catboost cache block, ~after line 700):**
    The catboost cache block ends at line 700 or so (the `save_signal_cache` call). AFTER this block, BEFORE the form signal computation comment (line 702), ADD:
    ```
    # ── Phase 18: Build xG overrides dict from CatBoost cache (D-02, D-06) ──
    xg_overrides: dict[str, tuple[float, float]] | None = None
    cb_matches = cb_cache.get("matches", {}) if cb_cache else {}
    for mid, entry in cb_matches.items():
        home_xg = entry.get("expected_home_goals")
        away_xg = entry.get("expected_away_goals")
        if home_xg is not None and away_xg is not None:
            if xg_overrides is None:
                xg_overrides = {}
            xg_overrides[mid] = (home_xg, away_xg)
    ```

    **Point 3 — Pass xg_overrides to run_full_simulation() (modify line 858):**
    Change from:
    ```
    probs = run_full_simulation(teams, groups, bracket, annex_c, played, played_groups=played_groups, iterations=50000, seed=seed, blend_params=blend_params)
    ```
    To:
    ```
    probs = run_full_simulation(teams, groups, bracket, annex_c, played, played_groups=played_groups, iterations=50000, seed=seed, blend_params=blend_params, xg_overrides=xg_overrides)
    ```

    **Point 4 — AI preview display after probability table (after ~line 870):**
    AFTER `output.print_delta_summary(probs, prev_probs)` (which ends around line 870), BEFORE `return` (line 872), ADD:
    ```
    # Phase 18: AI preview display (D-09)
    if args and getattr(args, 'ai_preview', False):
        output.print_ai_previews(played, played_groups)
    ```

    IMPORTANT: Pass `args` to `_run_iteration()`. Check if args is already a parameter. If args is not passed to _run_iteration, you need to add it as a parameter OR use a different approach.

    Wait — check if `_run_iteration()` already has access to args. Looking at the call sites... `_run_iteration()` is called from `main()` around line 1000+. The args namespace is created in `main()`. 

    The cleanest approach: `_run_iteration()` does NOT currently receive args. Since `_run_iteration()` has many parameters already, use the `NO_COLOR` module-level pattern from output.py instead. Add a module-level variable `_ai_preview_enabled = False` in main.py, set it in `main()` after parse_args, and read it in `_run_iteration()`:

    - Add `_ai_preview_enabled: bool = False` after line 36 (`_last_gov_time`)
    - In `main()` after `args = _parse_args()` (~line 908), add: `global _ai_preview_enabled; _ai_preview_enabled = args.ai_preview`
    - In `_run_iteration()`, check `if _ai_preview_enabled:` before calling output.print_ai_previews()
  </action>
  <verify>
    <automated>python -c "from main import _parse_args; args = _parse_args(['--ai-preview']); assert args.ai_preview == True; args = _parse_args([]); assert args.ai_preview == False; print('--ai-preview flag parse OK')"</automated>
    <automated>python -c "import main; assert hasattr(main, '_ai_preview_enabled'); assert main._ai_preview_enabled == False; print('_ai_preview_enabled module var exists')"</automated>
  </verify>
  <done>
    --ai-preview CLI flag parsed, xg_overrides built from catboost cache, passed through simulation, AI preview display triggered when flag is True.
  </done>
  <acceptance_criteria>
    - _parse_args(['--ai-preview']) returns Namespace with ai_preview=True
    - _parse_args([]) returns Namespace with ai_preview=False (default)
    - _ai_preview_enabled module variable exists in main.py
    - xg_overrides dict built from cb_cache entries with expected_home_goals and expected_away_goals
    - xg_overrides passed to run_full_simulation() call
    - _compute_group_display path does NOT receive xg_overrides (correct default behavior)
    - AI preview display is triggered when _ai_preview_enabled is True
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CLI flag test + integration tests</name>
  <files>tests/test_cli.py, tests/test_main_loop.py, tests/test_group_integration.py</files>
  <read_first>
    @C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/tests/test_cli.py (lines 1-60)
    @C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/tests/test_main_loop.py (lines 1-100)
    @C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/worldcup_predictor/tests/test_group_integration.py (lines 1-100)

    Read all three. Key patterns:
    - test_cli.py: TestParseArgs class, direct _parse_args() calls
    - test_main_loop.py: _runner_code_with_flag() pattern for subprocess tests
    - test_group_integration.py: group_a_fixture + elo_ratings + teams_dict fixtures
  </read_first>
  <action>
    Add tests at three locations:

    A — tests/test_cli.py — Add to TestParseArgs class:

    Add method `test_ai_preview_flag(self)`:
    ```python
    def test_ai_preview_flag(self):
        """--ai-preview flag sets ai_preview=True."""
        args = _parse_args(["--ai-preview"])
        assert args.ai_preview is True
    ```

    Add method `test_ai_preview_default_false(self)`:
    ```python
    def test_ai_preview_default_false(self):
        """Without --ai-preview, ai_preview defaults to False."""
        args = _parse_args([])
        assert args.ai_preview is False
    ```

    B — tests/test_group_integration.py — Add xG override integration test:

    Add a new test function `test_xg_override_wiring()`:
    - Use existing fixtures: `group_a_fixture`, `elo_ratings`, `teams_dict`
    - Create xg_overrides = {"GS_A_01": (3.5, 0.5)}  # override lambdas for Mexico vs South Africa
    - Call `matchup_lambdas = precompute_matchup_lambdas(group_a_fixture, elo_ratings, xg_overrides=xg_overrides)`
    - Assert that `matchup_lambdas["GS_A_01"]` equals `(3.5, 0.5)` — deterministic wiring verification
    - Assert that xG overrides propagate into the returned lambdas dict without running simulation
    - Import precompute_matchup_lambdas if not already imported at the top (it is already on line 19)

    C — tests/test_main_loop.py — Add --ai-preview flag integration test:

    Follow the existing `_runner_code_with_flag` pattern. Add a new test `test_ai_preview_flag_does_not_crash()`:
    - Use the same subprocess runner pattern with mocked API
    - Pass `--ai-preview` flag
    - Assert the process completes without error
    - This test verifies the flag doesn't crash the pipeline
  </action>
  <verify>
    <automated>pytest tests/test_cli.py::TestParseArgs::test_ai_preview_flag tests/test_cli.py::TestParseArgs::test_ai_preview_default_false -x -q -v</automated>
    <automated>pytest tests/test_group_integration.py -x -q -k "xg_override_wiring" -v</automated>
    <automated>pytest tests/test_main_loop.py -x -q -k "ai_preview" -v</automated>
  </verify>
  <done>
    CLI arg tests, xG integration test, and --ai-preview integration test all pass. Zero regressions.
  </done>
  <acceptance_criteria>
    - test_ai_preview_flag: _parse_args(["--ai-preview"]).ai_preview == True
    - test_ai_preview_default_false: _parse_args([]).ai_preview == False
    - test_xg_override_wiring: xG overrides appear in matchup_lambdas dict
    - test_ai_preview_flag_does_not_crash: --ai-preview flag doesn't crash pipeline
    - All existing tests in test_cli.py, test_main_loop.py, test_group_integration.py pass
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> main.py | --ai-preview flag parsed by argparse |
| CatBoost cache -> simulation | xg_overrides dict crosses from cache into simulation chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Tampering | xg_overrides dict construction | mitigate | Values come from cb_cache which is populated by parse_catboost_response() (trusted BSD API source, type-checked). |
| T-18-07 | Denial of Service | xg_overrides with bad value | mitigate | Floating-point values are used as-is (Poisson lambdas accept any non-negative float). CatBoost cache entries are already type-checked in extraction. |
| T-18-SC | Tampering | cargo-cult dependency risk | mitigate | No new packages. All changes use existing imports (argparse, typing). |
</threat_model>

<verification>
- `pytest tests/test_cli.py -x -q -k "ai_preview" -v` — CLI flag tests pass
- `pytest tests/test_group_integration.py -x -q -k "xg" -v` — xG integration test passes
- `pytest tests/test_main_loop.py -x -q -k "ai_preview" -v` — AI preview integration test passes
- `pytest -x --tb=short -q` — full suite zero regression
</verification>

<success_criteria>
- [x] --ai-preview CLI flag added to _parse_args()
- [x] _ai_preview_enabled module variable + wiring in main()
- [x] xg_overrides dict built from cb_cache entries with xG data
- [x] xg_overrides passed to run_full_simulation()
- [x] AI preview display triggered when _ai_preview_enabled is True
- [x] 2 CLI flag tests in test_cli.py
- [x] 1 xG wiring test in test_group_integration.py
- [x] 1 AI preview integration test in test_main_loop.py
- [x] Full test suite passes with zero regressions
</success_criteria>

<output>
Create `.planning/phases/18-xg-ai-prediction-signals/18-03-SUMMARY.md` when done
</output>
