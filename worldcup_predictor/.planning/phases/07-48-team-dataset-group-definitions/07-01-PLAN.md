---
phase: 07-48-team-dataset-group-definitions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - worldcup_predictor/src/constants.py
  - worldcup_predictor/data/teams.json
autonomous: true
requirements:
  - DATA2-01

must_haves:
  truths:
    - "Constants.GROUP_COUNT, TEAMS_PER_GROUP, MATCHES_PER_GROUP, ANNEX_C_ENTRIES, ANNEX_C_WINNER_GROUPS are importable and have correct values"
    - "data/teams.json contains exactly 48 teams, each with an elo rating"
    - "data/teams.json team names match the canonical names consumed by main.py"
  artifacts:
    - path: "worldcup_predictor/src/constants.py"
      provides: "Group and Annex C dimension constants"
      contains: "GROUP_COUNT, TEAMS_PER_GROUP, MATCHES_PER_GROUP, ANNEX_C_ENTRIES, ANNEX_C_WINNER_GROUPS"
    - path: "worldcup_predictor/data/teams.json"
      provides: "48-team roster with Elo ratings"
      min_entries: 48
  key_links:
    - from: "worldcup_predictor/src/constants.py"
      to: "worldcup_predictor/data/teams.json"
      via: "DATA_DIR path in constants.py"
      pattern: "DATA_DIR.*data"
---

<objective>
**Extend constants.py with group/Annex C dimensions and teams.json from 32 to 48 teams**

Purpose: Provide the foundation constants and team roster that all other Phase 7 data files (groups.json, annex_c.json, team_aliases.json) depend on. The 16 new teams must match the official FIFA 2026 qualified nations list and receive researched Elo ratings.

Output:
- `worldcup_predictor/src/constants.py` — 5 new integer/tuple constants appended at end of file
- `worldcup_predictor/data/teams.json` — 48 entries (existing 32 preserved, 16 new appended)
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-48-team-dataset-group-definitions/07-PATTERNS.md

# READ FIRST — existing code
@worldcup_predictor/src/constants.py
@worldcup_predictor/data/teams.json
@worldcup_predictor/data/bracket.json

<interfaces>
From `worldcup_predictor/src/constants.py` (current state — 22 lines, 6 exported symbols):
- `K_FACTOR: int = 60`
- `DEFAULT_ELO: int = 1500`
- `DATA_DIR: Path = .../data`
- `API_URL: str = ...`
- `API_TIMEOUT: int = 10`
- `POLL_INTERVAL: int = 60`

From `worldcup_predictor/data/teams.json` (current state — 32 entries):
- Object keyed by canonical team name string
- Each value is `{"elo": <int>}`
- The 32 existing teams are the base; 16 more must be inserted — NOT a replacement
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add group/Annex C dimension constants to constants.py</name>
  <files>worldcup_predictor/src/constants.py</files>
  <read_first>
    @worldcup_predictor/src/constants.py
  </read_first>
  <action>
    Append five new constants after the existing `POLL_INTERVAL` line (line 22), following the same docstring-per-constant pattern. Do NOT modify any existing lines.

    Values:
    - `GROUP_COUNT: int = 12` — docstring: "Number of groups in the 48-team format (A–L)."
    - `TEAMS_PER_GROUP: int = 4` — docstring: "Number of teams in each group."
    - `MATCHES_PER_GROUP: int = 6` — docstring: "Number of round-robin matches per group (n*(n-1)/2 for n=4)."
    - `ANNEX_C_ENTRIES: int = 495` — docstring: "Number of entries in Annex C third-place lookup table = C(12,8)."
    - `ANNEX_C_WINNER_GROUPS: tuple[str, ...] = ("A", "B", "D", "E", "G", "I", "K", "L")` — docstring: "Group winners that host third-place teams in R32 (derivable from Annex C structure; hardcoded for convenience)."

    Import `Path` is already present at line 4. No new imports needed.
  </action>
  <acceptance_criteria>
    - `python -c "from src.constants import GROUP_COUNT, TEAMS_PER_GROUP, MATCHES_PER_GROUP, ANNEX_C_ENTRIES, ANNEX_C_WINNER_GROUPS; print(GROUP_COUNT, TEAMS_PER_GROUP, MATCHES_PER_GROUP, ANNEX_C_ENTRIES, ANNEX_C_WINNER_GROUPS)"` prints `12 4 6 495 ('A', 'B', 'D', 'E', 'G', 'I', 'K', 'L')`
    - No SyntaxError or ImportError
  </acceptance_criteria>
  <verify>
    <automated>cd worldcup_predictor && python -c "from src.constants import GROUP_COUNT, TEAMS_PER_GROUP, MATCHES_PER_GROUP, ANNEX_C_ENTRIES, ANNEX_C_WINNER_GROUPS; assert GROUP_COUNT == 12; assert TEAMS_PER_GROUP == 4; assert MATCHES_PER_GROUP == 6; assert ANNEX_C_ENTRIES == 495; assert ANNEX_C_WINNER_GROUPS == ('A', 'B', 'D', 'E', 'G', 'I', 'K', 'L')"</automated>
  </verify>
  <done>All 5 constants importable and match their expected values</done>
</task>

<task type="auto">
  <name>Task 2: Extend teams.json from 32 to 48 teams with researched Elo ratings</name>
  <files>worldcup_predictor/data/teams.json</files>
  <read_first>
    @worldcup_predictor/data/teams.json
    @worldcup_predictor/data/team_aliases.json
  </read_first>
  <action>
    The current `teams.json` has 32 entries. Append 16 additional teams to produce exactly 48 entries.

    Team selection rules (in priority order):
    1. MUST match the 48 official qualified teams for the 2026 FIFA World Cup (hosts: USA, Canada, Mexico, plus 45 qualifiers)
    2. All 32 existing teams that ARE qualified for 2026 must be preserved with their current Elo ratings
    3. Any existing team that did NOT qualify for 2026 MUST be removed and replaced with a qualified team
    4. Elo ratings for the 16 new teams: use the formula `Elo = 1500 + (32 - FIFA_rank) * 4` (per RESEARCH.md Gaps to Address #3), cross-referenced against worldfootballrating.com or equivalent for the most recent available ratings
    5. Each entry follows the exact pattern: `"Canonical Name": {"elo": <int>}` — indent 2 spaces, alphabetical ordering within the file

    Key constraints:
    - Every key in the resulting `teams.json` MUST be a canonical name that matches the `key` in `team_aliases.json` (once aliases are extended in Plan 2) and references used in `groups.json` (once groups are created in Plan 2)
    - The canonical names MUST match the naming convention used by the BSD API after alias resolution
    - Do NOT add `"group"` or any other key inside team values — the existing `{"elo": <int>}` structure must be preserved
    - Preserve existing Elo ratings for the 32 current teams — do NOT re-research or change them
    - **Canonical name resolution:** The current `teams.json` uses `"USA"` while `team_aliases.json` uses `"United States"` as its canonical key. Resolve this by renaming `"USA"` to `"United States"` in `teams.json` to match the `team_aliases.json` convention. This ensures `set(teams.keys()) == set(team_aliases.keys())` consistency. If `"USA"` is referenced in brackets, it will be replaced in Plan 2 by `groups.json`. (If any BSD API alias entry uses "USA" as an alias, preserve it in team_aliases.json's alias list for "United States".)
  </action>
  <acceptance_criteria>
    - `python -c "import json; d=json.load(open('worldcup_predictor/data/teams.json')); assert len(d)==48; assert all(isinstance(v,dict) and 'elo' in v and isinstance(v['elo'],int) for v in d.values())"`
    - Every existing team from the original 32 that qualified for 2026 is still present with unchanged `elo` value
    - Remove any non-qualifying teams from the existing 32 if they didn't qualify for 2026
    - Total count is exactly 48
  </acceptance_criteria>
  <verify>
    <automated>cd worldcup_predictor && python -c "
import json
with open('data/teams.json') as f:
    t = json.load(f)
assert len(t) == 48, f'Expected 48 teams, got {len(t)}'
for name, data in t.items():
    assert 'elo' in data, f'{name} missing elo'
    assert isinstance(data['elo'], int), f'{name} elo not int'
print(f'OK: {len(t)} teams validated')
"</automated>
  </verify>
  <done>teams.json loads as dict with exactly 48 keys, each value has int `elo` key, non-qualifying teams removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/teams.json on disk | Read by state.py at startup, never written during runtime (Elo updates go through state.save_teams() which overwrites the file, but the initial json is the seed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | teams.json on disk | mitigate | Elo values are loaded via json.load() not eval; no injection risk |
| T-07-02 | Tampering | constants.py | accept | Code file under git; tampering would break import, not silently corrupt |
| T-07-SC | Tampering | npm/pip installs | accept | No package installs this phase — pure data + stdlib Python |
</threat_model>

<verification>
1. Run `python -c "from src.constants import GROUP_COUNT"` from worldcup_predictor/ — no error
2. Run the teams.json count and structure check from Task 2 verify
</verification>

<success_criteria>
- constants.py has 5 new constants importable without error
- teams.json has exactly 48 entries, each valid (string key, {"elo": int} value)
- Existing 32 teams preserved where qualified; non-qualifiers removed
</success_criteria>

<output>
Create `.planning/phases/07-48-team-dataset-group-definitions/07-01-SUMMARY.md` when done
</output>
