---
phase: 07-48-team-dataset-group-definitions
plan: 04
type: execute
wave: 3
depends_on:
  - 07-02
  - 07-03
files_modified:
  - worldcup_predictor/tests/test_state.py
autonomous: true
requirements:
  - DATA2-05
  - DATA2-06

must_haves:
  truths:
    - "validate_groups() correctly validates production groups.json and rejects known-bad data"
    - "validate_annex_c() correctly validates production annex_c.json and rejects known-bad data"
    - "load_groups() and load_annex_c() integrate with existing load patterns"
    - "All existing tests continue to pass after additions"
  artifacts:
    - path: "worldcup_predictor/tests/test_state.py"
      provides: "Tests for validate_groups, load_groups, validate_annex_c, load_annex_c"
      min_lines: 100
      contains: "test_valid_groups_passes, test_validate_groups_wrong_count, test_validate_groups_duplicate_team, test_annex_c_valid_passes, test_annex_c_wrong_count"
  key_links:
    - from: "test_state.py"
      to: "src/state.py"
      via: "import from src.state"
      pattern: "from src.state import"
---

<objective>
**Add comprehensive tests for the 4 new functions in state.py (validate_groups, load_groups, validate_annex_c, load_annex_c) and verify the production data files pass validation**

Purpose: Ensure the data validation functions correctly reject malformed data and that the production data files created in Plan 2 pass all checks. This prevents Annex C lookup failures (Pitfall 2) and group data corruption from reaching simulation.

Output:
- `worldcup_predictor/tests/test_state.py` — extended with test functions for new validators
- Verified: production groups.json and annex_c.json pass their respective validators
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-48-team-dataset-group-definitions/07-PATTERNS.md

# READ FIRST — existing test patterns + load/validate implementations
@worldcup_predictor/tests/test_state.py
@worldcup_predictor/src/state.py
@worldcup_predictor/src/constants.py

# Production data files to validate against
@worldcup_predictor/data/groups.json
@worldcup_predictor/data/annex_c.json
@worldcup_predictor/data/teams.json

<interfaces>
From `worldcup_predictor/tests/test_state.py` (current, 303 lines):
- Uses `tmp_path` fixture for temp file I/O (pytest built-in)
- Uses `sample_teams`, `sample_bracket`, `sample_played` fixtures from conftest.py
- Test naming: `test_valid_bracket_passes`, `test_duplicate_match_id`, `test_missing_source_match`, etc.
- Validation tests use `with pytest.raises(ValueError, match="...")` pattern
- Load tests use `tmp_path` to create temp JSON files and load from custom `data_dir`
- Imports: `json`, `os`, `subprocess`, `sys`, `Path` from pathlib, `pytest`
- Imports from src.state: `load_bracket`, `load_played`, `load_teams`, `save_bracket`, `save_played`, `save_teams`, `validate_bracket`

Conftest fixtures available:
- `sample_teams` — dict of 5 teams with elo ratings
- `sample_bracket` — list of 7 match dicts with valid DAG structure
- `sample_played` — empty dict

Current test categories (lines 25-303):
- validate_bracket success/error cases (lines 25-64)
- load_teams tests (lines 70-87)
- load_bracket test (lines 93-101)
- Save/persistence tests (lines 107-303)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validate_groups and load_groups tests to test_state.py</name>
  <files>worldcup_predictor/tests/test_state.py</files>
  <read_first>
    @worldcup_predictor/tests/test_state.py
    @worldcup_predictor/src/state.py
    @worldcup_predictor/data/groups.json
    @worldcup_predictor/data/teams.json
  </read_first>
  <behavior>
    - test_valid_groups_passes: a valid 12-group (A-L) structure with 4 teams and 6 matches each passes without exception
    - test_valid_groups_wrong_group_count: structure with 11 (or 13) groups raises ValueError
    - test_valid_groups_wrong_team_count: any group with <4 or >4 teams raises ValueError
    - test_valid_groups_wrong_match_count: any group with <6 or >6 matches raises ValueError
    - test_valid_groups_duplicate_team: same team appearing in 2+ groups raises ValueError
    - test_valid_groups_duplicate_match_id: duplicate match_id across groups raises ValueError
    - test_valid_groups_invalid_match_id: match_id not matching GS_{group}_NN pattern raises ValueError
    - test_valid_groups_team_not_in_teams: team name not in teams dict raises ValueError
    - test_load_groups_success: load from tmp_path returns dict with "groups" key
    - test_load_groups_file_not_found: missing groups.json raises FileNotFoundError
    - test_load_groups_invalid_json: corrupt JSON raises json.JSONDecodeError
    - test_load_groups_validation_error: invalid data raises ValueError
  </behavior>
  <action>
    Append test classes/functions to `worldcup_predictor/tests/test_state.py` after the last existing test function (around line 303). Follow the existing test patterns exactly:

    - Use `from src.state import validate_groups, load_groups` at the top of the file (add to the existing import block at lines 14-22)
    - Use `tmp_path` fixture for file I/O tests
    - Use `with pytest.raises(ValueError, match="...")` for validation error tests
    - Use `with pytest.raises(FileNotFoundError)` and `with pytest.raises(json.JSONDecodeError)` for load error tests

    Helper function to create a valid 12-group dict (for test data):
    ```python
    def _make_valid_groups(teams_override=None):
        groups = {}
        all_teams = teams_override or [f"Team_{i}" for i in range(48)]
        for idx, letter in enumerate("ABCDEFGHIJKL"):
            team_slice = all_teams[idx*4:(idx+1)*4]
            matches = []
            for mnum in range(1, 7):
                matches.append({
                    "match_id": f"GS_{letter}_{mnum:02d}",
                    "team_a": team_slice[0],
                    "team_b": team_slice[1] if mnum == 1 else team_slice[min(mnum, 3)],
                    "winner": None, "score_a": None, "score_b": None,
                })
            groups[letter] = {"teams": list(team_slice), "matches": matches}
        return {"groups": groups}
    ```

    Test functions to add:

    1. `test_valid_groups_passes()` — call `validate_groups(_make_valid_groups())` — should not raise
    2. `test_valid_groups_wrong_group_count()` — pass dict with only 11 groups — verify ValueError raised
    3. `test_valid_groups_wrong_team_count()` — pass dict where one group has 3 teams — verify ValueError
    4. `test_valid_groups_wrong_match_count()` — pass dict where one group has 5 matches — verify ValueError
    5. `test_valid_groups_duplicate_team()` — pass dict where same team appears in 2 groups — verify ValueError
    6. `test_valid_groups_duplicate_match_id()` — pass dict with duplicate match_id — verify ValueError
    7. `test_valid_groups_invalid_match_id()` — pass dict where match_id is `"GS_X_01"` for group A — verify ValueError
    8. `test_valid_groups_team_not_in_teams()` — pass groups + teams where a team in groups is not in teams — verify ValueError
    9. `test_load_groups_success(tmp_path)` — write valid groups.json to tmp_path, call load_groups(data_dir=tmp_path) — verify returns dict with "groups"
    10. `test_load_groups_file_not_found(tmp_path)` — call load_groups(data_dir=tmp_path) without writing file — verify FileNotFoundError
    11. `test_load_groups_invalid_json(tmp_path)` — write invalid JSON to tmp_path/groups.json — verify json.JSONDecodeError
    12. `test_load_groups_validation_error(tmp_path)` — write invalid groups.json — verify ValueError

    Comment header before the new test section:
    ```python
    # ─── validate_groups tests ──────────────────────────────────────────
    ```
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_state.py::test_valid_groups_passes -x -q` passes
    - `python -m pytest tests/test_state.py::test_valid_groups_wrong_group_count -x -q` passes
    - `python -m pytest tests/test_state.py::test_load_groups_success -x -q` passes
    - `python -m pytest tests/test_state.py -x -q` — all tests pass (existing + new)
  </acceptance_criteria>
  <verify>
    <automated>cd worldcup_predictor && python -m pytest tests/test_state.py -x -q</automated>
  </verify>
  <done>All validate_groups and load_groups tests pass, existing tests unaffected</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add validate_annex_c and load_annex_c tests + production data verification</name>
  <files>worldcup_predictor/tests/test_state.py</files>
  <read_first>
    @worldcup_predictor/tests/test_state.py
    @worldcup_predictor/src/state.py
    @worldcup_predictor/data/annex_c.json
    @worldcup_predictor/data/groups.json
    @worldcup_predictor/data/teams.json
  </read_first>
  <behavior>
    - test_annex_c_valid_passes: 495 valid entries created by itertools.combinations pass
    - test_annex_c_wrong_count: dict with <495 entries raises ValueError
    - test_annex_c_unsorted_key: key with unsorted group letters raises ValueError
    - test_annex_c_missing_value_key: entry missing one of 8 keys raises ValueError
    - test_annex_c_self_reference: entry with "1A": "3A" raises ValueError
    - test_annex_c_out_of_key_reference: entry referencing group not in key raises ValueError
    - test_annex_c_invalid_group_letter: key with non-A-L letter raises ValueError
    - test_load_annex_c_success: valid file loads successfully
    - test_load_annex_c_file_not_found: missing file raises FileNotFoundError
    - test_production_groups_validates: real groups.json passes validate_groups(..., teams=teams)
    - test_production_annex_c_validates: real annex_c.json passes validate_annex_c()
  </behavior>
  <action>
    Append more test functions to `worldcup_predictor/tests/test_state.py` after the validate_groups test section (added in Task 1). Follow the same patterns.

    Helper function (place before the annec_c test section):
    ```python
    def _make_valid_annex_c():
        import itertools
        all_groups = list('ABCDEFGHIJKL')
        winner_groups = ['A','B','D','E','G','I','K','L']
        data = {}
        for combo in itertools.combinations(all_groups, 8):
            key = ','.join(combo)
            value = {}
            for i, wg in enumerate(winner_groups):
                value[f'1{wg}'] = f'3{combo[i % 8]}'
            data[key] = value
        return data
    ```

    Test functions to add:

    1. `test_annex_c_valid_passes()` — generate 495 valid entries, call `validate_annex_c()` — should not raise
    2. `test_annex_c_wrong_count()` — pass dict with only 400 entries — verify ValueError
    3. `test_annex_c_unsorted_key()` — pass dict where one key is `"H,A,B,C,D,E,F,G"` — verify ValueError
    4. `test_annex_c_missing_value_key()` — pass valid dict but remove one key from a value — verify ValueError
    5. `test_annex_c_self_reference()` — pass valid dict but set `"1A": "3A"` — verify ValueError
    6. `test_annex_c_out_of_key_reference()` — pass valid dict but set value referencing a group NOT in its key — verify ValueError
    7. `test_annex_c_invalid_group_letter()` — pass dict with key containing "X" — verify ValueError
    8. `test_load_annex_c_success(tmp_path)` — write valid annex_c.json to tmp_path, load — verify returns dict
    9. `test_load_annex_c_file_not_found(tmp_path)` — call without writing file — verify FileNotFoundError

    Comment header:
    ```python
    # ─── validate_annex_c tests ─────────────────────────────────────────
    ```

    **Production data verification tests** (these use the real data files, not fixtures):
    Add these after the unit tests:

    10. `test_production_groups_validates()` — load `data/groups.json` and `data/teams.json`, call `validate_groups(groups, teams=teams)` — should not raise
    11. `test_production_annex_c_validates()` — load `data/annex_c.json`, call `validate_annex_c(annex_c)` — should not raise

    For the production tests, resolve paths relative to the test file location or use `data_dir` parameter pointing to `worldcup_predictor/data/`.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_state.py::test_annex_c_valid_passes -x -q` passes
    - `python -m pytest tests/test_state.py::test_production_groups_validates -x -q` passes (production data)
    - `python -m pytest tests/test_state.py::test_production_annex_c_validates -x -q` passes (production data)
    - `python -m pytest tests/test_state.py -x -q` — all tests pass (existing + all new)
  </acceptance_criteria>
  <verify>
    <automated>cd worldcup_predictor && python -m pytest tests/test_state.py -x -q</automated>
  </verify>
  <done>All validate_annex_c tests pass, production data files validate successfully through their respective validators</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test_state.py (test code) | Tests validate their own data and production files; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | test fixtures | accept | Test data is self-contained; only affects test correctness |
| T-07-SC | Tampering | pip installs | accept | pytest already installed — no new packages |
</threat_model>

<verification>
1. `cd worldcup_predictor && python -m pytest tests/test_state.py -x -v` — all existing + new tests pass
2. Production data validation tests confirm groups.json and annex_c.json are structurally valid
</verification>

<success_criteria>
- All existing tests (pre-Phase 7) pass unchanged (no regressions)
- All new validate_groups tests pass (10+ test cases)
- All new validate_annex_c tests pass (9+ test cases)
- Production data verification tests pass (groups.json + annex_c.json validate successfully)
- Total test count in test_state.py increases by 20+
</success_criteria>

<output>
Create `.planning/phases/07-48-team-dataset-group-definitions/07-04-SUMMARY.md` when done
</output>
