---
phase: 03-ucl-orchestration-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - competitions/ucl/result.py
  - competitions/ucl/main.py
  - competitions/ucl/__init__.py
  - competitions/ucl/tests/conftest.py
  - competitions/ucl/tests/test_cli.py
autonomous: true
requirements:
  - UCLO-01
user_setup: []
must_haves:
  truths:
    - "User can run `python -m competitions.ucl.main --help` and see all 3 flags"
    - "CLI accepts -n/--iterations, -s/--seed, -o/--output flags with correct defaults"
    - "SimulationResult dataclass can be instantiated with all required fields"
    - "display.py fixture `sample_result` exists in conftest.py as a SimulationResult instance"
    - "All argparse unit tests pass for defaults, all-flags, and non-int rejection"
    - "JSON output is valid when --output path is provided"
  artifacts:
    - path: "competitions/ucl/result.py"
      provides: "SimulationResult dataclass contract — display depends on this, never on simulation internals"
      exports: ["SimulationResult"]
    - path: "competitions/ucl/main.py"
      provides: "CLI entry point with _parse_args() and main() calling MC + one bracket iteration"
      contains: "_parse_args, def main"
    - path: "competitions/ucl/__init__.py"
      provides: "Updated exports for result.SimulationResult and main.main"
      contains: "SimulationResult, main"
    - path: "competitions/ucl/tests/conftest.py"
      provides: "sample_result fixture for downstream display tests"
      contains: "def sample_result"
    - path: "competitions/ucl/tests/test_cli.py"
      provides: "Argparse unit tests for all 3 flags"
      contains: "test_defaults, test_all_flags_together, test_seed_rejects_non_int"
  key_links:
    - from: "competitions/ucl/main.py"
      to: "competitions/ucl/result.py"
      via: "import SimulationResult"
      pattern: "from competitions\\.ucl\\.result import"
    - from: "competitions/ucl/main.py"
      to: "competitions/ucl/src/simulation.py"
      via: "calls run_monte_carlo()"
      pattern: "run_monte_carlo"
    - from: "competitions/ucl/main.py"
      to: "competitions/ucl/src/knockout.py"
      via: "calls knockout pipeline functions"
      pattern: "simulate_playoff_round|build_r16_bracket|simulate_knockout_tree|track_knockout_stages"
    - from: "competitions/ucl/__init__.py"
      to: "competitions/ucl/result.py"
      via: "re-exports SimulationResult"
      pattern: "SimulationResult"
    - from: "competitions/ucl/tests/conftest.py"
      to: "competitions/ucl/result.py"
      via: "constructs SimulationResult for sample_result fixture"
      pattern: "SimulationResult("
---

<objective>
**Phase Goal — As a** user, **I want to** run UCL simulations from the CLI with configurable parameters, **so that** I can generate predictions with specific iteration counts and seeds.

Create the `SimulationResult` dataclass contract (the architectural spine of Phase 3) and the `ucl-predict` CLI entry point (`main.py`). This plan establishes the three-layer architecture: CLI orchestration → `SimulationResult` contract → display (display layer comes in plans 02 and 03). The CLI runs Monte Carlo simulation, runs one representative bracket iteration, assembles a `SimulationResult`, prints a basic summary, and optionally writes JSON.

Per D-15/D-16: The `SimulationResult` dataclass is owned by orchestration (Phase 3), not by simulation engine or BSD. Per D-17: Display layer will never import simulation internals — only `result.py`.

Purpose: Deliver requirement UCLO-01 — CLI entry point with configurable iterations and seed.
Output: `result.py`, `main.py`, updated `__init__.py`, `conftest.py` fixture, `test_cli.py`
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ucl-orchestration-display/03-CONTEXT.md
@.planning/phases/03-ucl-orchestration-display/03-RESEARCH.md
@competitions/ucl/src/simulation.py
@competitions/ucl/src/knockout.py
@competitions/ucl/src/__init__.py
@competitions/ucl/tests/conftest.py
@competitions/euro/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SimulationResult dataclass + conftest fixture + update __init__.py exports</name>

  <files>
    competitions/ucl/result.py (NEW)
    competitions/ucl/__init__.py (MODIFY)
    competitions/ucl/tests/conftest.py (MODIFY)
  </files>

  <read_first>
    competitions/ucl/src/simulation.py lines 312-321 (run_monte_carlo return schema — shows teams dict shape, stage_order)
    competitions/ucl/src/knockout.py lines 365-369 (simulate_playoff_round return — winners dict + ties dict)
    competitions/ucl/src/knockout.py lines 716-721 (simulate_knockout_tree return — rounds dict + champion)
    competitions/ucl/src/knockout.py lines 724-780 (track_knockout_stages return — {team_name: stage_string})
    competitions/ucl/src/__init__.py lines 1-41 (current exports — last line is __all__ list, first export line is at line 3)
    competitions/ucl/tests/conftest.py lines 567-619 (sample_mc_output fixture — model for sample_result fixture)
    competitions/ucl/tests/conftest.py lines 1-8 (existing imports pattern)
    competitions/ucl/tests/conftest.py lines 28 (ALL_36_TEAMS list)
    .planning/phases/03-ucl-orchestration-display/03-RESEARCH.md lines 187-226 (recommended SimulationResult schema)
  </read_first>

  <acceptance_criteria>
    - `result.py` exists at `competitions/ucl/result.py` and exports `SimulationResult`
    - `from competitions.ucl.result import SimulationResult` succeeds
    - `SimulationResult` fields include: `snapshot_date: str`, `n_iterations: int`, `seed: int`, `standings: list[dict]`, `teams: dict[str, dict]`, `playoff_ties: dict[int, dict]`, `playoff_winners: dict[int, str]`, `bracket_rounds: dict[str, list[dict]]`, `bracket_champion: str | None`, `stages: dict[str, str]`, `stage_order: list[str]` (with default_factory of stage list). May grow with backward-compatible fields in future phases — no exact field count is asserted.
    - `__init__.py` adds `result` to imports and includes `SimulationResult`, `main` in `__all__`
    - `conftest.py` has `sample_result` fixture that returns a populated `SimulationResult` instance with all 36 teams, 8 playoff ties, 4 bracket rounds, realistic probabilities, and all 36 stage entries
    - `python -m pytest competitions/ucl/tests/ -x --collect-only` succeeds (fixture discovery)
    - `grep -n "from competitions.ucl.src" competitions/ucl/result.py` returns empty (D-17 compliance: no sim imports in contract layer)
  </acceptance_criteria>

  <action>
    Create `competitions/ucl/result.py` with a `@dataclass` (frozen=True) `SimulationResult` matching the recommended schema from RESEARCH.md lines 187-226. Fields: `snapshot_date` (str), `n_iterations` (int), `seed` (int), `standings` (list[dict]), `teams` (dict[str, dict]), `playoff_ties` (dict[int, dict]), `playoff_winners` (dict[int, str]), `bracket_rounds` (dict[str, list[dict]]), `bracket_champion` (str | None), `stages` (dict[str, str]), `stage_order` (list[str] with default_factory returning the 7-element STAGE_ORDER list). Add `from __future__ import annotations` at top. Do NOT add any methods — pure data container only.

    Modify `competitions/ucl/__init__.py`: Add `from competitions.ucl import result` import and add `"result"`, and the re-exported name `"SimulationResult"` to `__all__`. Maintain the existing sorted-by-module import order.

    Add `sample_result` fixture to `competitions/ucl/tests/conftest.py`: Build a `SimulationResult` using pre-existing fixture data and inline mock data. Use `_ALL_36_TEAMS` and `_ELO_RATINGS` from conftest module scope for team names. Build:
    - `standings`: 36 entries sorted by descending Elo, positions 1-36 with realistic `pts`, `gd`, `gs` values (Man City: pts=18, gd=+10, gs=14; bottom team: pts=2, gd=-8, gs=3), zone assignments per position (1-8="top_8", 9-24="playoff", 25-36="eliminated")
    - `teams`: All 36 teams with mock probabilities (top teams have high top_8_prob ~0.85, low teams ~0.01; champion_prob scaled accordingly)
    - `playoff_ties`: 8 ties with mock `simulate_two_legged_tie()` result dicts (winner, aggregate_a, aggregate_b, leg1/leg2, et_played=False, penalties_played=False)
    - `playoff_winners`: {1: first_playoff_winner, ..., 8: last_playoff_winner} — use actual team names
    - `bracket_rounds`: dict with keys "R16", "QF", "SF", "FINAL", each a list of match dicts with team_a, team_b, winner, result. R16=8 matches, QF=4, SF=2, FINAL=1
    - `bracket_champion`: "Man City" (or whichever team wins the mock bracket)
    - `stages`: all 36 teams mapped to stage strings (using sample_knockout_stage_result pattern for realism)
    - Add `stage_order` at end of fixture (eliminated→playoff→r16→qf→sf→final→champion)

    Use `dataclasses.field(default_factory=...)` for `stage_order` in the dataclass definition.
  </action>

  <verify>
    <automated>python -c "from competitions.ucl.result import SimulationResult; print('OK')"</automated>
    <automated>python -c "from competitions.ucl import SimulationResult; print('OK')"</automated>
    <automated>python -c "
from competitions.ucl.result import SimulationResult
r = SimulationResult(
    snapshot_date='2026-06-28', n_iterations=10000, seed=42,
    standings=[], teams={}, playoff_ties={}, playoff_winners={},
    bracket_rounds={}, bracket_champion=None, stages={}
)
assert r.snapshot_date == '2026-06-28'
assert r.stage_order == ['eliminated','playoff','r16','qf','sf','final','champion']
print('Instantiation OK')
"</automated>
    <automated>python -m pytest competitions/ucl/tests/ -x --collect-only -q | grep "sample_result"</automated>
    <automated>python -c "
import ast, os, sys
# result.py import check (display.py may not exist yet in this plan)
result_path = 'competitions/ucl/result.py'
if os.path.exists(result_path):
    with open(result_path) as f:
        tree = ast.parse(f.read())
    for node in ast.walk(tree):
        if isinstance(node, ast.ImportFrom) and node.module and 'competitions' in node.module:
            print(f'ERROR: result.py imports from {node.module}')
            sys.exit(1)
    print('result.py: no competition imports — OK')
else:
    print('result.py not found yet — skip import check')
"</automated>
  </verify>

  <done>
    SimulationResult dataclass compiles and instantiates; conftest.py has sample_result fixture producing a 36-team result; __init__.py re-exports both.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create main.py CLI entry point with _parse_args and main()</name>

  <files>
    competitions/ucl/main.py (NEW)
  </files>

  <read_first>
    competitions/ucl/src/simulation.py lines 55-102 (simulate_league_phase signature and return)
    competitions/ucl/src/simulation.py lines 184-321 (run_monte_carlo signature and return)
    competitions/ucl/src/knockout.py lines 215-369 (simulate_playoff_round signature)
    competitions/ucl/src/knockout.py lines 469-598 (build_r16_bracket signature)
    competitions/ucl/src/knockout.py lines 601-721 (simulate_knockout_tree signature)
    competitions/ucl/src/knockout.py lines 724-780 (track_knockout_stages signature)
    competitions/ucl/src/__init__.py (all exports available for import)
    competitions/ucl/result.py (just created — SimulationResult fields)
    competitions/euro/main.py lines 36-45 (_parse_args pattern)
    .planning/phases/03-ucl-orchestration-display/03-CONTEXT.md lines 18-33 (D-01 through D-05: flag names, defaults, single-run)
    .planning/phases/03-ucl-orchestration-display/03-CONTEXT.md lines 46-53 (D-14: dual output behavior)
    .planning/phases/03-ucl-orchestration-display/03-RESEARCH.md lines 248-299 (recommended CLI flow, Pitfall 2 about one extra iteration, Pitfall 5 against FileType)
  </read_first>

  <acceptance_criteria>
    - `_parse_args([])` returns `Namespace(iterations=10000, seed=None, output=None)`
    - `_parse_args(["-n", "5000", "-s", "42", "-o", "out.json"])` returns Namespace with iterations=5000, seed=42, output="out.json"
    - `_parse_args(["--seed", "abc"])` raises `SystemExit` (argparse type=int rejection)
    - `python -m competitions.ucl.main --help` shows all 3 flags with descriptions
    - `main()` can be called and completes without error (runs MC integration)
    - When `--output` is specified, a JSON file is written containing valid JSON with `snapshot_date`, `n_iterations`, `seed`, `teams`, `standings`, `playoff_ties`, `playoff_winners`, `bracket_rounds`, `bracket_champion`, `stages` keys
    - Stdout is always produced regardless of `--output` flag (D-14)
    - All imports from `competitions.ucl.src` are in `main.py` only — not in `result.py` or any future `display.py` (D-17 enforcement boundary)
  </acceptance_criteria>

  <action>
    Create `competitions/ucl/main.py` with:

    **Imports:**
    - stdlib: `argparse`, `json`, `os`, `random`, `sys`, `dataclasses.asdict`
    - From `competitions.ucl.src`: `run_monte_carlo`, `simulate_league_phase`, `simulate_playoff_round`, `build_r16_bracket`, `simulate_knockout_tree`, `track_knockout_stages`, `fetch_team_elos`
    - From `competitions.ucl.result`: `SimulationResult`
    - `from __future__ import annotations` at module top

    **`_parse_args(argv=None) -> argparse.Namespace`:**
    Per D-02/D-03/D-04/D-05 and RESEARCH.md lines 355-373:
    - `prog="ucl-predict"`, description focusing on UCL 2025/26 Monte Carlo predictor
    - `-n`/`--iterations`: type=int, default=10000, metavar="N"
    - `-s`/`--seed`: type=int, default=None, metavar="N"
    - `-o`/`--output`: type=str, default=None, metavar="FILE" (NOT `argparse.FileType` per Pitfall 5)
    - Return `parser.parse_args(argv)`

    **`build_simulation_result(fixtures, elo_ratings, seed, n_iterations) -> SimulationResult`:**
    Orchestration helper that separates result assembly from CLI logic:
    1. Call `run_monte_carlo(fixtures, elo_ratings=elo_ratings, n_iterations=n_iterations, seed=seed)` — elo_ratings passed explicitly so run_monte_carlo skips internal fetch
    2. Run one representative bracket iteration: create `rng = random.Random(seed)`, call `simulate_league_phase(fixtures, elo_ratings, rng)`, then `simulate_playoff_round()`, `build_r16_bracket()`, `simulate_knockout_tree()`, `track_knockout_stages()` in sequence. Pass pre-loaded data files for playoff pairings and bracket rules to avoid per-call disk I/O.
    3. Assemble and return `SimulationResult` with MC metadata + standings from representative iteration + teams probabilities from MC + playoff ties/winners from rep iteration + bracket rounds/champion from tree + stages

    **`main()`:**
    Per RESEARCH.md lines 253-299 flow:
    1. `args = _parse_args()` — get parsed arguments
    2. Resolve data directory: `os.path.join(os.path.dirname(os.path.abspath(__file__)), "data")`
    3. Load fixtures.json from data directory
    4. Extract team names from fixtures, call `fetch_team_elos()` to get elo ratings
    5. Set seed: use `args.seed` if provided, else `random.randrange(10000)` (report the chosen seed to user)
    6. Call `build_simulation_result(fixtures, elo_ratings, seed, args.iterations)` — returns populated `SimulationResult`
    7. Print summary line: "ucl-predict: {result.n_iterations} iterations, seed {result.seed}, snapshot {result.snapshot_date}"
    8. If `args.output`: open file, write `json.dump(asdict(result), f, indent=2)`, print "JSON written to {path}"
    9. Always print to stdout first (D-14) — stdout is never suppressed by output flags

    **`if __name__ == "__main__": main()`** guard.

    Use `os.system("")` on Windows for ANSI support (same pattern as euro/main.py line 191).
  </action>

  <verify>
    <automated>python -c "
from competitions.ucl.main import _parse_args
a = _parse_args([])
assert a.iterations == 10000
assert a.seed is None
assert a.output is None
a2 = _parse_args(['-n','5000','--seed','42','-o','out.json'])
assert a2.iterations == 5000
assert a2.seed == 42
assert a2.output == 'out.json'
print('_parse_args tests passed')
"</automated>
    <automated>python -c "
import sys
from competitions.ucl.main import _parse_args
try:
    _parse_args(['--seed', 'abc'])
    print('FAIL: should have raised SystemExit')
    sys.exit(1)
except SystemExit:
    print('Non-int seed rejected — OK')
"</automated>
    <automated>python -m competitions.ucl.main --help 2>&1 | findstr /C:"ucl-predict"</automated>
    <automated>python -c "
# Validate main.py runs without error (fast test with low iterations)
import sys, os
os.chdir(os.path.dirname(os.path.dirname(os.path.abspath('.'))))
# Just verify the module loads and _parse_args works
from competitions.ucl.main import _parse_args, main
import inspect
assert callable(_parse_args)
assert callable(main)
print('Module loads OK')
"</automated>
    <automated>grep -v "^#" competitions/ucl/main.py | grep -c "from competitions.ucl.src" | findstr /C:"1"</automated>
  </verify>

  <done>
    main.py has working _parse_args with 3 flags, main() orchestrates MC + rep iteration + assembles SimulationResult, JSON export works via --output, tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create test_cli.py with argparse unit tests</name>

  <files>
    competitions/ucl/tests/test_cli.py (NEW)
  </files>

  <read_first>
    competitions/ucl/main.py (just created — _parse_args function)
    .planning/phases/03-ucl-orchestration-display/03-RESEARCH.md lines 456-491 (test_cli.py code example with 6 test functions)
  </read_first>

  <acceptance_criteria>
    - `test_defaults`: `_parse_args([])` returns iterations=10000, seed=None, output=None
    - `test_iterations_flag`: `_parse_args(["-n", "5000"])` returns iterations=5000
    - `test_seed_flag`: `_parse_args(["--seed", "42"])` returns seed=42
    - `test_output_flag`: `_parse_args(["-o", "results.json"])` returns output="results.json"
    - `test_all_flags_together`: all 3 flags combined return correct values
    - `test_seed_rejects_non_int`: `_parse_args(["--seed", "abc"])` raises SystemExit
    - All 6 tests pass with `python -m pytest competitions/ucl/tests/test_cli.py -x -v`
  </acceptance_criteria>

  <action>
    Create `competitions/ucl/tests/test_cli.py` following the RESEARCH.md code example (lines 456-491) exactly. Import `_parse_args` from `competitions.ucl.main`. Define 6 test functions with no class wrapper (module-level pytest functions):

    1. `test_defaults` — empty argv, assert defaults
    2. `test_iterations_flag` — `-n 5000`
    3. `test_seed_flag` — `--seed 42`
    4. `test_output_flag` — `-o results.json`
    5. `test_all_flags_together` — all three flags combined
    6. `test_seed_rejects_non_int` — `--seed abc` triggers SystemExit

    Use `from competitions.ucl.main import _parse_args` at top. No conftest fixtures needed. Each function is a standalone test.
  </action>

  <verify>
    <automated>python -m pytest competitions/ucl/tests/test_cli.py -x -v 2>&1 | findstr /C:"passed" /C:"failed"</automated>
    <automated>python -m pytest competitions/ucl/tests/test_cli.py -x -v 2>&1 | findstr /C:"6 passed"</automated>
  </verify>

  <done>
    6 argparse tests pass. All flag parsing behaviors (defaults, each flag individually, combined, non-int rejection) are covered.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → main.py | Untrusted user input enters via argparse flags |
| main.py → JSON file | Simulation result data written to user-specified path via --output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | _parse_args | mitigate | argparse type=int for --iterations and --seed prevents non-numeric input; type=str for --output prevents injection |
| T-03-02 | Tampering | --output file path | mitigate | argparse validates as string only (not FileType "w" which truncates on parse). File opened only after successful simulation. Path is user-controlled — document that user chooses the path |
| T-03-03 | Info Disclosure | JSON output | accept | No PII in simulation data. Team names are public knowledge. Probabilities are derived, not sensitive |
| T-03-04 | Tampering | SimulationResult contract | mitigate | dataclass is frozen=True — prevents field mutation after construction. I/O only occurs via asdict() serialization |
| T-03-SC | Tampering | Stdlib packages | accept | No external package installs in Phase 3. All stdlib (argparse, json, dataclasses, os, sys) |
</threat_model>

<verification>
- `python -m pytest competitions/ucl/tests/test_cli.py -x -v` — all 6 tests pass
- `python -c "from competitions.ucl.result import SimulationResult; print('OK')"` — import succeeds
- `python -c "from competitions.ucl import SimulationResult; print('OK')"` — __init__ re-export works
- `python -c "from competitions.ucl.main import _parse_args, main; print('OK')"` — main module loads
</verification>

<success_criteria>
- `result.py` exists with frozen `SimulationResult` dataclass (stable schema, backward-compatible for future fields)
- `main.py` exists with `_parse_args()` and `main()`
- `__init__.py` re-exports `SimulationResult`
- `conftest.py` has `sample_result` fixture
- `test_cli.py` has 6 passing tests for all flag behaviors
- All imports in `result.py` are stdlib-only (no `competitions.ucl.src` imports)
</success_criteria>

<output>
Create `.planning/phases/03-ucl-orchestration-display/03-01-SUMMARY.md` when done
</output>
