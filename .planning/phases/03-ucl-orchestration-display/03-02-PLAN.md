---
phase: 03-ucl-orchestration-display
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - competitions/ucl/display.py
  - competitions/ucl/main.py
  - competitions/ucl/tests/test_display.py
autonomous: true
requirements:
  - UCLO-02
user_setup: []
must_haves:
  truths:
    - "User can view a formatted simulation summary (iterations, seed, snapshot date)"
    - "User can view a formatted 36-row league table with Pos, Team, Pts, GD, GS, Zone columns"
    - "Top-8 zone rows are highlighted in green, playoff zone in yellow, eliminated zone in red (when terminal supports color)"
    - "Plain text fallback when stdout is piped (no ANSI codes)"
    - "Section header uses `==== League Table ====` format per D-12"
    - "All display functions accept a SimulationResult and return None (side-effect: print to stdout)"
    - "D-17 enforced: display.py has zero imports from competitions.ucl.src"
  artifacts:
    - path: "competitions/ucl/display.py"
      provides: "Pure display functions — print_summary, print_league_table, ANSI helpers"
      exports: ["print_summary", "print_league_table", "_supports_color", "_ansi", "_green", "_yellow", "_red", "_bold"]
    - path: "competitions/ucl/main.py"
      provides: "Integrated display calls in main() after simulation completes"
      pattern: "print_summary|print_league_table"
    - path: "competitions/ucl/tests/test_display.py"
      provides: "Stdout capture tests for league table display"
      contains: "test_league_table_prints_all_36_teams, test_league_table_has_6_columns, test_ansi_present_when_tty, test_ansi_absent_when_piped"
  key_links:
    - from: "competitions/ucl/display.py"
      to: "competitions/ucl/result.py"
      via: "import SimulationResult"
      pattern: "from.*result.*import.*SimulationResult"
    - from: "competitions/ucl/main.py"
      to: "competitions/ucl/display.py"
      via: "imports and calls print_summary, print_league_table"
      pattern: "from.*display.*import"
    - from: "competitions/ucl/main.py"
      to: "competitions/ucl/main.py"
      via: "main() calls display after assembly, before JSON export"
      pattern: "print_summary|print_league_table"
---

<objective>
**Phase Goal — As a** user, **I want to** view formatted UCL simulation results — league table with qualification zone highlighting — after running `ucl-predict`, **so that** I can quickly assess team positions and advancement chances.

Create the `display.py` module with `print_summary()` and `print_league_table()`, plus ANSI color helper functions. The display layer reads ONLY the `SimulationResult` contract (D-17) — zero imports from `competitions.ucl.src`. Integrate these functions into `main.py`'s main() function in the correct display order (D-06: summary first, league table second).

This plan follows the vertical-slice pattern: after this plan, running `ucl-predict` produces formatted, color-highlighted league table output.

Purpose: Deliver requirement UCLO-02 — display league table with qualification zones after simulation.
Output: `display.py`, updated `main.py`, `test_display.py`
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ucl-orchestration-display/03-CONTEXT.md
@.planning/phases/03-ucl-orchestration-display/03-RESEARCH.md
@competitions/ucl/result.py
@competitions/ucl/main.py
@competitions/euro/display.py
@competitions/ucl/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create display.py with print_summary, print_league_table, and ANSI helpers</name>

  <files>
    competitions/ucl/display.py (NEW)
  </files>

  <read_first>
    competitions/ucl/result.py (SimulationResult dataclass fields — standings list[dict] shape, stage_order)
    .planning/phases/03-ucl-orchestration-display/03-CONTEXT.md lines 36-38 (D-07: table columns, D-10/D-11: ANSI colors)
    .planning/phases/03-ucl-orchestration-display/03-CONTEXT.md lines 39-42 (D-12: section headers with separator lines)
    .planning/phases/03-ucl-orchestration-display/03-CONTEXT.md lines 26-35 (D-06: display order — summary first, league table second)
    .planning/phases/03-ucl-orchestration-display/03-RESEARCH.md lines 376-397 (ANSI color detection + factory code example)
    .planning/phases/03-ucl-orchestration-display/03-RESEARCH.md lines 233-246 (display function signature pattern)
    .planning/phases/03-ucl-orchestration-display/03-RESEARCH.md lines 301-305 (anti-patterns: D-17 violation, JSON mixing)
    competitions/euro/display.py lines 1-18 (Euro's 44-line display module — minimal pattern)
    competitions/ucl/tests/conftest.py (sample_result fixture shape — what fields test will use)
  </read_first>

  <acceptance_criteria>
    - `from competitions.ucl.display import print_summary, print_league_table, _supports_color, _ansi` succeeds
    - `print_summary(result)` prints "==== Simulation Summary ====" header followed by lines showing iterations count, seed value, snapshot date
    - `print_league_table(result)` prints "==== League Table ====" header followed by 36 data rows + header row
    - Output contains "Pos", "Team", "Pts", "GD", "GS", "Zone" as column headers
    - Top-8 zone rows contain ANSI green codes (`\033[32m`), playoff rows contain yellow (`\033[33m`), eliminated rows contain red (`\033[31m`) when terminal supports it
    - `_supports_color()` returns `True` when `sys.stdout.isatty()` and `display.NO_COLOR` is False
    - Column widths are at least: Pos=2, Team=22, Pts=3, GD=4, GS=3, Zone=10 — printed via f-string formatting
    - `grep -c "from competitions.ucl.src" competitions/ucl/display.py` returns 0 (D-17 enforcement — zero sim imports)
    - `grep -c "from competitions.ucl import" competitions/ucl/display.py` matches exactly `from competitions.ucl.result import SimulationResult` (only import from result.py)
  </acceptance_criteria>

  <action>
    Create `competitions/ucl/display.py` per D-17: imports from `competitions.ucl.result` (SimulationResult) and stdlib ONLY. No imports from `competitions.ucl.src`.

    **Module-level constants:**
    - `NO_COLOR = False` — module flag, set by main.py if stdout is not a TTY
    - `_SECTION_SEP = "=" * 60` — section separator line (per D-12 section header style)
    - Zone color names matching RESEARCH.md examples: `_GREEN = "32"`, `_YELLOW = "33"`, `_RED = "31"`, `_BOLD = "1"`

    **ANSI helpers (per RESEARCH.md lines 376-397):**
    - `_supports_color() -> bool`: returns `sys.stdout.isatty() and not NO_COLOR`
    - `_ansi(code: str) -> callable`: returns a wrapper function that wraps text in `\033[{code}m{text}\033[0m` if color is supported, else returns text unchanged
    - Module-level wrappers: `_green = _ansi("32")`, `_yellow = _ansi("33")`, `_red = _ansi("31")`, `_bold = _ansi("1")`
    - `_zone_color(zone: str) -> callable`: returns `_green` if zone=="top_8", `_yellow` if zone=="playoff", `_red` if zone=="eliminated", else identity function

    **`print_summary(result: SimulationResult) -> None`** (per D-06 position 1):
    - Print blank line, then `"==== Simulation Summary ===="`, then blank line
    - Print: `"  Iterations: {result.n_iterations}"`, `"  Seed: {result.seed}"`, `"  Snapshot: {result.snapshot_date}"`
    - Print blank line after

    **`print_league_table(result: SimulationResult) -> None`** (per D-06 position 2, D-07 columns):
    - Print blank line, then `"==== League Table ===="`, then blank line
    - Column format (use hardcoded widths): Pos=right-aligned 2, Team=left-aligned 24, Pts=right-aligned 3, GD=right-aligned 4, GS=right-aligned 3, Zone=left-aligned 6
    - Print header row with bold ANSI wrapping: `_bold("Pos")`, `_bold("Team")`, `_bold("Pts")`, `_bold("GD")`, `_bold("GS")`, `_bold("Zone")`
    - Print separator line: `"-" * 48` (or appropriate width)
    - For each entry in `result.standings` (sorted by position ascending — they arrive pre-sorted from simulation):
      - Get the zone color function via `_zone_color(entry["zone"])`
      - Format row: `{pos:>2}  {team:<24} {pts:>3} {gd:>+4} {gs:>3} {zone_color(zone_label):<6}`
      - Zone label: uppercase of entry["zone"] (e.g., "TOP_8", "PLAYOFF", "ELIMINATED")
    - Print blank line after table

    **Column format details:**
    - Position: `{entry['position']:>2}.` (right-aligned, period suffix)
    - Team: `{entry['team']:<24}` (left-aligned, 24 chars)
    - Pts: `{entry['pts']:>3}` (right-aligned, 3 chars)
    - GD: `{entry['gd']:>+4}` (right-aligned with sign, 4 chars, e.g. "+10" or " -3")
    - GS: `{entry['gs']:>3}` (right-aligned, 3 chars)
    - Zone: color-wrapped uppercase zone label, left-aligned 6 chars
  </action>

  <verify>
    <automated>python -c "from competitions.ucl.display import print_summary, print_league_table; print('OK')"</automated>
    <automated>grep -c "from competitions.ucl.src" competitions/ucl/display.py || echo "0 (D-17 compliant)"</automated>
    <automated>python -c "
import ast
with open('competitions/ucl/display.py') as f:
    tree = ast.parse(f.read())
import_lines = []
for node in ast.walk(tree):
    if isinstance(node, ast.ImportFrom) and node.module:
        import_lines.append(node.module)
assert len([m for m in import_lines if 'competitions' in m]) == 1, f'Expected 1 competition import: {import_lines}'
assert 'competitions.ucl.result' in import_lines, f'Missing result import: {import_lines}'
print('D-17: only result.py import found')
"</automated>
    <automated>python -c "
import io, sys
from competitions.ucl.result import SimulationResult
from competitions.ucl.display import print_summary, print_league_table
r = SimulationResult(
    snapshot_date='2026-06-28', n_iterations=10000, seed=42,
    standings=[{'position': 1, 'team': 'Man City', 'pts': 18, 'gd': 10, 'gs': 14, 'zone': 'top_8'},
               {'position': 36, 'team': 'Bodo/Glimt', 'pts': 2, 'gd': -8, 'gs': 3, 'zone': 'eliminated'}],
    teams={}, playoff_ties={}, playoff_winners={}, bracket_rounds={},
    bracket_champion=None, stages={}
)
buf = io.StringIO(); old = sys.stdout; sys.stdout = buf
try:
    print_summary(r)
    print_league_table(r)
finally:
    sys.stdout = old
output = buf.getvalue()
assert '==== Simulation Summary ====' in output
assert '==== League Table ====' in output
assert 'Pos' in output and 'Team' in output and 'Pts' in output
assert 'Man City' in output and 'Bodo/Glimt' in output
assert 'TOP_8' in output and 'ELIMINATED' in output
print('Display output validation OK')
"</automated>
  </verify>

  <done>
    display.py created with print_summary, print_league_table, ANSI helpers; D-17 compliant (zero src imports); produces formatted 36-row league table with zone coloring.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate display functions into main.py main()</name>

  <files>
    competitions/ucl/main.py (MODIFY)
  </files>

  <read_first>
    competitions/ucl/main.py (just created — existing main() body with print of summary line)
    competitions/ucl/display.py (just created — print_summary, print_league_table signatures)
    competitions/ucl/result.py (SimulationResult fields)
    .planning/phases/03-ucl-orchestration-display/03-CONTEXT.md lines 26-31 (D-06: display order — summary first, league table second)
    .planning/phases/03-ucl-orchestration-display/03-CONTEXT.md lines 39-42 (D-12: section headers, blank lines)
  </read_first>

  <acceptance_criteria>
    - main.py import line `from competitions.ucl.display import print_summary, print_league_table` exists
    - `main()` calls `print_summary(result)` then `print_league_table(result)` after assembling SimulationResult
    - The single `print(f"ucl-predict: {n_iterations} iterations...")` line is replaced by the display functions (not in addition to)
    - JSON export (--output) still happens AFTER display calls per D-14
    - Result from plan 03-01's basic stdout print is now the full formatted summary + table
    - Every D-06 section header is present: "==== Simulation Summary ====" and "==== League Table ===="
  </acceptance_criteria>

  <action>
    Modify `competitions/ucl/main.py`:

    1. Add import: `from competitions.ucl.display import print_summary, print_league_table` (insert after the result.py import line)
    2. In `main()`, after the SimulationResult assembly block and before JSON export, replace the single summary print line:
       - Remove: `print(f"ucl-predict: {n_iterations} iterations, seed {seed}, snapshot {snapshot_date}")`
       - Add: `print_summary(result)`
       - Add: `print_league_table(result)`
    3. Keep the rest of main() unchanged (MC loop, assembly, JSON export)

    The order after assembly should be: first call `print_summary(result)`, then call `print_league_table(result)`, then handle JSON export (if `args.output` is set, open file and `json.dump(asdict(result), f, indent=2)`).

    Do NOT add `print_playoff_rounds()`, `print_knockout_bracket()`, or `print_odds()` calls — those come in Plan 03-03.
  </action>

  <verify>
    <automated>grep -n "from competitions.ucl.display import" competitions/ucl/main.py</automated>
    <automated>grep -n "print_summary\|print_league_table" competitions/ucl/main.py</automated>
    <automated>python -c "
import ast
with open('competitions/ucl/main.py') as f:
    tree = ast.parse(f.read())
# Verify display calls exist after assembly
source = open('competitions/ucl/main.py').read()
assert 'print_summary(result)' in source
assert 'print_league_table(result)' in source
# Verify old basic print is gone
assert 'ucl-predict:' not in source or 'print_summary' in source  # either removed or replaced
print('Display integration verified')
"</automated>
  </verify>

  <done>
    main.py imports and calls print_summary and print_league_table in D-06 order, after assembly but before JSON export.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create test_display.py with stdout capture tests for league table</name>

  <files>
    competitions/ucl/tests/test_display.py (NEW)
  </files>

  <read_first>
    competitions/ucl/display.py (just created — all exported functions)
    competitions/ucl/result.py (SimulationResult dataclass)
    competitions/ucl/tests/conftest.py (sample_result fixture — will be used in tests)
    .planning/phases/03-ucl-orchestration-display/03-RESEARCH.md lines 428-453 (stdout capture pattern + example test)
  </read_first>

  <acceptance_criteria>
    - `test_league_table_prints_all_36_teams`: captured output contains "1." and "36."
    - `test_league_table_has_6_columns`: captured output contains "Pos", "Team", "Pts", "GD", "GS", "Zone"
    - `test_league_table_zone_colors`: ANSI escape codes appear for top_8 (\033[32m), playoff (\033[33m), eliminated (\033[31m) rows when _supports_color is forced
    - `test_league_table_plain_text_when_piped`: output contains zone labels (TOP_8, PLAYOFF, ELIMINATED) without ANSI codes when color disabled
    - `test_summary_shows_metadata`: output contains "Iterations: 10000", "Seed: 42", "Snapshot: 2026-06-28"
    - `test_print_league_table_signature`: function accepts SimulationResult, returns None
    - `python -m pytest competitions/ucl/tests/test_display.py -x -v` — all tests pass
  </acceptance_criteria>

  <action>
    Create `competitions/ucl/tests/test_display.py` with:

    **Imports:** `io`, `sys`, `pytest`, `from competitions.ucl.result import SimulationResult`, `from competitions.ucl import display`

    **Helper:** `_capture(func, *args, **kwargs) -> str` — redirects sys.stdout to io.StringIO, calls func(*args, **kwargs), restores stdout, returns captured text (per RESEARCH.md lines 437-445)

    **Tests (7 functions):**

    1. `test_summary_shows_metadata(sample_result)`: capture `print_summary(result)`, assert "Iterations:" in output, "Seed:" in output, "Snapshot:" in output

    2. `test_league_table_prints_all_36_teams(sample_result)`: capture `print_league_table(result)`, assert "1." in output AND "36." in output (check first and last position markers)

    3. `test_league_table_has_6_columns(sample_result)`: capture `print_league_table(result)`, assert all 6 column headers present using `_bold()` wrapped versions — check for "Pos", "Team", "Pts", "GD", "GS", "Zone" in output

    4. `test_league_table_zone_colors(sample_result)`: set `display.NO_COLOR = True` so _supports_color returns True, capture output, assert `\033[32m` (green) appears for top_8 rows (top teams), `\033[33m` (yellow) for playoff, `\033[31m` (red) for eliminated. Reset `display.NO_COLOR = False` after. Use try/finally to ensure reset.

    5. `test_league_table_plain_text_when_piped(sample_result)`: ensure `display.NO_COLOR = False` (the default), capture output via StringIO (which is not a TTY), assert NO ANSI escape codes (`\033[`) appear in output, assert "TOP_8" text appears

    6. `test_print_league_table_zone_labels_all_present(sample_result)`: capture output, assert "TOP_8" and "PLAYOFF" and "ELIMINATED" appear in output (zone labels rendered)

    7. `test_team_column_contains_known_team(sample_result)`: assert "Man City" appears somewhere in the league table output

    Use `@pytest.mark.usefixtures("sample_result")` or direct fixture parameter. The `sample_result` fixture is defined in conftest.py and provides a populated SimulationResult with all 36 teams.

    Add a D-17 static check at module level: import `competitions.ucl.display`, then inspect `sys.modules` for any key containing `competitions.ucl.src`. Assert that none exist (meaning loading display.py did NOT transitively import simulation modules). Use `assert len([m for m in sys.modules if 'competitions.ucl.src' in m]) == 0`.
    (This check runs at import time of test_display.py, verifying no simulation modules were transitively imported.)
  </action>

  <verify>
    <automated>python -m pytest competitions/ucl/tests/test_display.py -x -v 2>&1 | findstr /C:"passed" /C:"failed"</automated>
    <automated>python -m pytest competitions/ucl/tests/ -x -q 2>&1 | findstr /C:"passed"</automated>
    <automated>python -c "
# Verify D-17 static check in test file
with open('competitions/ucl/tests/test_display.py') as f:
    content = f.read()
assert 'competitions.ucl.src' in content or 'D-17' in content, 'D-17 check must be present'
print('D-17 static check present')
"</automated>
  </verify>

  <done>
    7 display tests pass covering: 36-team output, 6 column headers, ANSI zone coloring, plain-text fallback, zone label presence, metadata display, D-17 compliance.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| display.py → stdout | Formatted text output with ANSI escape codes |
| display.py → SimulationResult | Read-only consumption of result data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | display.py imports | mitigate | D-17 enforced via static grep check and runtime module audit in test_display.py. Any `from competitions.ucl.src` import will fail both |
| T-03-06 | Spoofing | ANSI escape injection | accept | Team names are from data files (controlled source), not user input. Probabilities and stats are numeric — no injection surface |
| T-03-07 | Repudiation | display output | accept | Display is ephemeral stdout. Persistent output is via `--output` JSON file (separate code path in main.py, not in display.py) |
| T-03-SC | Tampering | Stdlib packages | accept | display.py uses only sys (stdlib) and competitions.ucl.result (our code). No external packages |
</threat_model>

<verification>
- `python -m pytest competitions/ucl/tests/test_display.py -x -v` — all 7 tests pass
- `python -m pytest competitions/ucl/tests/ -x -q` — all UCL tests pass (plan 01 + plan 02 tests)
- `grep -c "from competitions.ucl.src" competitions/ucl/display.py` — 0 results (D-17)
- `python -c "import competitions.ucl.display; print('OK')"` — module loads without simulation imports
</verification>

<success_criteria>
- `display.py` has `print_summary`, `print_league_table`, ANSI helpers — zero imports from `competitions.ucl.src`
- `main.py` calls `print_summary` and `print_league_table` in D-06 order
- League table has 36 rows, 6 columns (Pos/Team/Pts/GD/GS/Zone), zone color highlighting
- 7 display tests pass
- Full UCL test suite green
- D-17 enforced in tests via static + runtime checks
</success_criteria>

<output>
Create `.planning/phases/03-ucl-orchestration-display/03-02-SUMMARY.md` when done
</output>
