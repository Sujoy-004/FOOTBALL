---
phase: 03-ucl-orchestration-display
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - competitions/ucl/display.py
  - competitions/ucl/main.py
  - competitions/ucl/tests/test_display.py
autonomous: true
requirements:
  - UCLO-03
  - UCLO-04
user_setup: []
must_haves:
  truths:
    - "User can view 8 playoff ties with aggregate scores, advancing winners, ET/Pens shown only when triggered"
    - "User can view knockout bracket as round-by-round match list (R16 → QF → SF → Final, NOT ASCII tree)"
    - "User can view champion/qualification odds for all 36 teams sorted by champion probability descending"
    - "Odds columns: Rank, Team, Champion %, Final %, SF %, QF % (per D-09)"
    - "Display order follows D-06 tournament chronology (summary → table → playoff → bracket → odds)"
    - "JSON export via --output includes full playoff/bracket data with stable schema for Phase 4 consumers"
    - "D-17 enforced: display.py still has zero imports from competitions.ucl.src (no new imports added)"
  artifacts:
    - path: "competitions/ucl/display.py"
      provides: "Extended with print_playoff_rounds, print_knockout_bracket, print_odds"
      exports: ["print_playoff_rounds", "print_knockout_bracket", "print_odds"]
    - path: "competitions/ucl/main.py"
      provides: "Full 5-section display pipeline + JSON export with complete schema"
      pattern: "print_playoff_rounds|print_knockout_bracket|print_odds"
    - path: "competitions/ucl/tests/test_display.py"
      provides: "Extended tests for playoff, bracket, odds, ANSI consistency, JSON schema"
      contains: "test_playoff_displays_8_ties, test_bracket_rounds_in_order, test_odds_sorted_by_champion"
  key_links:
    - from: "competitions/ucl/main.py"
      to: "competitions/ucl/display.py"
      via: "imports all 5 print functions"
      pattern: "from.*display.*import.*print_summary.*print_league_table.*print_playoff_rounds.*print_knockout_bracket.*print_odds"
    - from: "competitions/ucl/display.py"
      to: "competitions/ucl/result.py"
      via: "reads playoff_ties, bracket_rounds, teams, stages from SimulationResult"
      pattern: "result\\.playoff_ties|result\\.bracket_rounds|result\\.stages"
    - from: "competitions/ucl/main.py"
      to: "---output JSON file"
      via: "dataclasses.asdict(result) for full schema export"
      pattern: "asdict"
---

<objective>
**Phase Goal — As a** user, **I want to** view the complete UCL knockout bracket with matchups and champion/qualification odds after running `ucl-predict`, **so that** I can analyze which teams are most likely to advance through each round.

Extend `display.py` with `print_playoff_rounds()`, `print_knockout_bracket()`, and `print_odds()`. Wire all 5 display sections into `main.py`'s main() in tournament chronology order (D-06). Add comprehensive tests for bracket display, odds formatting, and JSON schema stability. This plan completes the display layer — after this, running `ucl-predict` produces the full output: Summary → League Table → Playoff Results → Knockout Bracket → Champion/Qualification Odds.

Purpose: Deliver requirements UCLO-03 (knockout bracket) and UCLO-04 (champion/qualification odds).
Output: Extended `display.py`, extended `main.py`, extended `test_display.py`
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ucl-orchestration-display/03-CONTEXT.md
@.planning/phases/03-ucl-orchestration-display/03-RESEARCH.md
@competitions/ucl/display.py
@competitions/ucl/main.py
@competitions/ucl/result.py
@competitions/ucl/tests/test_display.py
@competitions/ucl/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend display.py with playoff rounds, knockout bracket, and odds display</name>

  <files>
    competitions/ucl/display.py (MODIFY — append 3 new functions at end)
  </files>

  <read_first>
    competitions/ucl/display.py (current functions: print_summary, print_league_table, ANSI helpers — zone color functions available: _green, _yellow, _red, _bold, _zone_color)
    competitions/ucl/result.py (SimulationResult dataclass fields — playoff_ties: dict[int, dict], playoff_winners: dict[int, str], bracket_rounds: dict[str, list[dict]], teams: dict[str, dict], stages: dict[str, str], stage_order: list[str])
    competitions/ucl/src/knockout.py lines 197-212 (simulate_two_legged_tie result dict keys: winner, loser, aggregate_a, aggregate_b, leg1/leg2, et_played, et_a, et_b, penalties_played, penalty_a, penalty_b)
    .planning/phases/03-ucl-orchestration-display/03-CONTEXT.md lines 26-35 (D-06: display order positions 3-5)
    .planning/phases/03-ucl-orchestration-display/03-CONTEXT.md lines 33-35 (D-08: round-by-round match list, playoff ties with aggregate scores)
    .planning/phases/03-ucl-orchestration-display/03-CONTEXT.md lines 34-35 (D-09: odds columns, sorted by champion prob descending, all 36 teams)
    .planning/phases/03-ucl-orchestration-display/03-CONTEXT.md lines 36-38 (D-10: ANSI color, D-12: section headers)
    .planning/phases/03-ucl-orchestration-display/03-RESEARCH.md lines 400-425 (bracket display code example)
    .planning/phases/03-ucl-orchestration-display/03-RESEARCH.md lines 113-115 (playoff tie format: "9th Team A  3-2 agg  24th Team B  → Team A advances")
    .planning/phases/03-ucl-orchestration-display/03-RESEARCH.md lines 301-305 (anti-patterns: don't mix JSON export into display functions)
  </read_first>

  <acceptance_criteria>
    - `print_playoff_rounds(result)` prints 8 ties, each showing aggregate score and advancing winner
    - `print_knockout_bracket(result)` prints R16 → QF → SF → FINAL sections in order, each with matchups
    - `print_odds(result)` prints all 36 teams sorted by champion probability descending
    - Odds columns: Rank, Team, Champion %, Final %, SF %, QF %
    - Bracket uses round-by-round match list format (NOT ASCII tree)
    - ANSI bold used for section headers, zone colors NOT needed for bracket/odds (only league table gets zone colors per D-10)
    - `grep -c "from competitions.ucl.src" competitions/ucl/display.py` returns 0 (D-17 still enforced with new functions)
    - Playoff ET/Pens display format: when `et_played=True`, append `(X-X ET)`; when `penalties_played=True`, append `(X-X pens)` — only shown when triggered
  </acceptance_criteria>

  <action>
    Append three new functions to `competitions/ucl/display.py`. Do NOT add new imports — existing imports (SimulationResult + stdlib) are sufficient.

    **`print_playoff_rounds(result: SimulationResult) -> None`** (D-06 position 3):
    - Print blank line, `"==== Playoff Results ===="`, blank line
    - Iterate over `result.playoff_ties` sorted by tie number (1-8)
    - For each tie: get the full result dict from `result.playoff_ties[tie_num]`, advance winner from `result.playoff_winners[tie_num]`
    - Format per CONTEXT.md line 113-115:
      - Base: `"  Tie {n}: {team_a} {agg_a}-{agg_b} agg  {team_b} → {winner} advances"`
      - If `et_played`: `{agg_a}-{agg_b} agg ({et_a}-{et_b} ET)`  — replace `agg` segment
      - If `penalties_played`: append `({pen_a}-{pen_b} pens)` after the agg display
      - Example output line: `  Tie 1: Man City 3-2 agg  Sporting → Man City advances`
      - With ET: `  Tie 1: Man City 2-2 agg (1-0 ET)  Sporting → Man City advances`
      - With Pens: `  Tie 1: Man City 2-2 agg (1-1 ET, 4-3 pens)  Sporting → Man City advances`
    - Print blank line after

    **`print_knockout_bracket(result: SimulationResult) -> None`** (D-06 position 4, D-08 format):
    - Print blank line, `"==== Knockout Bracket ===="`, blank line
    - Round order: `["R16", "QF", "SF", "FINAL"]`
    - For each round:
      - Print `"  --- {round_name} ---"` (bold via _bold wrapper)
      - Get matches from `result.bracket_rounds.get(round_name, [])`
      - For each match in the round:
        - Extract `team_a`, `team_b`, `winner`, `result` (the match result dict)
        - For R16/QF/SF (two-legged): show aggregate scores
          - `agg_a = result["aggregate_a"]`, `agg_b = result["aggregate_b"]`
          - Base: `"{team_a} {agg_a}-{agg_b} agg  {team_b}"` — mark winner with `*{team_a}*` or `*{team_b}*` (but not needed — just show scores)
          - ET suffix: `" ({et_a}-{et_b} ET)"` if `result.get("et_played")`
          - Pens suffix: `" ({pen_a}-{pen_b} pens)"` if `result.get("penalties_played")`
        - For FINAL (single match, `is_final=True`): `"{team_a} {score_a}-{score_b} {team_b}"` — ET/Pens only if triggered
          - Check `result.get("is_final")` key — if present and True, use score_a/score_b directly
        - Prepend `"    "` indent
        - Print each match on its own line
      - Print blank line after each round
    - Print blank line after bracket section

    **`print_odds(result: SimulationResult) -> None`** (D-06 position 5, D-09 format):
    - Print blank line, `"==== Champion / Qualification Odds ===="`, blank line
    - Per D-09: Rank, Team, Champion %, Final %, SF %, QF % — for all 36 teams
    - Sort teams from `result.teams` by `champion_prob` descending
    - Tie-breaking: if equal champion_prob, sort alphabetically by team name (for deterministic output)
    - Column widths: Rank=right-aligned 4, Team=left-aligned 24, Champion%=right-aligned 8, Final%=right-aligned 8, SF%=right-aligned 8, QF%=right-aligned 8
    - Print header row with bold ANSI: `_bold("Rank")`, `_bold("Team")`, `_bold("Champion")`, `_bold("Final")`, `_bold("SF")`, `_bold("QF")` — with `%` suffix in headers
    - Print separator: `"-" * 64` (or appropriate width)
    - For each team entry (ranked 1-36):
      - Probability columns: `champion_prob`, `stage_final_prob`, `stage_sf_prob`, `stage_qf_prob`
      - Format: `{rank:>4}  {team:<24} {champ:>7.1%} {final:>7.1%} {sf:>7.1%} {qf:>7.1%}`
      - Note: `stage_final_prob` may not exist for all teams — use `team_data.get("stage_final_prob", 0.0)` with default 0.0 to handle teams eliminated before that stage
    - Print blank line after

    **Key state note:** The `sample_result` fixture in conftest.py must include `stage_final_prob`, `stage_sf_prob`, `stage_qf_prob` in team probability dicts. Verify the fixture has these fields — if not, add them during this task.
  </action>

  <verify>
    <automated>grep -c "from competitions.ucl.src" competitions/ucl/display.py || echo "0 (D-17 OK)"</automated>
    <automated>python -c "
from competitions.ucl.display import print_playoff_rounds, print_knockout_bracket, print_odds
print('All 3 new functions importable')
"</automated>
    <automated>python -c "
import io, sys
from competitions.ucl.result import SimulationResult
from competitions.ucl import display
r = SimulationResult(
    snapshot_date='2026-06-28', n_iterations=10000, seed=42,
    standings=[],
    teams={
        'Man City': {'champion_prob': 0.15, 'stage_final_prob': 0.25, 'stage_sf_prob': 0.40, 'stage_qf_prob': 0.60},
        'Bayern': {'champion_prob': 0.12, 'stage_final_prob': 0.20, 'stage_sf_prob': 0.35, 'stage_qf_prob': 0.55},
    },
    playoff_ties={1: {'winner': 'Man City', 'loser': 'Sporting', 'aggregate_a': 3, 'aggregate_b': 2, 'leg1': {}, 'leg2': {}, 'et_played': False, 'penalties_played': False, 'et_a': 0, 'et_b': 0, 'penalty_a': 0, 'penalty_b': 0}},
    playoff_winners={1: 'Man City'},
    bracket_rounds={'R16': [{'match_id': 'r16_01', 'team_a': 'Man City', 'team_b': 'Sporting', 'winner': 'Man City', 'result': {'aggregate_a': 4, 'aggregate_b': 1, 'et_played': False, 'penalties_played': False}}],
                    'QF': [{'match_id': 'qf_01', 'team_a': 'Man City', 'team_b': 'Bayern', 'winner': 'Man City', 'result': {'aggregate_a': 2, 'aggregate_b': 0, 'et_played': False, 'penalties_played': False}}],
                    'SF': [], 'FINAL': [{'match_id': 'final_01', 'team_a': 'Man City', 'team_b': 'Real Madrid', 'winner': 'Man City', 'result': {'score_a': 1, 'score_b': 0, 'is_final': True, 'et_played': False, 'penalties_played': False}}]},
    bracket_champion='Man City',
    stages={},
)
buf = io.StringIO(); old = sys.stdout; sys.stdout = buf
try:
    display.print_playoff_rounds(r)
    display.print_knockout_bracket(r)
    display.print_odds(r)
finally:
    sys.stdout = old
out = buf.getvalue()
assert '==== Playoff Results ====' in out
assert '==== Knockout Bracket ====' in out
assert '==== Champion / Qualification Odds ====' in out
assert 'Man City' in out
assert 'Champion' in out  # odds header
assert '15.0%' in out or '15.0' in out  # champion prob formatted
print('All sections output correctly')
"</automatic>
  </verify>

  <done>
    display.py extended with print_playoff_rounds, print_knockout_bracket, print_odds. All D-17 compliant. Output format matches CONTEXT.md specifications.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire all 5 display sections into main.py + complete JSON export schema</name>

  <files>
    competitions/ucl/main.py (MODIFY)
  </files>

  <read_first>
    competitions/ucl/main.py (currently imports print_summary, print_league_table and calls them)
    competitions/ucl/display.py (now has all 5 functions)
    competitions/ucl/result.py (SimulationResult fields for JSON schema)
    .planning/phases/03-ucl-orchestration-display/03-CONTEXT.md lines 26-35 (D-06: full display order)
    .planning/phases/03-ucl-orchestration-display/03-CONTEXT.md lines 41-43 (D-13: JSON format)
    .planning/phases/03-ucl-orchestration-display/03-CONTEXT.md lines 43-44 (D-14: dual output behavior)
    .planning/phases/03-ucl-orchestration-display/03-RESEARCH.md lines 337-342 (Pitfall 4: JSON schema stability for Phase 4)
  </read_first>

  <acceptance_criteria>
    - main.py imports all 5 display functions: `print_summary`, `print_league_table`, `print_playoff_rounds`, `print_knockout_bracket`, `print_odds`
    - `main()` calls all 5 in D-06 order: summary → league table → playoff → bracket → odds
    - JSON export (--output) uses `dataclasses.asdict(result)` to get the full schema
    - JSON file contains ALL fields: snapshot_date, n_iterations, seed, teams, standings, playoff_ties, playoff_winners, bracket_rounds, bracket_champion, stages, stage_order
    - JSON file is valid JSON and can be parsed back
    - Stdout still prints formatted text when --output is given (D-14)
  </acceptance_criteria>

  <action>
    Modify `competitions/ucl/main.py`:

    1. **Update import line** to import all 5 display functions: `from competitions.ucl.display import print_summary, print_league_table, print_playoff_rounds, print_knockout_bracket, print_odds`

    2. **In `main()`, after assembly and before JSON export**, replace the current two display calls with the full 5-section pipeline in D-06 order: call `print_summary(result)`, `print_league_table(result)`, `print_playoff_rounds(result)`, `print_knockout_bracket(result)`, `print_odds(result)` sequentially.

    3. **Keep JSON export** unchanged — already uses `dataclasses.asdict(result)` which captures the full SimulationResult schema including playoff_ties, bracket_rounds, and champion fields.

    4. No other changes to main.py needed — the data assembly (MC + rep iteration) already populates all fields of SimulationResult.
  </action>

  <verify>
    <automated>grep -c "print_playoff_rounds\|print_knockout_bracket\|print_odds" competitions/ucl/main.py | findstr /C:"3"</automated>
    <automated>python -c "
import ast
with open('competitions/ucl/main.py') as f:
    tree = ast.parse(f.read())
source = open('competitions/ucl/main.py').read()
for func in ['print_summary', 'print_league_table', 'print_playoff_rounds', 'print_knockout_bracket', 'print_odds']:
    assert func in source, f'{func} not called in main()'
print('All 5 display functions called in main()')
"</automated>
  </verify>

  <done>
    main.py imports and calls all 5 display functions in D-06 tournament chronology order. JSON export captures full SimulationResult schema.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend test_display.py with bracket, playoff, odds, and JSON schema tests</name>

  <files>
    competitions/ucl/tests/test_display.py (MODIFY — append new test functions)
    competitions/ucl/tests/conftest.py (MODIFY — ensure sample_result has stage_final_prob etc.)
  </files>

  <read_first>
    competitions/ucl/tests/test_display.py (existing test functions and _capture helper)
    competitions/ucl/tests/conftest.py (sample_result fixture — verify it has stage_final_prob, stage_sf_prob, stage_qf_prob in teams dict)
    competitions/ucl/display.py (new functions: print_playoff_rounds, print_knockout_bracket, print_odds signatures)
    competitions/ucl/result.py (SimulationResult dataclass — asdict serialization)
    .planning/phases/03-ucl-orchestration-display/03-RESEARCH.md lines 400-425 (bracket display code example)
    .planning/phases/03-ucl-orchestration-display/03-RESEARCH.md lines 456-491 (test patterns)
  </read_first>

  <acceptance_criteria>
    - `test_playoff_displays_8_ties`: captured output contains "Tie 1" through "Tie 8"
    - `test_playoff_shows_advancing_winner`: output contains "advances" for each tie
    - `test_bracket_rounds_in_order`: output contains "--- R16 ---", "--- QF ---", "--- SF ---", "--- FINAL ---" in that order
    - `test_bracket_shows_teams_and_scores`: output contains team names and score patterns (e.g., "4-1")
    - `test_odds_sorted_by_champion`: in captured output, first odds row has highest champion_prob, last row has lowest
    - `test_odds_shows_all_36_teams`: 36 team entries in odds section
    - `test_odds_columns_present`: headers contain Rank, Team, Champion, Final, SF, QF with % signs
    - `test_json_export_schema`: `dataclasses.asdict(sample_result)` contains all expected top-level keys
    - `test_display_order_follows_d06`: in captured output, section headers appear in correct order (Summary → League Table → Playoff → Bracket → Odds)
    - `python -m pytest competitions/ucl/tests/test_display.py -x -v` — all tests pass (existing + new)
  </acceptance_criteria>

  <action>
    **Step 1: Verify conftest.py `sample_result` fixture completeness.**
    Check if `stage_final_prob`, `stage_sf_prob`, `stage_qf_prob` keys exist in each team's probability dict inside `sample_result.teams`. If missing, add them with realistic values (e.g., Man City: stage_final_prob=0.25, stage_sf_prob=0.40, stage_qf_prob=0.60; Slovan Bratislava: all ~0.0).
    Also verify `playoff_ties` has all 8 ties (1-8) and `bracket_rounds` has R16=8 entries, QF=4, SF=2, FINAL=1 at minimum.

    **Step 2: Append new test functions to `competitions/ucl/tests/test_display.py`:**

    **Playoff tests:**
    - `test_playoff_displays_8_ties(sample_result)`: capture `print_playoff_rounds(result)`, assert "Tie 1" through "Tie 8" appear
    - `test_playoff_shows_advancing_winner(sample_result)`: capture, assert "advances" appears at least 8 times
    - `test_playoff_shows_agg_score(sample_result)`: capture, assert "agg" appears in output (aggregate indicator)

    **Bracket tests:**
    - `test_bracket_rounds_in_order(sample_result)`: capture `print_knockout_bracket(result)`, extract round header positions, assert order is R16, QF, SF, FINAL
    - `test_bracket_shows_teams_and_scores(sample_result)`: capture, assert known team names appear, assert score patterns like digit-digit exist

    **Odds tests:**
    - `test_odds_sorted_by_champion(sample_result)`: capture `print_odds(result)`, parse probability values from first and last data rows, assert first >= last
    - `test_odds_shows_all_36_teams(sample_result)`: capture, count team-name lines between header and end (aim for 36 distinct team names)
    - `test_odds_columns_present(sample_result)`: capture, assert "Rank", "Team", "Champion", "Final", "SF", "QF" in bold-wrapped format

    **JSON/static tests:**
    - `test_json_export_schema(sample_result)`: use `dataclasses.asdict(sample_result)`, assert all expected keys present: `snapshot_date`, `n_iterations`, `seed`, `standings`, `teams`, `playoff_ties`, `playoff_winners`, `bracket_rounds`, `bracket_champion`, `stages`, `stage_order`
    - `test_display_order_follows_d06(sample_result)`: capture all sections (call all 5 print functions sequentially), extract section headers in order, assert they appear as: Simulation Summary, League Table, Playoff Results, Knockout Bracket, Champion / Qualification Odds

    **Full suite:**
    - `test_ansi_consistency(sample_result)`: verify that ANSI codes (when enabled) don't leak into non-zone-colored sections (playoff, bracket, odds use bold for headers but not zone colors)

    **Helper to capture all 5 functions:** Create `_capture_full(sample_result)` at module level that creates `io.StringIO()`, saves and redirects `sys.stdout`, calls all 5 display functions (print_summary, print_league_table, print_playoff_rounds, print_knockout_bracket, print_odds) in sequence with `sample_result`, restores stdout in finally, returns `buf.getvalue()`. Use the same try/finally pattern as the existing `_capture()` helper.

    **Important:** Use `display.NO_COLOR = False` (default) for tests that check plain text output. Only tests specifically checking ANSI codes should set NO_COLOR.
  </action>

  <verify>
    <automated>python -m pytest competitions/ucl/tests/test_display.py -x -v 2>&1 | findstr /C:"passed" /C:"failed"</automated>
    <automated>python -m pytest competitions/ucl/tests/ -x -q 2>&1 | findstr /C:"passed"</automated>
    <automated>python -c "
from competitions.ucl.tests.conftest import *
import dataclasses, json
from competitions.ucl.result import SimulationResult
# Verify sample_result has complete team probs
sr = sample_result()
team_sample = list(sr.teams.values())[0]
for key in ['champion_prob', 'stage_final_prob', 'stage_sf_prob', 'stage_qf_prob']:
    assert key in team_sample, f'Missing {key} in sample_result teams'
# Verify JSON schema is complete
d = dataclasses.asdict(sr)
for key in ['snapshot_date', 'n_iterations', 'seed', 'standings', 'teams', 'playoff_ties', 'playoff_winners', 'bracket_rounds', 'bracket_champion', 'stages', 'stage_order']:
    assert key in d, f'Missing {key} in asdict output'
json.dumps(d)  # Verify serializable
print('JSON schema validation OK')
"</automated>
  </verify>

  <done>
    All display tests pass (existing 7 + new 11+ = 18+ total). JSON schema verified with all 11 top-level keys. D-17 still holds. Full feature set delivered.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| display.py → stdout | Odds percentages and bracket data printed to terminal |
| main.py → JSON file | Full SimulationResult serialized to disk via --output |
| JSON schema → Phase 4 consumer | Schema stability is a cross-phase contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | JSON schema changes | mitigate | JSON schema is tested in test_display.py::test_json_export_schema — all 11 keys must be present. Phase 4 tests can import this same assertion |
| T-03-09 | Information Disclosure | Odds percentages | accept | All probabilities are simulation-derived from public team data. No private information |
| T-03-10 | Denial of Service | Bracket round iteration | mitigate | `bracket_rounds` is data-driven but bounded (always 4 rounds, 8+4+2+1 = 15 matches max). No unbounded loops |
| T-03-11 | Elevation of Privilege | asdict serialization | accept | `dataclasses.asdict()` recursively converts to plain dicts — no code execution risk. JSON output is pure data |
| T-03-SC | Tampering | Package installs | accept | No new packages in Phase 3. All stdlib |
</threat_model>

<verification>
- `python -m pytest competitions/ucl/tests/test_display.py -x -v` — all display tests pass (existing + new)
- `python -m pytest competitions/ucl/tests/ -x -q` — complete UCL test suite green
- `python -c "import competitions.ucl.display; print('OK')"` — display module loads with no sim imports
- `grep -c "from competitions.ucl.src" competitions/ucl/display.py` — 0 results (D-17 still holds)
- `python -c "from competitions.ucl.main import main; print('OK')"` — main module loads with all imports
</verification>

<success_criteria>
- `display.py` has all 5 print functions: print_summary, print_league_table, print_playoff_rounds, print_knockout_bracket, print_odds
- `main.py` calls all 5 in D-06 order (summary → league table → playoff → bracket → odds)
- JSON export via `--output` produces complete schema with all 11 fields
- All display tests pass (18+ tests covering playoff, bracket, odds, ANSI, order, JSON schema)
- D-17 still enforced: display.py imports from result.py and stdlib only
- Full UCL test suite green
</success_criteria>

<output>
Create `.planning/phases/03-ucl-orchestration-display/03-03-SUMMARY.md` when done
</output>
