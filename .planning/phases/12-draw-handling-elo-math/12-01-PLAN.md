---
phase: 12-draw-handling-elo-math
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - worldcup_predictor/src/elo.py
  - worldcup_predictor/tests/test_elo.py
autonomous: true
requirements:
  - V2-04
  - V2-03
user_setup: []

must_haves:
  truths:
    - "compute_k_factor(0, 60) returns 60.0 (draw/one-goal win, G=1)"
    - "compute_k_factor(2, 60) returns 90.0 (two-goal win, G=1.5)"
    - "compute_k_factor(3, 60) returns 105.0 (three-goal win, G=(11+N)/8)"
    - "update_ratings() accepts pk_winner parameter for 0.75/0.25 split"
    - "PK match (winner set + is_draw=false + GD=0) uses G=1 multiplier, 0.75/0.25 result"
    - "apply_elo_update computes compute_k_factor(goal_diff, K) and passes adjusted K to update_ratings()"
    - "All existing tests in test_elo.py continue to pass after changes"
  artifacts:
    - path: "worldcup_predictor/src/elo.py"
      provides: "compute_k_factor function, PK mode in update_ratings, K-multiplier wiring in apply_elo_update"
      min_lines: 145  # was 115, adding ~30 lines
      contains: "def compute_k_factor"
    - path: "worldcup_predictor/tests/test_elo.py"
      provides: "TestComputeKFactor class (6+ tests), PK mode tests"
      min_lines: 160  # was 117
      contains: "class TestComputeKFactor"
  key_links:
    - from: "apply_elo_update (elo.py)"
      to: "compute_k_factor"
      via: "call: compute_k_factor(goal_diff, constants.K_FACTOR) at start of apply_elo_update"
      pattern: "compute_k_factor"
    - from: "apply_elo_update (elo.py)"
      to: "update_ratings"
      via: "passes K=int(round(adjusted_K)) and pk_winner=..."
      pattern: "update_ratings.*pk_winner"
---

<objective>
Extend the Elo engine with goal-difference K-multiplier and penalty shootout PK mode.

**Purpose:** Implement `compute_k_factor()` step-function per eloratings.net (V2-04), add `pk_winner` parameter to `update_ratings()` for 0.75/0.25 PK split (D-07), and wire both into `apply_elo_update()` (D-13). This is the math foundation that downstream plans consume.

**Output:** Modified `elo.py` with ~30 new lines and extended `test_elo.py` with 9+ new tests.
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-draw-handling-elo-math/12-CONTEXT.md
@.planning/phases/12-draw-handling-elo-math/12-RESEARCH.md

<interfaces>
From src/elo.py (current signatures):
```
expected_score(rating_a: float, rating_b: float, home_advantage: int = 0) -> float
update_ratings(team_a: str, team_b: str, winner: str | None, current_elos: dict[str, float], K: int = 60) -> dict[str, float]
apply_elo_update(match: dict, teams: dict[str, dict]) -> dict[str, dict[str, float]]
```

From src/constants.py:
```
K_FACTOR: int = 60  # line 6
```

From src/output.py:
```
print_elo_changes(updates: dict[str, dict[str, float]]) -> None  # line 284
```

Test patterns in test_elo.py:
- `TestUpdateRatings` class with 9 test methods (lines 36-117)
- Draw test: `test_draw_favored_team_loses_elo` (line 88) — verifies 0.5/0.5 split
- apply_elo_update test: `test_apply_elo_update_draw` (line 110) — verifies draw with K=60
- Match dict shape: `{"team_a": str, "team_b": str, "winner": str | None}`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement compute_k_factor() with step-function formula (TDD)</name>
  <files>
    worldcup_predictor/src/elo.py
    worldcup_predictor/tests/test_elo.py
  </files>

  <read_first>
    worldcup_predictor/src/elo.py (full — 115 lines, existing function patterns)
    worldcup_predictor/tests/test_elo.py:36-98 (TestUpdateRatings class patterns — test structure, assertions, rounding)
    .planning/phases/12-draw-handling-elo-math/12-RESEARCH.md:422-461 (exact compute_k_factor reference implementation)
  </read_first>

  <behavior>
    RED: Create `TestComputeKFactor` class in test_elo.py with 6 test methods that call `compute_k_factor(gd, base_K)` and assert expected float values BEFORE the function exists:
    - `test_gd_0`: compute_k_factor(0, 60) → 60.0 (draw)
    - `test_gd_1`: compute_k_factor(1, 60) → 60.0 (one-goal win)
    - `test_gd_2`: compute_k_factor(2, 60) → 90.0 (two-goal win)
    - `test_gd_3`: compute_k_factor(3, 60) → 105.0 (three-goal win, (11+3)/8 * 60)
    - `test_gd_7`: compute_k_factor(7, 60) → 135.0 (seven-goal win, (11+7)/8 * 60)
    - `test_custom_base_k`: compute_k_factor(2, 40) → 60.0 (1.5 * 40)

    GREEN: Implement `compute_k_factor(goal_diff: int, base_K: int = 60) -> float` in elo.py.
    Step-function per D-10:
      GD <= 1 → G = 1.0 (draws and one-goal wins)
      GD == 2 → G = 1.5
      GD >= 3 → G = (11 + goal_diff) / 8.0
    Returns `base_K * G` as float.
  </behavior>

  <action>
    Step 1 — Write failing tests (RED): Append `TestComputeKFactor` class to test_elo.py after line 117 with 6 test methods. Use `from src.elo import compute_k_factor` (import already exists at line 10 for expected_score/update_ratings — add compute_k_factor to the import line). Each test calls compute_k_factor with specific GD/base_K and asserts the exact float result using `assert ... ==`. Use `exact ==` asserts (no rounding needed for these values — 60.0, 90.0, 105.0, 135.0, 60.0 are exact floats in Python).

    Step 2 — Confirm RED: Run `pytest tests/test_elo.py::TestComputeKFactor -x -q` — MUST fail with ImportError/NameError since compute_k_factor doesn't exist.

    Step 3 — Implement (GREEN): Add `compute_k_factor` function to elo.py. Place it between `expected_score` (line 34) and `update_ratings` (line 37) for logical grouping — K-multiplier is a supporting computation. Signature: `def compute_k_factor(goal_diff: int, base_K: int = 60) -> float:`. Docstring: describe step-function per D-10 with examples. Body: 3-case if/elif/else matching the step-function spec. Return `base_K * G`.

    Step 4 — Confirm GREEN: Run `pytest tests/test_elo.py::TestComputeKFactor -x -q` — all 6 must pass.

    Step 5 — REFACTOR (if needed): Verify edge cases — negative goal_diff is not expected (caller provides abs()), but if it occurs, the first branch `if goal_diff <= 1` catches negative values. Add a brief docstring note. Run full test_elo.py: `pytest tests/test_elo.py -x -q` — all existing tests + new tests must pass.
  </action>

  <verify>
    <automated>pytest tests/test_elo.py -x -q</automated>
  </verify>

  <done>
    - TestComputeKFactor class exists with 6 passing test methods
    - compute_k_factor(0, 60) == 60.0, compute_k_factor(2, 60) == 90.0, compute_k_factor(7, 60) == 135.0
    - All existing test_elo.py tests continue to pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add PK mode to update_ratings() with pk_winner parameter (TDD)</name>
  <files>
    worldcup_predictor/src/elo.py
    worldcup_predictor/tests/test_elo.py
  </files>

  <read_first>
    worldcup_predictor/src/elo.py:37-86 (update_ratings current implementation)
    worldcup_predictor/tests/test_elo.py:36-98 (existing test patterns for update_ratings)
    .planning/phases/12-draw-handling-elo-math/12-RESEARCH.md:328-357 (Pattern 4: PK mode reference)
    .planning/phases/12-draw-handling-elo-math/12-CONTEXT.md (D-06, D-07, D-08)
  </read_first>

  <behavior>
    RED: Add `TestUpdateRatings.test_pk_split` and `test_pk_winner_invalid` tests BEFORE changing update_ratings:
    - `test_pk_split`: elos={"A": 2000, "B": 2000}, call update_ratings("A", "B", "A", elos, K=60, pk_winner="A") — assert A gets 2000 + 60*(0.75-0.5) = 2015.0, B gets 2000 + 60*(0.25-0.5) = 1985.0
    - `test_pk_winner_invalid`: call update_ratings("A", "B", None, elos, K=60, pk_winner="C") — assert raises ValueError matching "pk_winner"

    GREEN: Add `pk_winner: str | None = None` parameter to update_ratings() signature. Inside function, before the existing result_a logic, insert a pk_winner branch:
      if pk_winner is not None:
          if pk_winner == team_a: result_a = 0.75
          elif pk_winner == team_b: result_a = 0.25
          else: raise ValueError(f"pk_winner '{pk_winner}' must be '{team_a}' or '{team_b}'")
    Then skip the existing winner-based result_a logic (the pk_winner branch sets result_a directly, so the elif/else below should NOT override it). Use an if/elif/else structure: first check pk_winner, then winner is None (draw, result_a=0.5), then winner==team_a (1.0), then winner==team_b (0.0), then else (invalid winner ValueError).

    The key structural change: restructure result_a logic from a flat if/elif to a two-tier: (1) pk_winner check first, (2) fall through to existing winner checks only if pk_winner is None.
  </behavior>

  <action>
    Step 1 — Write failing tests (RED): Add to TestUpdateRatings class in test_elo.py:
    - `test_pk_split`: Equal elos 2000/2000, update_ratings("A","B","A",{"A":2000,"B":2000},K=60,pk_winner="A") → A=2015.0, B=1985.0
    - `test_pk_winner_invalid`: update_ratings("A","B",None,{"A":2000,"B":1900},K=60,pk_winner="C") → pytest.raises(ValueError, match="pk_winner")

    Step 2 — Confirm RED: `pytest tests/test_elo.py::TestUpdateRatings::test_pk_split -x -q` — MUST fail with TypeError: unexpected keyword argument 'pk_winner'.

    Step 3 — Implement (GREEN): Modify update_ratings in elo.py:
    - Change signature: add `pk_winner: str | None = None` after K parameter.
    - Restructure result_a computation at lines 69-78 from flat if/elif to pk-winner-first:
      ```
      if pk_winner is not None:
          if pk_winner == team_a:
              result_a = 0.75
          elif pk_winner == team_b:
              result_a = 0.25
          else:
              raise ValueError(f"pk_winner '{pk_winner}' must be '{team_a}' or '{team_b}'")
      elif winner is None:
          result_a = 0.5
      elif winner == team_a:
          result_a = 1.0
      elif winner == team_b:
          result_a = 0.0
      else:
          raise ValueError(...)
      ```

    Step 4 — Confirm GREEN: `pytest tests/test_elo.py::TestUpdateRatings::test_pk_split -x -q` — must pass. Run all UpdateRatings tests: `pytest tests/test_elo.py::TestUpdateRatings -x -q` — all 11 tests pass.

    Step 5 — Verify backward compatibility: Run `pytest tests/test_elo.py -x -q` — all existing tests (draw, win, invalid_winner, custom_K, etc.) pass unchanged. The pk_winner parameter defaults to None, so existing callers are unaffected.
  </action>

  <verify>
    <automated>pytest tests/test_elo.py -x -q</automated>
  </verify>

  <done>
    - test_pk_split and test_pk_winner_invalid added and passing
    - update_ratings accepts pk_winner parameter with 0.75/0.25 split
    - Invalid pk_winner raises ValueError with descriptive message
    - All 9 existing update_ratings tests pass unchanged
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire K-multiplier and PK detection into apply_elo_update() (TDD)</name>
  <files>
    worldcup_predictor/src/elo.py
    worldcup_predictor/tests/test_elo.py
  </files>

  <read_first>
    worldcup_predictor/src/elo.py:37-112 (update_ratings with new pk_winner param, current apply_elo_update)
    worldcup_predictor/tests/test_elo.py:100-117 (existing apply_elo_update tests)
    .planning/phases/12-draw-handling-elo-math/12-RESEARCH.md:465-509 (Pattern: PK mode integration in apply_elo_update)
    .planning/phases/12-draw-handling-elo-math/12-CONTEXT.md D-10 (step-function), D-11 (GD = abs diff), D-12 (all matches), D-13 (caller computes adjusted K)
  </read_first>

  <behavior>
    RED: Add tests to TestUpdateRatings (or a new class) BEFORE changing apply_elo_update:
    - `test_apply_elo_update_k_multiplier`: match with home_score=3, away_score=1 (GD=2) → adjusted_K = compute_k_factor(2, 60) = 90.0. For a win, check elo change reflects K=90 not K=60.
    - `test_apply_elo_update_pk`: match with is_draw=False, winner="A", home_score=1, away_score=1 (GD=0) → pk_winner should be "A", G=1, K stays 60. Result 0.75 for A.
    - `test_apply_elo_update_draw_no_scores` (backward compat): existing test without scores — GD=0 (default), G=1, K=60. Same expected value as before.

    GREEN: Modify apply_elo_update to:
    1. Extract home_score/away_score from match, compute GD = abs(home - away)
    2. Compute adjusted_K = compute_k_factor(GD, constants.K_FACTOR)
    3. Detect PK mode: if match.get("is_draw") is False and match.get("winner") is not None with GD == 0 → pk_winner = match["winner"]
    4. Pass K=int(round(adjusted_K)) and pk_winner to update_ratings()

    Import constants at top of elo.py (add `from src import constants` — consistent with other modules).
  </behavior>

  <action>
    Step 1 — Write failing tests (RED): Add these test methods to TestUpdateRatings class:
    - `test_apply_elo_update_k_multiplier`: teams={"A":{"elo":2000},"B":{"elo":1900}}, match={"team_a":"A","team_b":"B","winner":"A","home_score":3,"away_score":1} → GD=2 → K=90. Compute expected_a = 2000 + 90*(1.0 - expected_score(2000,1900)). Assert teams["A"]["elo"] == expected.
    - `test_apply_elo_update_pk`: teams={"A":{"elo":2000},"B":{"elo":2000}}, match={"team_a":"A","team_b":"B","winner":"A","is_draw":False,"home_score":1,"away_score":1} → PK mode. Expected: A = 2000 + 60*(0.75-0.5) = 2015.0, B = 2000 + 60*(0.25-0.5) = 1985.0.
    - `test_apply_elo_update_draw_gd0`: teams={"A":{"elo":2100},"B":{"elo":1800}}, match={"team_a":"A","team_b":"B","winner":None,"home_score":1,"away_score":1} → GD=0 → K=60. Expected same as existing test_apply_elo_update_draw.
    - Update `test_apply_elo_update_draw` at line 110: add home_score=1, away_score=1 to the match dict (currently has no scores). The expected values are GD=0→G=1→K=60, so assertion values remain unchanged.

    Step 2 — Confirm RED: `pytest tests/test_elo.py::TestUpdateRatings::test_apply_elo_update_k_multiplier -x -q` — MUST fail because apply_elo_update doesn't use K-multiplier yet.

    Step 3 — Implement (GREEN): Modify apply_elo_update in elo.py:
    - Add `from src import constants` at top of elo.py
    - Inside apply_elo_update, BEFORE calling update_ratings:
      ```
      goal_diff = abs(match.get("home_score", 0) - match.get("away_score", 0))
      adjusted_K = compute_k_factor(goal_diff, constants.K_FACTOR)
      pk_winner = None
      if not match.get("is_draw", True) and match.get("winner") is not None:
          pk_winner = match["winner"]
      ```
    - Change the update_ratings call to: `update_ratings(match["team_a"], match["team_b"], match.get("winner"), current_elos, K=int(round(adjusted_K)), pk_winner=pk_winner)`
    - Keep the existing import of math — add `from src import constants` as a new line.

    Step 4 — Confirm GREEN: `pytest tests/test_elo.py -x -q` — all tests pass including 3 new ones. The backward-compatible `test_apply_elo_update_mutates_teams` still passes because its match dict has no scores → GD=0 → G=1 → K=60 → unchanged behavior.

    Step 5 — Verify `int(round())` conversion: The step-function can produce fractional adjusted_K (e.g., GD=3 → 105.0 which is already int, but GD=4 → 112.5 → rounds to 112, GD=5 → 120.0). The `int(round(adjusted_K))` ensures the value is a valid int for the K parameter. Per D-10 footnote: "Points Change is rounded to the nearest integer" matching eloratings.net behavior.
  </action>

  <verify>
    <automated>pytest tests/test_elo.py -x -q</automated>
  </verify>

  <done>
    - apply_elo_update computes K-multiplier via compute_k_factor(goal_diff, K_FACTOR)
    - PK detection: when is_draw=False and winner is set (with GD=0), passes pk_winner to update_ratings
    - True draws (winner=None, any GD) are unaffected — GD=0→G=1→K=60
    - apply_elo_update passes K=int(round(adjusted_K)) to update_ratings
    - 3 new apply_elo_update tests pass; all 12+ existing tests pass
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elo engine → external | compute_k_factor is a pure function with no I/O; update_ratings receives typed parameters only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Spoofing | N/A | accept | No authentication/identity boundaries in this plan |
| T-12-02 | Tampering | N/A | accept | All inputs are typed function parameters; no external data flow |
| T-12-SC | Tampering | pip installs | accept | No new packages installed in this phase |

**Security enforcement status:** N/A — this plan adds pure math functions with no I/O, no network, no secrets.
</threat_model>

<verification>
1. `pytest tests/test_elo.py -x -q` — all 15+ tests pass (9 existing + 6 new)
2. compute_k_factor returns correct values for GD 0, 1, 2, 3, 7
3. update_ratings(pk_winner="A") produces 0.75/0.25 split
4. apply_elo_update with home/away_score computes adjusted K
5. apply_elo_update with is_draw=False + winner set + equal scores triggers PK mode
6. apply_elo_update with winner=None (true draw) unchanged — GD=0, K=60
</verification>

<success_criteria>
1. compute_k_factor() function exists in elo.py with 3-case step-function
2. update_ratings() accepts pk_winner parameter with 0.75/0.25 split
3. apply_elo_update() computes K-multiplier from goal difference and detects PK mode
4. All existing test_elo.py tests pass unchanged
5. 9+ new tests covering K-multiplier, PK mode, and backward compatibility
</success_criteria>

<output>
Create `.planning/phases/12-draw-handling-elo-math/12-01-SUMMARY.md` when done.
</output>
