---
phase: 04-validation-production-readiness
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - competitions/ucl/README.md
  - ARCHITECTURE.md
autonomous: false
requirements:
  - UCLV-05
  - UCLV-06
user_setup: []

must_haves:
  truths:
    - "Developer can read comprehensive UCL module documentation in competitions/ucl/README.md"
    - "Developer can read UCL architecture documentation in ARCHITECTURE.md"
    - "WC test suite remains green (613 pass, 1 skip) — regression verified"
    - "Euro simulation output unchanged — regression verified"
    - "Known limitations documented: synthetic fixtures, BSD API dependency"
    - "Documentation covers: architecture, usage, validation, benchmarks, known limitations, release notes"
  artifacts:
    - path: competitions/ucl/README.md
      provides: "Full UCL module documentation"
      min_lines: 80
    - path: ARCHITECTURE.md
      provides: "UCL architecture documentation"
      min_lines: 50
  key_links:
    - from: documentation
      to: BSD API fetcher
      via: "README.md documents --validate flag and BSD_API_KEY env var"
    - from: documentation
      to: known limitations
      via: "README.md documents D-08 synthetic fixtures limitation"

---

<objective>
**As a** developer, **I want to** verify regression and read comprehensive documentation, **so that** the engine is proven correct and maintainable before v2 features.

This plan finalizes Phase 4 with three deliverables: (1) full UCL module documentation in `competitions/ucl/README.md`, (2) project-level architecture documentation in `ARCHITECTURE.md`, and (3) regression verification that WC test suite (613 pass, 1 skip) remains green after all Phase 4 changes. The synthetic fixtures limitation (D-08) is documented. The Euro simulation output baseline must be checked for unchanged behavior.

Purpose: Make the UCL module discoverable, documented, and proven regression-free before v2 development begins.
Output: `competitions/ucl/README.md`, `ARCHITECTURE.md`, regression verification results
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/competitions/ucl/README.md
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/competitions/ucl/result.py
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/competitions/ucl/main.py
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/competitions/ucl/src/fetcher.py
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/competitions/ucl/data/fixtures.json
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/competitions/ucl/benchmarks/benchmark_simulation.py
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/competitions/ucl/benchmarks/BENCHMARK_RESULTS.md
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/.planning/ROADMAP.md
@C:/Users/KIIT0001/Documents/antigravity skills/FOOTBALL/.planning/REQUIREMENTS.md

<interfaces>
Project structure overview for documentation:

```
competitions/ucl/
├── __init__.py
├── main.py                  # CLI entry point (ucl-predict)
├── result.py                # SimulationResult dataclass
├── display.py               # Formatted output (league table, bracket, odds, validation)
├── README.md                # Module documentation (this file)
├── data/
│   ├── fixtures.json        # UCL 25/26 fixture schedule (synthetic per D-08)
│   ├── team_aliases.json    # BSD name -> canonical name mapping
│   ├── bracket_rules.json   # R16 bracket pairing rules
│   ├── playoff_pairings.json # Playoff draw pairings
│   └── uefa_coefficients.json
├── src/
│   ├── __init__.py
│   ├── simulation.py        # MC simulation + league phase
│   ├── elo_fetcher.py       # ClubElo rating fetch
│   ├── groups.py            # Swiss system standings + tiebreakers
│   ├── knockout.py          # Two-legged ties, playoff, bracket, knockout tree
│   ├── validation.py        # Fixture schedule validation
│   └── fetcher.py           # BSD API data fetch (Phase 4)
├── benchmarks/
│   ├── __init__.py
│   ├── benchmark_simulation.py  # Performance benchmark (Phase 4)
│   └── BENCHMARK_RESULTS.md     # Latest benchmark results
├── tests/
│   ├── conftest.py
│   ├── test_cli.py
│   ├── test_display.py
│   ├── test_fetcher.py
│   ├── test_fixture_validation.py
│   ├── test_knockout.py
│   ├── test_monte_carlo.py
│   ├── test_simulation.py
│   ├── test_swiss_tiebreakers.py
│   └── test_validation.py
└── display.py

football_core/
├── evaluation.py            # Shared accuracy metrics (Phase 4)
└── ...

competitions/worldcup/
└── src/evaluation.py        # Imports from football_core.evaluation (Phase 4)
```

CLI usage (from main.py):
```
ucl-predict [-h] [-n N] [-s SEED] [-o FILE] [--validate] [--api-key KEY]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write comprehensive UCL README.md</name>
  <files>competitions/ucl/README.md</files>

  <read_first>
  - competitions/ucl/README.md (current — 3 lines, "Coming soon.")
  - competitions/ucl/main.py (CLI flags and usage)
  - competitions/ucl/src/fetcher.py (BSD validation feature — created in 04-01)
  - competitions/ucl/result.py (SimulationResult contract)
  - competitions/ucl/benchmarks/benchmark_simulation.py (benchmark docs)
  - .planning/ROADMAP.md (phase descriptions)
  - .planning/REQUIREMENTS.md (requirements for reference)
  </read_first>

  <action>
  Replace `competitions/ucl/README.md` with full module documentation following this structure:

  ```markdown
  # UCL Predictor

  Monte Carlo simulation engine for the UEFA Champions League 2025/26 season (36-team Swiss system format).

  ## Overview

  The UCL Predictor simulates the complete UCL tournament — league phase (8 matches per team, pot-constrained opponents), playoff round (positions 9–24), seeded R16 bracket with top-4 protection, quarter-finals, semi-finals, and final — using Poisson-based match simulation driven by ClubElo ratings.

  **Key capabilities:**
  - 36-team Swiss-system league table with correct UCL tiebreaker chain
  - Two-legged knockout ties with extra time and penalty shootouts
  - Seeded R16 bracket with exact UEFA pairing rules
  - Monte Carlo simulation for per-team advancement/elimination probabilities
  - Live validation against BSD API match results
  - JSON export for downstream analysis

  ## Architecture

  The UCL module follows the competition module pattern: standalone, importable, with zero modifications to `football_core`. All data files live in `competitions/ucl/data/`. The display layer reads exclusively from the `SimulationResult` contract (no simulation imports in display code).

  ```
  ucl-predict CLI (main.py)
    ├── Simulation orchestration (build_simulation_result)
    │   ├── run_monte_carlo()         — N-iteration simulation
    │   ├── simulate_league_phase()   — Single league phase snapshot
    │   └── simulate_knockout_tree()  — Full knockout pipeline
    ├── Display (display.py)
    │   ├── print_summary()
    │   ├── print_league_table()      — ANSI-colored zone highlighting
    │   ├── print_playoff_rounds()
    │   ├── print_knockout_bracket()  — Round-by-round match list
    │   ├── print_odds()              — Champion/qualification probabilities
    │   └── print_validation_summary()— Accuracy metrics (--validate)
    └── Validation (src/fetcher.py)
        └── fetch_ucl_matches()       — BSD API live match results
  ```

  ## Usage

  ### Basic simulation
  ```
  python -m competitions.ucl.main -n 10000
  ```

  ### Reproducible run with seed
  ```
  python -m competitions.ucl.main -n 10000 -s 42
  ```

  ### JSON export
  ```
  python -m competitions.ucl.main -n 10000 -o results.json
  ```

  ### Validate predictions against real results
  ```
  export BSD_API_KEY=your_key_here
  python -m competitions.ucl.main -n 10000 --validate
  ```

  ### Validate with JSON enrichment
  ```
  python -m competitions.ucl.main -n 10000 --validate -o results_with_validation.json
  ```

  **CLI flags:**
  | Flag | Description | Default |
  |------|-------------|---------|
  | `-n, --iterations` | Monte Carlo iterations | 10000 |
  | `-s, --seed` | Random seed for reproducibility | random |
  | `-o, --output` | JSON output file path | (none) |
  | `--validate` | Cross-check predictions against BSD match results | off |
  | `--api-key` | BSD API key (overrides BSD_API_KEY env var) | env var |

  ### Benchmark
  ```
  python competitions/ucl/benchmarks/benchmark_simulation.py
  ```

  ## Data Sources

  - **ClubElo ratings** (footballclubelo.com) — Team strength ratings fetched once per run, cached for reproducibility.
  - **BSD API** (bzzoiro.com) — Live match results for validation against real outcomes. League ID: 7 (UCL 25/26). API key required.
  - **Fixture schedule** — `data/fixtures.json` contains the synthetic 36-team fixture schedule.

  ## Validation

  The `--validate` flag fetches completed UCL match results from the BSD API, computes home-win probability for each match using `expected_score()` from Elo ratings, and compares against actual outcomes. Metrics:

  - **Brier Score** — Mean squared error between predicted probability and actual outcome (0.0 = perfect)
  - **Log Loss** — Logarithmic loss, penalizes confident wrong predictions heavily
  - **Accuracy** — Binary correctness thresholded at 50%
  - **Calibration ECE** — Expected Calibration Error, measures how well predicted probabilities match observed frequencies
  - **Market Odds Comparison** — Same metrics computed for BSD pre-match odds (after vig removal)

  Validation output appears as a summary table on stdout and, if `--output` is used, enriches the JSON export with a `"validation"` section.

  ## Benchmarks

  Latest performance measurements for the full UCL Monte Carlo simulation pipeline (see `benchmarks/BENCHMARK_RESULTS.md`):

  | Iterations | Time (s) |
  |------------|----------|
  | 1,000 | *see BENCHMARK_RESULTS.md* |
  | 10,000 | *see BENCHMARK_RESULTS.md* |
  | 50,000 | *see BENCHMARK_RESULTS.md* |

  ## Tests

  UCL module: `pytest competitions/ucl/tests/ -x --timeout=60`
  Full validation regression: `pytest competitions/worldcup/tests/ -x --timeout=120`

  ## Known Limitations

  - **Synthetic fixture schedule** — Validation performed with a synthetic fixture schedule. The 36-team Swiss-system draw for 2025/26 UCL had not been conducted at the time of development. Re-run validation with the official schedule when available.
  - **BSD API dependency** — Validation requires internet connectivity and a valid BSD API key. Without either, `--validate` prints a clear error and exits.
  - **Single Elo signal** — Predictions are based solely on ClubElo ratings. Multi-signal blending (CatBoost, market odds, etc.) is a Phase 5 differentiator.
  - **No injury/suspension modeling** — Squad composition is not modeled; Elo reflects team strength only.
  - **No pyproject.toml/pip package** — The engine is run from source. Packaging is deferred until 3+ competition modules are stable.

  ## Release Notes

  ### v1.0 (current)
  - Phase 1: 36-team Swiss-system league phase with correct tiebreaker chain
  - Phase 2: Two-legged knockout playoff, seeded R16 bracket, full knockout tree
  - Phase 3: `ucl-predict` CLI with formatted output and JSON export
  - Phase 4: BSD API validation, accuracy metrics, performance benchmarks, full documentation

  ### v2 (planned)
  - What-if scenario analysis
  - Competition differentiators (player form, signal blending)
  - Additional competition support (La Liga, Premier League)
  ```

  **Formatting rules:**
  - Use ATX headings (`##`, `###`, `####`)
  - Use fenced code blocks for code examples
  - Use tables for CLI flags and measurement data
  - Keep line lengths reasonable (~100 chars)
  </action>

  <verify>
  <automated>python -c "lines = open('competitions/ucl/README.md').readlines(); assert len(lines) >= 80, f'README too short: {len(lines)} lines'; assert '## Overview' in open('competitions/ucl/README.md').read(); print('OK')"</automated>
  </verify>

  <acceptance_criteria>
  - `competitions/ucl/README.md` exists with >= 80 lines
  - Contains sections: Overview, Architecture, Usage, Data Sources, Validation, Benchmarks, Tests, Known Limitations, Release Notes
  - Usage section documents `-n`, `-s`, `-o`, `--validate`, `--api-key` flags
  - Known Limitations section includes synthetic fixtures limitation (per D-08)
  - Architecture section describes the module structure and component roles
  - Validation section documents Brier, Log Loss, accuracy, calibration ECE, market odds comparison
  - Table of CLI flags with descriptions and defaults
  </acceptance_criteria>

  <done>
  UCL README.md replaced with full documentation covering architecture, usage, validation, benchmarks, known limitations, and release notes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ARCHITECTURE.md</name>
  <files>ARCHITECTURE.md</files>

  <read_first>
  - competitions/ucl/main.py (CLI flow)
  - competitions/ucl/result.py (SimulationResult contract)
  - competitions/ucl/src/simulation.py (MC engine)
  - competitions/ucl/src/knockout.py (knockout pipeline)
  - competitions/ucl/src/fetcher.py (BSD integration)
  - football_core/evaluation.py (extracted metrics)
  - .planning/ROADMAP.md (phase descriptions)
  </read_first>

  <action>
  Create `ARCHITECTURE.md` at the project root (not inside `competitions/ucl/`). This document covers the UCL module architecture for the project.

  ```markdown
  # Architecture: UCL Predictor Module

  ## Overview

  The UCL Predictor is a competition module for the football prediction engine, implementing the 2025+ UEFA Champions League format (36-team Swiss system). It follows the established competition module pattern: self-contained, zero modifications to `football_core`, data-driven configuration.

  ## Module Layout

  All UCL code lives under `competitions/ucl/`:
  - `main.py` — CLI entry point (`ucl-predict`), orchestration
  - `result.py` — `SimulationResult` dataclass (contract between simulation and display)
  - `display.py` — Formatted output (imports only `result.py`)
  - `src/simulation.py` — Monte Carlo simulation core
  - `src/groups.py` — Swiss system standings + tiebreaker chain
  - `src/knockout.py` — Two-legged ties, playoff, bracket, knockout tree
  - `src/elo_fetcher.py` — ClubElo rating fetch
  - `src/fetcher.py` — BSD API live match data (validation)
  - `data/` — JSON data files (fixtures, pairings, bracket rules, aliases, coefficients)
  - `benchmarks/` — Performance benchmark script + results
  - `tests/` — pytest test suite (129+ tests)
  - `README.md` — Module documentation

  ## Data Flow

  1. **CLI** (`main.py`) parses flags, loads fixture data from `data/fixtures.json`, fetches Elo ratings
  2. **Simulation** (`build_simulation_result`) runs Monte Carlo (`run_monte_carlo`) for aggregated probabilities + one representative iteration for bracket display
  3. **Display** (`display.py`) reads `SimulationResult` and prints formatted output
  4. **Validation** (optional): BSD API fetcher fetches real match results, `run_validation` cross-checks predictions using Elo-based `expected_score()`, metrics computed by `football_core/evaluation.py`

  ## Key Contracts

  ### SimulationResult (result.py)
  Frozen dataclass, JSON-native types throughout. Serves as the API boundary between simulation internals and display/export. Fields: standings, teams (probabilities), playoff_ties, bracket_rounds, stages (eliminated→champion).

  ### evaluation.py (football_core/)
  Shared accuracy metrics extracted from WC. Five functions: brier_score, log_loss, compute_metrics, calibration_curve, expected_calibration_error.

  ## Simulation Pipeline

  1. Swiss-system league phase (8 matches, pot constraints, 10-step tiebreaker)
  2. Playoff round (positions 9–24, 8 ties)
  3. R16 bracket construction (seeded: 1/2 → 15/18, 3/4 → 13/20, etc.)
  4. Knockout tree (R16 → QF → SF → Final)
  5. Stage tracking (all 36 teams: eliminated → champion)

  ## Validation Pipeline

  1. BSD API fetch → normalize team names via alias lookup
  2. Match BSD events to fixture schedule entries
  3. For each real match: compute `expected_score(home_elo, away_elo)` → home-win probability
  4. Compare against actual outcome: Brier, Log Loss, accuracy, calibration
  5. Compare against pre-match market odds (BSD odds_home/odds_draw/odds_away, vig removed)

  ## Dependencies

  - Python 3.11+ stdlib (no external ML dependencies)
  - `requests` for BSD API and ClubElo HTTP calls
  - `football_core` for fetcher (`fetch_raw_matches`), Elo (`expected_score`), evaluation metrics, and odds (`remove_vig`)

  ## Design Decisions

  - **D-01:** `--validate` flag on `ucl-predict` — not a standalone script
  - **D-04/D-05:** Accuracy metrics extracted to `football_core/evaluation.py` (Rule of Two: WC + UCL)
  - **D-06/D-07:** Benchmark script at `competitions/ucl/benchmarks/benchmark_simulation.py`
  - **D-08:** Synthetic fixture schedule — documented limitation
  - **D-17:** Display layer imports only `result.py` — no simulation internals
  ```

  **What to AVOID:**
  - Do NOT include Euro or WC architecture details (this is UCL-specific)
  - Do NOT include v2 feature plans (deferred items stay in ROADMAP)
  - Do NOT include user-facing instructions (those belong in README.md)
  </action>

  <verify>
  <automated>python -c "lines = open('ARCHITECTURE.md').readlines(); assert len(lines) >= 50, f'ARCHITECTURE too short: {len(lines)} lines'; assert '## Data Flow' in open('ARCHITECTURE.md').read(); print('OK')"</automated>
  </verify>

  <acceptance_criteria>
  - `ARCHITECTURE.md` exists at project root with >= 50 lines
  - Contains sections: Overview, Module Layout, Data Flow, Key Contracts, Simulation Pipeline, Validation Pipeline, Dependencies, Design Decisions
  - Module Layout lists all key files and their roles
  - Key Contracts documents SimulationResult and evaluation.py
  - Design Decisions references D-nn decision IDs from planning
  - No v2 feature plans included
  - No user-facing CLI usage instructions (those go in README.md)
  </acceptance_criteria>

  <done>
  ARCHITECTURE.md created at project root documenting UCL module architecture, data flow, contracts, and design decisions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: WC regression verification + test suite validation</name>
  <files>(no files modified — verification-only task)</files>

  <read_first>
  - (no files needed — this is a verification task)
  </read_first>

  <what-built>
  All Phase 4 changes: football_core/evaluation.py extraction, WC evaluation.py import migration, UCL BSD fetcher, validation engine in main.py, display additions, benchmark script, README.md and ARCHITECTURE.md documentation.
  </what-built>

  <how-to-verify>
  1. **WC regression test:** Run the full WC test suite and confirm output shows "613 passed, 1 skipped":
  ```
  python -m pytest competitions/worldcup/tests/ -x --timeout=120 -v 2>&1 | tail -10
  ```
  Expected: `613 passed, 1 skipped` (or higher count if tests added). If test count drops below 613, the evaluation.py extraction broke backward compatibility.

  2. **UCL full suite:** Run the full UCL test suite (should include new fetcher and validation tests):
  ```
  python -m pytest competitions/ucl/tests/ -x --timeout=120 -v 2>&1 | tail -10
  ```
  Expected: All tests pass (count should be > 129 from Phase 3 baseline).

  3. **Euro simulation unchanged:** Run a quick Euro sim check:
  ```
  python -c "from competitions.euro.src.simulation import simulate; result = simulate(seed=42); print(f'Euro champion: {result.get(\"champion\", \"N/A\")}')"
  ```
  This verifies the Euro module still imports and runs without error after any Phase 4 changes.

  4. **Documentation review (manual):** Open `competitions/ucl/README.md` and verify:
  - All CLI flags documented
  - Validation feature documented
  - Known limitations section includes synthetic fixtures
  - Benchmark section references BENCHMARK_RESULTS.md

  5. **ARCHITECTURE.md review (manual):** Open `ARCHITECTURE.md` and verify:
  - Module layout covers all files
  - Data flow describes CLI → simulation → display → validation pipeline
  - Design decisions reference decision IDs
  </how-to-verify>

  <resume-signal>
  Type "regression verified" if WC tests pass (613 passed, 1 skipped), UCL tests pass, Euro sim runs without error, and documentation looks correct. Describe any issues if not.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | No code changes — no trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-05 | Repudiation | README/ARCHITECTURE docs | mitigate | Docs reflect current implementation; architecture diagram matches code; synthetic fixtures limitation explicitly documented (D-08) |
| T-4-SC | Tampering | no packages installed | accept | No npm/pip/cargo installs in this plan |
</threat_model>

<verification>
1. `python -m pytest competitions/worldcup/tests/ -x --timeout=120 2>&1 | grep -E "passed|failed|error"` — WC suite green
2. `python -m pytest competitions/ucl/tests/ -x --timeout=120 2>&1 | grep -E "passed|failed|error"` — UCL suite green
3. `python -c "from competitions.euro.src.simulation import simulate; simulate(seed=42)"` — Euro imports work
4. `python -c "open('competitions/ucl/README.md').read().count('##') >= 6"` — README has all sections
5. `python -c "open('ARCHITECTURE.md').read().count('##') >= 5"` — ARCHITECTURE has all sections
</verification>

<success_criteria>
- WC test suite: 613 passed, 1 skipped (or higher count with extra tests, but never lower)
- UCL test suite: all tests pass (129+ baseline + new fetcher/validation/CLI tests)
- Euro simulation imports and runs without error
- README.md documents architecture, usage, validation, benchmarks, known limitations
- ARCHITECTURE.md documents module layout, data flow, key contracts, design decisions
- Human has verified documentation is complete and correct
</success_criteria>
